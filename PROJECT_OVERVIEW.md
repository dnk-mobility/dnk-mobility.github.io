# 공정 설비 정보관리 시스템 — 프로젝트 개요

> 후공정 생산기술팀 · e-AWD 70kW MOTOR HOUSING ASS'Y 라인
> 최종 업데이트: 2026-09-22 (챗봇 위젯 탐색 구조 개편 — 헤더에 홈 버튼 추가, 닫으면 대화 초기화, 답변마다 반복되던 4버튼 메뉴 제거) — 이전: 2026-09-22 (챗봇 위젯의 진해졌다 흐려지는 시안 글로우 펄스를 PC hover 전용에서 모든 화면(터치 포함) 상시 재생 앰비언트 애니메이션으로 전환 — 스마트폰엔 hover 자체가 없어 반응 없던 문제 해결) — 이전: 2026-09-22 (설비 페이지 10개 상단 id-bar 텍스트를 지정된 지점에서 줄바꿈 — `DnK MOBILITY`/`후공정 생산기술팀`, `e-AWD 70kW`/`MOTOR HOUSING ASS'Y`) — 이전: 2026-09-22 (설비 페이지 10개에 index.html로 가는 홈 아이콘 버튼 추가, index.html 자체의 PC 가로폭 불일치 수정 — 아코디언 섹션 색상 구분은 시도했다가 사용자 피드백으로 되돌림) — 이전: 2026-09-22 (설비 부착용 QR 라벨에 "전체 설비 인덱스"(index.html)로 바로 가는 라벨 추가 — 기존엔 개별 설비 10개 라벨만 있었음) — 이전: 2026-09-22 (기술문서 PDF가 PC에서 안 열리고 모바일에서 흐릿하던 문제 수정 후 커밋·푸시하여 실제 배포 사이트에 반영 — 최초 수정은 로컬에만 있어 사용자가 같은 증상을 재보고했었음) — 이전: 2026-09-22 (PC 화면에서 상단 헤더와 본문 가로폭이 안 맞던 문제 수정 — `.id-bar`/`.doc-header`에 `max-width:640px` 추가) — 이전: 2026-09-22 (여러 파일로 나뉜 기계도면 병합·업로드 — `leak-pre-flow-drawing.pdf`, pdf.js 뷰어에 페이지별 제한시간 방어 로직 추가) — 이전: 2026-09-22 (첫 기술문서 PDF 업로드분 파일명 오류 정정 — `leak-pre-flow-circuit.pdf`) — 이전: 2026-09-21 (기술문서 PDF 2차 암호 반복 요구 버그 수정 — PDF 링크를 새 탭이 아닌 같은 탭에서 열도록 변경) — 이전: 2026-09-18 (첨부문서를 설비별 폴더 구조로 재편 + 맨 위로 이동 버튼 추가 + 설비 9개소 트러블슈팅 이력 대량 보강(엑셀 원본 대조로 정정 포함) + 마스터 샘플·보정 이력 섹션을 누설값관리시스템으로 이관) — 이전: 2026-09-17 (정밀 재점검·최적화 + 챗봇형 안내 위젯 + 재발방지 체크리스트 + 설비 부착용 QR 라벨 확정(이후 70×100mm로 재조정) + 진입 인트로 전 진입점 확장)

이 문서는 현재 구축되어 있는 "공정 설비 정보관리 시스템"의 구조·설계·운영 방식을 상세히 기록한 기술 참고 문서입니다. 향후 유지보수나 확장 작업 시 이 문서를 먼저 참고하면 됩니다.

---

## 1. 프로젝트 목적

설비에 부착한 QR코드를 스마트폰으로 스캔하면, 해당 설비의 작업표준·검사규격·일상점검표·트러블슈팅 이력 등을 모바일 웹페이지로 즉시 확인할 수 있게 하는 시스템. 종이/PDF 문서를 캐비닛에서 찾아야 하는 기존 방식을 대체하는 것이 목표.

---

## 2. 배포 구조 (호스팅)

| 항목 | 내용 |
|---|---|
| 저장소(repository) | [`dnk-mobility/dnk-mobility.github.io`](https://github.com/dnk-mobility/dnk-mobility.github.io) |
| 브랜치 | `main` (단일 브랜치, 별도 dev/staging 브랜치 없음) |
| 배포 방식 | GitHub Pages (저장소 이름이 `<계정 또는 조직>.github.io` 형식이면 자동으로 루트 URL에 배포됨) |
| 실서비스 주소 | https://dnk-mobility.github.io/ |
| 빌드 도구 | 없음 — 순수 정적 HTML/CSS/JS 파일. `main`에 push되면 수 분 내 자동 반영 |
| 프레임워크 | 없음 (React/Vue 등 미사용, 바닐라 HTML + 인라인 `<style>` + 바닐라 JS) |
| 로컬 개발 환경 | `C:\Users\user\Desktop\후공정 설비 QR 코드 스마트폰 관리 시스템\후공정 설비 정보 시스템 프로젝트` |

배포 절차는 `git add` → `git commit` → `git push origin main` 세 단계뿐이며, 별도 배포 서버·CI 파이프라인이 없습니다.

**2026-09-16, 개인 계정(`dongnam959`) → 조직 계정(`dnk-mobility`)으로 이전했습니다.** 이전 주소는 `dongnam959.github.io` (개인 계정 소유 저장소는 `dongnam959/dongnam959.github.io`, 특수 케이스로 루트 URL에 자동 배포되던 것). 조직으로 옮긴 이유: 개인 계정 하나에 시스템이 묶여 있으면 그 사람이 퇴사할 때 인수인계가 어려워짐. 조직은 공동 관리자를 여러 명 등록할 수 있어 이 문제가 없음. QR 라벨을 아직 현장에 부착하기 전이라 재출력 비용 없이 지금 옮길 수 있었음 (라벨 부착 후였다면 11개 전부 재출력해야 했음). 같은 이유로 신설 중인 "e-AWD 모터 하우징 누설값 관리 시스템"(별도 저장소)도 같은 조직으로 함께 이전.

---

## 3. 사이트맵 (전체 11개 페이지)

공정 흐름(마킹 → 조립전 검사 → 조립 → 조립후 검사·최종검사) 순서로 정렬.

| 공정No. | 설비명 | 파일명 | 문서번호 | 아코디언 섹션 수 |
|---|---|---|---|---|
| — | (홈/메뉴) | `index.html` | — | 해당 없음 |
| 110 | 레이저 마킹기 | `laser-marking.html` | DnK-250915-001 | 8 |
| 120 | 조립전 유로계 리크검사기 | `leak-pre-flow.html` | DnK-250915-002 | 10 |
| 130 | 조립전 전체계 리크검사기 | `leak-pre-full.html` | DnK-250915-003 | 10 |
| 140 | 오일니쁠 압입기 / 테이퍼 플러그 체결기(1개소) | `nipple-oil-press.html` | DnK-250915-004 | 8 |
| 150 | 씰캡 압입기 | `cap-sealing-press.html` | DnK-250915-005 | 8 |
| 160 | 테이퍼 플러그 체결기(5개소) | `taper-plug-fastening.html` | DnK-250915-006 | 8 |
| 170 | 다월핀 압입기 | `dowel-pin-press.html` | DnK-250915-007 | 8 |
| 180 | 조립후 유로계 리크검사기 | `leak-post-flow.html` | DnK-250915-008 | 10 |
| 190 | 조립후 전체계 리크검사기 | `leak-post-full.html` | DnK-250915-009 | 10 |
| 200 | 테이퍼 플러그 돌출높이 검사기 | `taper-plug-height.html` | DnK-250915-010 | 8 |

> `leak-pre-flow.html`은 원래 이 프로젝트의 최초 파일인 `index.html`이었습니다. 홈/메뉴 페이지를 새로 만들면서 `leak-pre-flow.html`로 이름을 옮기고, `index.html`은 전체 목록 페이지로 새로 제작했습니다 (2026-07-30, 커밋 `8a49c52`).

---

## 4. 디자인 시스템

**설비 페이지 10개는 공용 스타일시트 `styles.css` 하나를 공유합니다** (2026-09-03 추출 — 이전에는 동일한 `<style>` 블록 10.6KB가 10개 파일에 복사되어 있어 디자인 수정 시 전 파일을 일괄 수정해야 했음). 디자인을 바꿀 때는 `styles.css`만 고치면 전 설비 페이지에 반영됩니다. 홈(`index.html`)과 PDF 뷰어(`pdf-viewer.html`)는 레이아웃이 달라 각자의 인라인 `<style>`을 유지합니다 — **단, `.id-bar`/`.doc-header`의 `max-width:640px` 규칙만은 세 곳(styles.css, index.html, pdf-viewer.html은 다른 구조라 해당 없음) 모두에 각자 들어있어야 합니다.** `styles.css`만 고치고 `index.html`은 안 고치는 식으로 한쪽만 수정하면 그 페이지만 PC에서 폭이 어긋나는 회귀가 생깁니다(2026-09-22 실제 사례, §17 참고). `table.hist`(이력 표) 규칙은 리크테스터 4개소에서만 쓰이지만 무해하므로 공용 파일에 포함되어 있습니다.

### 색상 팔레트 (CSS 변수)

| 변수 | 값 | 용도 |
|---|---|---|
| `--navy` | `#0a2540` | 브랜드 메인 컬러, 상단 바·헤더·강조 요소 |
| `--navy-2` | `#0f3255` | 보조 네이비 |
| `--steel` | `#1a4971` | 보조 |
| `--accent` | `#0f6cb0` | 타임라인 강조선 |
| `--gray-900`~`--gray-100` | `#181d22` ~ `#f4f6f8` | 본문/보조 텍스트, 배경 단계별 회색조 |
| `--ok-bg` / `--ok-text` | `#eaf4ee` / `#1c6b41` | 정상·완료 상태 태그 |
| `--warn-bg` / `--warn-text` | `#fbf3e2` / `#8a5a12` | 주의·경고 콜아웃 |
| `--line` | `#d3d8de` | 테두리/구분선 |

이 팔레트는 실제 DnK 모빌리티 로고(네이비+시안) 톤에 맞춰 선정했으며, 이후 PPT 보고자료 표지 및 접속 암호 화면 디자인에도 동일하게 사용했습니다.

### 타이포그래피 & 레이아웃

- 폰트: `-apple-system, BlinkMacSystemFont, "Malgun Gothic", "Apple SD Gothic Neo", sans-serif`
- 뷰포트: `max-width: 640px` 중앙 정렬 — 모바일 브라우저 기준으로 설계 (데스크톱에서도 문제없이 보이지만 최적화 대상은 스마트폰)
- **확대/축소 허용** (`width=device-width, initial-scale=1.0`) — 2026-09-03에 `maximum-scale=1.0`을 제거했습니다. 12px 미만 글자가 190곳이나 되고(마스터 샘플 표는 11.5px, 표 헤더는 10px) 현장에서 장갑 끼고 보는 화면이라, 손가락으로 확대가 안 되는 게 실제 불편이었습니다. 확대를 막는 건 접근성 관점에서도 권장되지 않습니다.
  - 확대를 허용하면 iOS Safari가 **16px 미만 입력창에 포커스할 때 화면을 멋대로 확대**하므로, `gate.js`의 암호 입력창 글자를 15px → 16px로 올려 이 현상을 막았습니다. 사이트에서 입력창은 이것 하나뿐입니다.

### 인터랙션 (hover / tap, 2026-09-03 추가)

**이 사이트는 QR 스캔 → 스마트폰이 주 사용 환경이라, hover 효과를 그냥 걸면 안 됩니다.** 안드로이드 크롬 등에서 탭한 요소에 hover 상태가 눌어붙어(sticky hover) 다른 곳을 누를 때까지 계속 강조된 것처럼 보입니다. 그래서 모든 hover 규칙은 `@media (hover:hover) and (pointer:fine)` 안에만 두고, 터치용 피드백은 `:active`로 따로 정의했습니다.

| 대상 | 효과 |
|---|---|
| `.eq-card` (홈 카드 10장) | hover: `translateY(-3px)` + 그림자, 공정No. 배지가 `--navy` → `--accent`로 채워지며 `scale(1.06)`, 화살표 `translateX(5px)`, 설비명 네이비로 진해짐 / active: `scale(.985)` + 배지 채움 |
| `.doc-item` (첨부문서·기술문서 PDF 링크) | hover: `translateY(-2px)` + 그림자, 배경 `--gray-100` → 흰색, PDF 아이콘 `--accent`로 채움, 화살표 `translateX(4px)` / active: `scale(.99)` + 아이콘 채움 |
| `.section > summary` (아코디언 헤더) | hover/active: 왼쪽에 3px 세로 바가 위→아래로 차오르고(`::before` + `scaleY`) 배경 연회색, `padding-left` 14→19px. **바 색은 일반 항목 `--accent`(시안), `data-lock="2"` 잠금 항목은 `#c8981f`(주황)** — 잠김 여부를 색으로 한 번 더 알려줌 |

- `transform` / `opacity` / `box-shadow` 위주로만 움직여서 저사양 스마트폰에서도 부담이 적습니다. JS는 쓰지 않습니다.
- `@media (prefers-reduced-motion:reduce)`에서 전 transition을 끕니다.
- `.section > summary`는 `position:relative`가 필수입니다 (`::before` 바의 기준). summary가 `display:flex`라 `::before`를 `position:absolute`로 빼지 않으면 플렉스 아이템이 되어 레이아웃이 밀립니다.

### 공통 레이아웃 구조 (위→아래)

1. **`.id-bar`** — 네이비 바탕 최상단 바. 설비 페이지 10개는 `DnK MOBILITY<br>후공정 생산기술팀` / `e-AWD 70kW<br>MOTOR HOUSING ASS'Y`로 **의도적으로 줄바꿈**되어 있습니다(2026-09-22, 사용자 요청 — 자동 줄바꿈에 맡기면 폭에 따라 "· 후공정"처럼 어중간한 지점에서 끊길 수 있어 `<br>`로 지점을 고정). `index.html`은 이 줄바꿈이 적용되지 않은 한 줄 표기 그대로입니다(요청 범위가 "공정별 화면"으로 한정됨). **설비 페이지 10개는 맨 왼쪽에 집 모양 아이콘 버튼(`.home-btn`, 2026-09-22 추가)이 있어 눌러서 `index.html`(전체 설비 인덱스)로 바로 이동할 수 있습니다** — 이전엔 홈으로 돌아갈 방법이 브라우저 뒤로가기뿐이었음. 처음엔 상단 문서 라벨 텍스트를 링크로 만들었으나 "사용자가 클릭 가능한지 모를 수 있다"는 피드백에 따라 아이콘 버튼으로, 그다음엔 "모바일에서 잘 안 보인다"는 피드백에 따라 32×32px 시안(`#37c6e0`, QR 라벨과 동일한 브랜드 시안) 배경의 꽉 채운 집 아이콘으로 두 차례 키움. `index.html` 자신은 이 버튼이 없습니다(자기 자신으로 가는 링크는 불필요).
2. **`.doc-header`** — 네이비 배경, 문서 라벨(`공정 설비 정보관리 시스템 · EQUIPMENT INFORMATION MANAGEMENT SYSTEM`) + 페이지 제목(h1) + `.title-block`(흰 배경 요약표)
3. **`.title-grid`** — 2열 그리드 표. 공정명/공정No.(전체너비) → 품명 → 품번 → 설비명 → 문서번호 → 개정일자 → 상태 → 설비별 핵심 파라미터 4칸
4. **`<main>` 아코디언 섹션들** — `<details class="section">` (기본 닫힘 상태), 원 번호 아이콘(①~⑩) + 제목 + 화살표(▾)
5. **`<footer>`** — 브랜드명 + 안내 문구

### 아코디언 섹션 구성 (기본 8단계, 리크테스터는 조작매뉴얼 항목 추가로 9단계)

🔒 = 2차 암호가 필요한 항목 (§8 참고)

| 번호 | 섹션명 | 비고 |
|---|---|---|
| ① | 설비 개요 | 설비관리번호(공란)·문서버전·점검주기·최종업데이트. 개인 이름 미기재 |
| ② | 첨부 문서 | PDF 링크 3~4개 (§6 참고). 잠금 없음 |
| ③ | 기술문서 🔒 | **2026-09-03 신설, 전 설비 10개소 공통.** 설비 전기회로도·기계도면 PDF 링크 2개. `<details data-lock="2">`로 표시되어 펼칠 때 2차 암호를 물음 |
| ④ | 표준 작업 순서 | 번호 타임라인(`.flow`) + 안전주의사항 + 이상발생시 조치 + 부적합품 처리 |
| ⑤ | 검사 규격 및 파라미터 | 표 (항목/규격/주기) |
| ⑥ | 설비 일상점검표 체크 항목 | 전원부/구동부/안전장치/공정특성 구분, 주간·야간 체크란 |
| ⑦ | 조작 매뉴얼 요약 | **리크테스터(LS-R700/LS-R902) 설비 4개소만 존재** (120·130·180·190) |
| ⑧ | 트러블슈팅 이력 | 2026-09-18 기준 **10개소 중 9개소**(110·120·130·140·150·160·170·180·190)에 실사례 기재, 200(테이퍼 플러그 돌출높이 검사기)만 "등록된 사례 없음" — 출처: 사내 카카오톡 대화 로그 전수 검토 + 사용자 제공 설비 OPEN ISSUE 이력표(엑셀 시트1~3)·PPTX 분석자료. 전 사례 개선 완료 상태로 반영 |
| ⑨ | 최근 공지 | 현재 전 설비 "등록된 공지 없음" (수시 업데이트 영역) |

> **2026-09-18 변경**: 이전 ⑨ "마스터 샘플 · 보정 이력"(리크테스터 4개소) 섹션은 **삭제하고 별도 개발 중인 "누설값관리시스템"으로 이관**했습니다. 이 시스템은 정적 페이지라 실측값을 실제로 누적 입력할 방법이 없었고(HTML을 직접 고쳐야만 갱신), 계속 쌓이는 측정·보정 데이터는 데이터 관리가 가능한 전용 시스템에서 다루는 것이 맞다고 판단했습니다. 이관 시 필요한 데이터 구조·이력은 [`인계_마스터샘플_보정이력.md`](인계_마스터샘플_보정이력.md) 참고.

---

## 5. 홈 화면 (`index.html`) 구성

기존 설비 상세 페이지와 다른 전용 레이아웃(아코디언 없음, 카드 리스트 형태):

- 상단 통계 칩 3개: 등록 설비(10개소) / 공정 범위(No.110~200) / 문서번호(DnK-250915-001~010)
- 공정 흐름별 그룹 헤더 4개: **마킹 → 조립전 리크검사 → 조립 → 조립후 검사·최종검사**
- 각 그룹 안에 `.eq-card` 카드 리스트 (공정No. 배지 + 설비명 + 공정명 + 화살표), 클릭 시 해당 설비 페이지로 이동

### 진입 인트로 (`intro.js`, 2026-09-03 추가)

QR을 찍으면 **암호창보다 먼저** 약 4.5초짜리 인트로가 재생됩니다. 전체 진입 순서는 **QR 스캔 → 인트로 → 암호 → 콘텐츠**입니다.

**2026-09-17부터 설비 페이지 10개에도 로드됩니다** (`index.html`에서만 로드되던 것에서 확장). 처음에는 "매번 나오면 현장에서 방해되므로" 홈에만 뒀는데, 이후 QR을 홈을 거치지 않고 **설비별로 직접 찍어 들어가는 운영 방식**이 되면서, 그 경로로 들어가는 사람은 아예 인트로를 못 보는 게 이상해짐 — sessionStorage 세션당 1회 제한(§ 아래 표)이 이미 있어서, 어느 페이지로 처음 들어오든 그 세션에서 딱 한 번만 재생되고 이후 같은 탭에서 다른 설비로 이동해도 반복되지 않습니다.

연출 순서 — QR 조준선(코너 브래킷 4개) 페이드인 → 시안 스캔 라인이 위→아래로 1회 통과 → `DnK MOBILITY` / `공정 설비 정보관리` 텍스트가 떠오름 → 시안 밑줄 그어짐 → 전체 페이드아웃.

| 항목 | 처리 |
|---|---|
| 로드 순서 | 인트로가 있는 11개 페이지(홈+설비 10개) 모두 `<body>` 안에서 **`intro.js` → `gate.js` → `assistant.js` → `scroll-top.js`** 순서. 순서가 바뀌면 암호창이 먼저 뜸 |
| 게이트 연동 | `intro.js`가 `window.dnkIntro.done` Promise를 내보내고, `gate.js`가 그것을 기다렸다가 암호창을 띄움. 인트로가 없는 `pdf-viewer.html`·`404.html`은 `window.dnkIntro`가 없어 즉시 통과 |
| 전환 이음새 | 인트로(`z-index:99998`)와 암호창(`99999`) 배경이 같은 `#0a2540`. 인트로가 resolve된 뒤 페이드아웃되는 동안 암호창이 이미 그 위에 올라와 있어 끊김이 안 보임 |
| 실측 타이밍 | 페이지 로드 기준 — 밑줄 완성 2,791ms · 제목 완전 등장 3,271ms · 페이드아웃 시작 4,531ms · 암호창 등장 4,531ms · 인트로 DOM 제거 5,821ms |
| 종료 시점 | **고정 타이머가 아니라 5개 동작(`.ret .scan .brand .title .rule`)의 `animationend`를 세어서 전부 끝나면 종료.** 최초 로드 때는 CSS 애니메이션 시작이 수백 ms 밀려서 고정 타이머로 재면 마지막 동작이 잘림(실측 확인). 또 delay·duration을 바꾸면 "가장 늦게 끝나는 동작"이 달라지므로(현재는 밑줄이 아니라 제목) 하나만 지켜보면 안 됨. 전부 끝난 뒤 `TAIL_MS` 만큼 멈췄다가 종료하며 `TIMEOUT_MS`(6000ms) 안전장치가 있음 |
| 글자 읽는 시간 | **`TAIL_MS`(1000ms)가 결정합니다.** 텍스트 "등장(페이드인) 시간"을 늘리는 것으로는 해결되지 않음 — 글자가 완전히 또렷해지는 시점만 뒤로 밀리고 읽을 시간은 그대로입니다. `TAIL_MS` 300ms일 때 완전 가독 구간이 570ms에 불과해 "읽기 전에 사라진다"는 피드백이 있었고, 1000ms로 올려 1,260ms 확보 (여기에 페이드아웃 1.2초 동안도 어느 정도 읽힘) |
| 속도 조정 | `intro.js` 안 CSS의 animation delay·duration 을 조정하면 종료 시점은 자동으로 따라옴. 정지 시간은 `TAIL_MS`(1000), 페이드아웃 길이는 `FADE_MS`(1200) |
| 반복 | `sessionStorage` 키 `dnk_intro_shown_v1` — **세션당 1회**. 탭을 닫으면 초기화(암호 인증과 동일 수명) |
| 건너뛰기 | 화면 탭 시 즉시 종료 |
| 접근성 | `prefers-reduced-motion: reduce` 설정이면 아예 재생하지 않음 |
| 로딩 부담 | 외부 이미지·폰트 없이 CSS 도형만 사용 — 전송 용량 증가 없음. 인덱스 콘텐츠는 인트로가 도는 동안 뒤에서 이미 렌더링 완료 |

---

## 6. 첨부 문서 (PDF) 체계

> ⚠️ **파일명은 아래 표에 적힌 것과 글자 하나까지 정확히 같아야 합니다.** 알아보기 쉬우라고 다른 이름(`leak-pre-flow-electrical circuit diagram.pdf` 등)으로 올리면, 사이트는 그 파일을 "존재하지 않음"으로 인식합니다 — 폴더 위치는 몰라도 되지만(§6 표대로 넣으면 됨), 파일명만큼은 정확히 표의 이름 그대로(확장자 `.pdf` 포함) 저장해야 합니다. (2026-09-22, 실제로 이 문제로 문서가 안 뜬 사례 있음 — `개선이력.md` 참고)

**2026-09-18부터 설비별 폴더로 관리합니다.** 전에는 프로젝트 루트에 파일을 전부 평면으로 뒀는데, 업로드할 PDF가 46개(첨부문서 26 + 기술문서 20)나 되면 나중에 뭐가 뭔지 헷갈릴 것 같다는 판단으로, 실제 업로드가 본격적으로 시작되기 전에 아래처럼 정리했습니다.

```
첨부문서/
  110_레이저마킹기/
  120_조립전유로계리크검사기/
  130_조립전전체계리크검사기/
  140_오일니쁠압입기·테이퍼플러그체결기(1개소)/
  150_씰캡압입기/
  160_테이퍼플러그체결기(5개소)/
  170_다월핀압입기/
  180_조립후유로계리크검사기/
  190_조립후전체계리크검사기/
  200_테이퍼플러그돌출높이검사기/
  공용_LS-R700디텍터/   ← 120·130·180이 같은 디텍터 매뉴얼(설치/조작)을 공유해서 씀
```

- 폴더명 앞에 **공정No.**를 붙여 사이트에 보이는 순서와 그대로 맞춤.
- **파일명은 폴더로 옮긴 뒤에도 기존 설비 접두어 규칙을 그대로 유지**(예: `laser-marking-partlist.pdf`) — 폴더 밖으로 파일이 나와도 어떤 설비 건지 바로 알아볼 수 있게 이중 안전장치.
- 링크는 `pdf-viewer.html?file=첨부문서/<폴더>/<파일명>.pdf` 형태로 이미 전부 걸려 있음. 브라우저가 한글·특수문자(`·`, `(` `)` 등)를 자동으로 퍼센트 인코딩해서 요청하므로 별도 인코딩 처리 불필요(실측 확인: `%EC%B2%A8...` 형태로 정상 요청되고 200 OK 응답).

| 설비 | 폴더 | 필요 PDF 파일명 | 현재 상태 |
|---|---|---|---|
| `leak-pre-flow.html` | `120_조립전유로계리크검사기/` | `leak-pre-flow-partlist.pdf`, `leak-pre-flow-spec.pdf`, `leak-pre-flow-manual.pdf` + 공용 폴더의 `ls-r700-manual-install.pdf`, `ls-r700-manual-operation.pdf` | ✅ 모두 저장소에 존재 (최초 업로드분 + 2026-09-17 매뉴얼 분할, 2026-09-18 폴더 이동·개명) |
| `laser-marking.html` | `110_레이저마킹기/` | `laser-marking-partlist.pdf`, `laser-marking-spec.pdf`, `laser-marking-manual.pdf` | ⬜ 미업로드 |
| `leak-pre-full.html` | `130_조립전전체계리크검사기/` | `leak-pre-full-partlist.pdf`, `leak-pre-full-spec.pdf`, `leak-pre-full-manual.pdf` + 공용 폴더의 매뉴얼 2종 | ⬜ 미업로드 (자체 3종 기준, 공용 매뉴얼 2종은 이미 존재) |
| `nipple-oil-press.html` | `140_오일니쁠압입기·테이퍼플러그체결기(1개소)/` | `nipple-oil-partlist.pdf`, `nipple-oil-spec.pdf`, `nipple-oil-manual.pdf` | ⬜ 미업로드 |
| `cap-sealing-press.html` | `150_씰캡압입기/` | `cap-sealing-partlist.pdf`, `cap-sealing-spec.pdf`, `cap-sealing-manual.pdf` | ⬜ 미업로드 |
| `taper-plug-fastening.html` | `160_테이퍼플러그체결기(5개소)/` | `taper-fastening-partlist.pdf`, `taper-fastening-spec.pdf`, `taper-fastening-manual.pdf` | ⬜ 미업로드 |
| `dowel-pin-press.html` | `170_다월핀압입기/` | `dowel-pin-partlist.pdf`, `dowel-pin-spec.pdf`, `dowel-pin-manual.pdf` | ⬜ 미업로드 |
| `leak-post-flow.html` | `180_조립후유로계리크검사기/` | `leak-post-flow-partlist.pdf`, `leak-post-flow-spec.pdf`, `leak-post-flow-manual.pdf` + 공용 폴더의 매뉴얼 2종 | ⬜ 미업로드 (자체 3종 기준, 공용 매뉴얼 2종은 이미 존재) |
| `leak-post-full.html` | `190_조립후전체계리크검사기/` | `leak-post-full-partlist.pdf`, `leak-post-full-spec.pdf`, `leak-post-full-manual.pdf`, `ls-r902-manual.pdf`(이 설비 전용, 공용 아님) | ⬜ 미업로드 |
| `taper-plug-height.html` | `200_테이퍼플러그돌출높이검사기/` | `taper-height-partlist.pdf`, `taper-height-spec.pdf`, `taper-height-manual.pdf` | ⬜ 미업로드 |

링크는 이미 페이지에 걸려 있으므로, 위 표의 **폴더 안에** 파일명 그대로 PDF를 넣고 `git add`/`commit`/`push`만 하면 "② 첨부 문서" 버튼이 즉시 작동합니다.

### 기술문서 PDF (③ 기술문서 섹션, 2026-09-03 신설, 2026-09-18 폴더 구조 반영)

전 설비 10개소 공통으로 각자 폴더 안에서 아래 2개 파일을 찾습니다. 파일명 규칙은 **`<설비 접두어>-circuit.pdf`(설비 전기회로도) / `<설비 접두어>-drawing.pdf`(기계도면)** 이며, 접두어는 위 표의 PART LIST 파일명과 동일합니다.

| 설비 페이지 | 폴더 | 전기회로도 | 기계도면 |
|---|---|---|---|
| `laser-marking.html` | `110_레이저마킹기/` | `laser-marking-circuit.pdf` | `laser-marking-drawing.pdf` |
| `leak-pre-flow.html` | `120_조립전유로계리크검사기/` | `leak-pre-flow-circuit.pdf` | `leak-pre-flow-drawing.pdf` |
| `leak-pre-full.html` | `130_조립전전체계리크검사기/` | `leak-pre-full-circuit.pdf` | `leak-pre-full-drawing.pdf` |
| `nipple-oil-press.html` | `140_오일니쁠압입기·테이퍼플러그체결기(1개소)/` | `nipple-oil-circuit.pdf` | `nipple-oil-drawing.pdf` |
| `cap-sealing-press.html` | `150_씰캡압입기/` | `cap-sealing-circuit.pdf` | `cap-sealing-drawing.pdf` |
| `taper-plug-fastening.html` | `160_테이퍼플러그체결기(5개소)/` | `taper-fastening-circuit.pdf` | `taper-fastening-drawing.pdf` |
| `dowel-pin-press.html` | `170_다월핀압입기/` | `dowel-pin-circuit.pdf` | `dowel-pin-drawing.pdf` |
| `leak-post-flow.html` | `180_조립후유로계리크검사기/` | `leak-post-flow-circuit.pdf` | `leak-post-flow-drawing.pdf` |
| `leak-post-full.html` | `190_조립후전체계리크검사기/` | `leak-post-full-circuit.pdf` | `leak-post-full-drawing.pdf` |
| `taper-plug-height.html` | `200_테이퍼플러그돌출높이검사기/` | `taper-height-circuit.pdf` | `taper-height-drawing.pdf` |

20개 중 18개 미업로드 상태입니다(2026-09-22, `leak-pre-flow-circuit.pdf`·`leak-pre-flow-drawing.pdf` 업로드 완료 — 최초 업로드 사례. `-drawing.pdf`는 위텍 원본 기계도면 8개 파일(각 1페이지)을 병합해서 만듦, 아래 참고. 이 2개 파일은 `.pages/` 사전 래스터화 이미지도 함께 생성해둠). **파일명 규칙(`-circuit.pdf` / `-drawing.pdf`)이 곧 2차 암호 적용 기준**이므로 (`gate.js`의 `TECH_PDF_RE = /-(circuit|drawing)\.pdf$/i` — 파일명 *끝부분*만 검사하므로 폴더 경로가 앞에 붙어도 정상 작동, 폴더 구조 도입 시 코드로 재확인함), 다른 이름을 쓰면 뷰어 직접 접근 시 2차 암호가 걸리지 않습니다.

### 원본이 여러 파일로 나뉜 기계도면 병합 (2026-09-22 신설)

위텍(설비업체) 원본 기계도면은 도면 1장당 파일 1개(각 1페이지)로 흩어져 있는 경우가 많습니다(예: 조립전 유로계 리크검사기의 `LK2522-00-000.pdf` ~ `LK2522-70-000.pdf` 8개). 사이트는 설비당 `-drawing.pdf` **한 파일**만 인식하므로, 업로드 전에 병합이 필요합니다.

- 병합: `pymupdf`로 파일명 번호 순서대로 `insert_pdf()` — 순서가 도면 순서와 같아야 하므로 파일명의 번호(-00-, -10-, -20- ...)로 정렬해서 병합
- 병합 후 용량이 크면(§ 아래 압축 기준 참고) `tools/compress_pdf.py`로 재압축
- **알려진 이슈(2026-09-22 발견 → 2026-09-22 해결)**: 여러 페이지로 합친 PDF를 `pdf-viewer.html`(pdf.js 기반)에서 열었을 때, 드물게 1페이지만 표시되고 이후 페이지가 응답 없이 멈추는 현상이 관찰됨. 처음엔 Claude의 자동화 브라우저 테스트 환경 특유의 문제로 추정하고 페이지별 20초 제한시간만 방어책으로 추가했으나, **실제 사용자가 실사용 PC에서도 같은 현상(기술문서 PDF 2건이 아예 안 열림)을 보고**하면서 자동화 환경 한정 문제가 아님이 확인됨. 최종 조치는 아래 "기술문서 PDF 사전 래스터화" 참고.

### 기술문서 PDF 사전 래스터화 — pdf.js 실시간 렌더링 정지 문제 해결 (2026-09-22)

전기회로도·기계도면처럼 벡터가 매우 복잡한 CAD형 PDF는 pdf.js가 브라우저에서 실시간으로 캔버스에 그릴 때 특정 페이지에서 응답 없이 멈추는 경우가 있음(원인 미확정 — 위 "알려진 이슈" 참고). 이 문제를 우회하기 위해, **업로드 시점에 미리 각 페이지를 PNG 이미지로 래스터화**해서 `<파일명>.pages/1.png, 2.png, ...` 폴더에 저장해두는 방식으로 전환함.

- 생성: `python tools/pdf_to_page_images.py 첨부문서/<폴더>/<파일명>.pdf` — pymupdf로 200dpi PNG를 페이지마다 저장 (예: `leak-pre-flow-circuit.pages/1.png` ~ `16.png`)
- `pdf-viewer.html`은 `file` 파라미터로 받은 PDF 경로에서 확장자만 `.pages/`로 바꿔 `1.png`을 먼저 시도하고, 있으면 그대로 `<img>`로 순서대로 붙여나가다 다음 번호가 404이면 종료(별도 매니페스트 파일 불필요). 이미지가 없으면(`1.png` 404) 기존 pdf.js 실시간 렌더링 경로로 자동 폴백 — 따라서 **기술문서가 아닌 일반 첨부문서(PART LIST·매뉴얼 등)는 지금까지처럼 pdf.js로 그대로 렌더링**되고, 영향받지 않음.
- 200dpi로 렌더링한 이유: 화면 표시용으로 충분히 선명하면서(전기회로도 1페이지 A3 기준 3309×2339px) 파일 크기도 원본 벡터 PDF보다 오히려 작음(16페이지 합쳐 4.2MB) — 상세 수치는 `개선이력.md` 참고.
- **기술문서 PDF를 교체·추가할 때마다 이 스크립트를 다시 실행해서 `.pages/` 폴더를 갱신해야 함.** 스크립트를 안 돌리면 옛날 이미지가 그대로 보이거나(파일을 새로 올렸는데 `.pages/` 폴더를 안 지운 경우), 반대로 `.pages/` 폴더가 아예 없으면 자동으로 pdf.js 폴백 경로를 타므로 "안 보이는" 사고는 나지 않지만, 최신 도면이 아닌 것을 볼 위험은 있음.

### PDF 업로드 압축 기준 (2026-09-03 신설, 실측 기반)

| 기준 | 내용 |
|---|---|
| 목표 | 파일당 **3MB 이하** (LTE 10Mbps 기준 약 2.4초) |
| 상한 | **5MB 초과 금지** — 재압축해도 넘으면 문서를 분할해서 업로드 |
| 재압축 방법 | `python tools/compress_pdf.py 파일.pdf` — 150dpi / JPEG 품질 75로 이미지 재압축, `<이름>-small.pdf` 생성 (원본은 건드리지 않음) |
| 예외 처리 | **재압축 결과가 원본보다 크면 도구가 자동으로 원본 유지 판정** — 이미 최적화된 PDF는 재압축하면 오히려 커짐 |
| 업로드 전 확인 | 재압축본을 스마트폰 화면에서 열어 표·도면 글자 가독성 확인 후 원본 파일명으로 교체 |

실측 근거 (기존 파일 2개): `machine-spec.pdf` 4.82→**2.63MB(45%↓)** / `ls-r700-manual.pdf` 6.47→9.16MB로 **오히려 커져 원본 유지** (파일마다 결과가 달라 일괄 적용 금지, 도구가 자동 판정).

**2026-09-17**: `machine-spec.pdf`는 위 압축본(2.63MB)으로 교체 완료 — 이전까지 저장소에는 압축 전 원본이 그대로 커밋돼 있었음. `ls-r700-manual.pdf`(6.47MB, 5MB 상한 초과)는 재압축이 안 통하는 파일이라, PDF 내장 목차의 "설치매뉴얼/조작매뉴얼" 경계(43p)로 분할 후 각각 재압축해 `ls-r700-manual-install.pdf`(1.74MB) / `ls-r700-manual-operation.pdf`(3.69MB)로 교체 (상세: `개선이력.md` 2026-09-17).

### 뷰어 (다운로드 버튼 없이 보기 전용, 2026-08-31 추가)

"② 첨부 문서" 링크는 PDF 파일을 직접 열지 않고 `pdf-viewer.html?file=파일명.pdf` 형태로 감싸서 엽니다.

- `pdf-viewer.html`: `pdfjs/pdf.min.mjs`(vendored, `pdfjs-dist` npm 패키지의 빌드 산출물)로 PDF 각 페이지를 `<canvas>`에 직접 그려서 보여주는 자체 제작 뷰어. 다운로드·인쇄·저장 버튼을 아예 두지 않았고, 우클릭(길게 누르기) 메뉴도 막아둠. 기술문서 PDF처럼 사전 래스터화된 `.pages/` 이미지가 있는 파일은 이 pdf.js 경로 대신 이미지를 바로 보여줌(위 "기술문서 PDF 사전 래스터화" 참고).
- **고해상도(레티나) 화면 대응(2026-09-22)**: pdf.js로 캔버스에 그릴 때 `window.devicePixelRatio`만큼 캔버스의 실제 해상도를 화면 표시 크기(CSS px)보다 더 높여서 그림. 이전에는 캔버스 해상도가 CSS 표시 크기와 1:1이라, DPR이 2~3인 스마트폰에서 화면에 맞게 확대 표시되며 흐릿하게 보이는 문제가 있었음(상세: `개선이력.md`).
- 화면 상단 파일명 표시·`<title>`은 `file` 파라미터의 **마지막 경로 조각(basename)만** 보여줍니다(2026-09-18, 첨부문서 폴더 구조 도입과 함께 수정 — 이전엔 전체 경로를 그대로 보여줘서 폴더가 생기자 `첨부문서/120_조립전.../leak-pre-flow-partlist.pdf`처럼 길게 노출되는 회귀가 있었음).
- `gate.js`도 이 페이지에 함께 걸려 있어, 새 탭으로 PDF 링크를 열면(예: 다른 세션에서 직접 접근) 다시 암호를 확인합니다.
- **완전한 다운로드 차단은 아닙니다.** 파일 자체는 여전히 서버에 그대로 존재해서 주소를 알면 직접 받을 수 있고, 화면 캡처도 막을 수 없습니다. "저장 버튼을 없애 덜 눈에 띄게" 하는 수준입니다 (§8 암호 게이트와 동일한 성격의 트레이드오프).
- 파일 용량: `pdf.min.mjs`(~350KB) + `pdf.worker.min.mjs`(~1.3MB), 최초 방문 시 1회 다운로드되고 이후 캐시됨.

---

## 7. QR 코드

- 위치: `qr-codes/` 폴더, PNG 11개 (홈 1개 + 설비 10개)
- 생성 방식: Python `qrcode` 라이브러리 (`qrcode.make(url, box_size=10, border=4)`)
- URL ↔ 파일 매핑: 각 PNG 파일명이 대응 `.html` 파일명과 동일 (예: `laser-marking.png` → `https://dnk-mobility.github.io/laser-marking.html`)
- `leak-pre-flow.png`는 index.html 개편 이후 추가로 재생성됨 (구 index.html 주소가 홈 화면으로 바뀌면서, 원래 그 페이지였던 조립전 유로계 리크검사기용 QR을 새 주소로 다시 생성)

QR 자체는 URL을 담은 이미지일 뿐이며 접근 제어 기능은 없습니다. 실제 접근 제한은 §8의 암호 게이트가 담당합니다.

---

## 8. 접속 제한 (암호 게이트, `gate.js`)

2026-08-17 추가, 2026-08-20 전체 잠금으로 재설계, **2026-09-03 2단계(1차 진입 + 2차 기술자료) 구조로 확장**. 새 계정·유료 서비스 가입 없이, 지금 쓰는 GitHub Pages 그대로 클라이언트(브라우저) JS만으로 구현한 간이 접근 제한 장치입니다.

### 1차 — 페이지 진입 암호

- **11개 페이지(index.html 포함)와 PDF 뷰어 전부, 진입 즉시 화면 전체가 가려지고 암호 입력창이 뜹니다.** 설비명·요약표·섹션 제목 등 예외 없이 전부 숨김. (홈+설비 10개 페이지는 세션당 처음 한 번만 진입 인트로 약 4.5초가 먼저 재생된 뒤 암호창이 뜹니다 — §5. 인트로 중에도 콘텐츠는 계속 가려져 있습니다.)
  - 스크립트가 `<body>` 최상단에서 동기 실행되며 `document.documentElement.style.visibility = "hidden"`을 콘텐츠가 그려지기 전에 적용하는 방식이라, 깜빡임 없이 즉시 가려집니다.
- 한 번 맞으면 그 페이지뿐 아니라 같은 탭에서 방문하는 나머지 페이지도 모두 즉시 열립니다.
- 암호: `dnkm8721` (`gate.js`의 `HASH_1`, `sessionStorage` 키 `dnk_gate_ok_v1`)

### 2차 — 기술자료 열람 암호

- 1차를 통과해도 **아래 항목은 바로 열리지 않고, 펼칠 때 2차 암호를 한 번 더 물어봅니다.**
  - **③ 기술문서** (설비 전기회로도·기계도면) — 전 설비 10개소
- 잠금 대상 표시는 HTML에서 `<details class="section" data-lock="2">` 한 줄뿐입니다. **새 항목을 잠그려면 그 `<details>`에 `data-lock="2"`를 붙이고 `<summary>` 안에 `<span class="lock">잠금</span>` 배지를 넣기만 하면 됩니다** (`gate.js` 수정 불필요).
- 잠긴 항목은 제목 옆에 주황색 `잠금` 배지가 보이고, 해제되면 초록색 `해제`로 바뀝니다(`.section.unlocked`).
- 한 번 맞으면 같은 탭 안의 **모든** 2차 항목이 함께 풀립니다 (항목마다 따로 묻지 않음).
- **기술문서 PDF는 뷰어 주소로 직접 열어도 2차 암호를 요구합니다.** `gate.js`의 `TECH_PDF_RE = /-(circuit|drawing)\.pdf$/i` 로 파일명을 보고 판정하며, `pdf-viewer.html`은 `await window.dnkGate.ready` 로 게이트 통과를 기다린 뒤에야 파일을 내려받습니다 (게이트 전에 네트워크 요청이 나가지 않음).
- 암호: `dnkm8721*` (`gate.js`의 `HASH_2`, `sessionStorage` 키 `dnk_gate2_ok_v1`)
- **"② 첨부 문서"·"③ 기술문서" PDF 링크는 2026-09-21부터 같은 탭에서 엽니다** (이전엔 `target="_blank" rel="noopener"`로 새 탭에서 열었음). 이유: `sessionStorage`는 탭마다 별개라, 새 탭으로 열면 방금 푼 2차 암호가 그 탭엔 없어서 매번 다시 물어보는 문제가 있었음(§17 체크리스트 참고). `pdf-viewer.html`에 뒤로가기 버튼(◀)을 추가해 같은 탭에서 열어도 원래 페이지로 쉽게 돌아갈 수 있게 함.

### 공통 사항

- 인증 상태는 `sessionStorage`에 저장됩니다: **같은 탭에서 다른 페이지로 이동해도 유지**되지만, **탭/브라우저를 닫거나 QR을 새로 스캔해 새 탭으로 열면 1차부터 다시 물어봅니다.**
- 암호는 SHA-256 해시로 비교합니다 (평문을 코드에 그대로 남기지 않기 위함).
- `gate.js` 파일 하나가 12개 페이지(11개 + `pdf-viewer.html`)에서 공용으로 로드됨 (`<body>` 시작 직후 `<script src="gate.js"></script>` 한 줄씩)
- 암호를 바꾸려면 `gate.js` 안의 `HASH_1` / `HASH_2` 를 새 암호의 SHA-256 해시로 교체 (파일 상단 주석에 생성 명령어 있음)
- 변경 이력: 최초 `dnk0915` → 2026-08-17 `dnkm8721` → 2026-09-03 2차 암호 `dnkm8721*` 추가

> 참고 — 이전 버전(2026-08-17~08-20)도 "아코디언 펼칠 때만 암호" 방식이었지만, (1) index.html은 아코디언이 없어 완전 공개였고 (2) 설비 페이지도 요약표·섹션 제목이 미리 보여 "잠금이 안 걸린 것처럼" 보인다는 피드백에 전체 잠금으로 통일했습니다. 이번 2단계 구조는 그 전체 잠금을 **유지한 채** 민감 항목만 한 겹 더 얹은 것이라, 당시 문제(공개 노출)는 재발하지 않습니다.

### 검색엔진 차단 (`robots.txt` + `noindex`, 2026-09-03 추가)

**암호 게이트는 크롤러에 아무 효과가 없습니다.** `gate.js`는 브라우저에서 도는 클라이언트 JS라, JS를 실행하지 않고 HTML만 받아가는 쪽에는 걸리지 않습니다. 실측으로 확인됨(2026-09-03, 당시 주소 `dongnam959.github.io` 기준) — 암호 없이 `GET .../leak-pre-full.html` 한 번에 24KB 전체가 응답되며, 여기에 마스터 샘플 보정값·설비주변온도·트러블슈팅 이력이 전부 들어 있습니다.

방치하면 네이버·구글에 사내 작업표준이 그대로 색인될 수 있어 두 겹으로 막았습니다.

- **`robots.txt`** — `User-agent: * / Disallow: /`. 규칙을 지키는 크롤러는 HTML·PDF 어느 것도 가져가지 않습니다.
- **각 페이지 `<meta name="robots" content="noindex, nofollow, noarchive, nosnippet, noimageindex">`** — robots.txt를 무시하는 크롤러 대비.

> 참고 — 엄밀히는 `robots.txt`로 수집을 막으면 크롤러가 페이지를 안 읽으므로 그 안의 `noindex`도 못 봅니다. 그래서 "어디선가 링크가 걸리면 내용 없이 URL만 색인되는" 경우가 이론상 남습니다. 이 사이트는 외부에서 링크될 일이 없어 실질 위험이 없다고 보고 수집 차단(내용 유출 방지)을 우선했습니다. 만약 URL 노출까지 확실히 막아야 한다면 `robots.txt`의 `Disallow`를 풀고 `noindex` 메타만 남기는 쪽이 정석입니다.

### 한계 (의도적으로 받아들인 트레이드오프)

- **진짜 보안이 아님**: 브라우저 개발자도구로 `gate.js` 소스를 보면 로직과 해시를 확인할 수 있어, 마음먹으면 우회 가능합니다. 2차 암호도 성격은 동일하며, "1차를 아는 사람 중에서도 도면은 아무나 못 보게" 하는 구분용입니다.
- **개인별 구분 없음**: 공유 암호 2개(1차/2차)로 전원이 동일하게 인증합니다. 개인 계정·열람 이력이 필요하면 별도 백엔드(Firebase 등)가 필요하며, 이는 정적 사이트 범위를 벗어나는 별도 개발 프로젝트입니다 (§11 참고).
- **PDF 파일 자체는 여전히 공개 경로에 있음**: 2차 암호는 뷰어 화면을 막을 뿐, `https://.../laser-marking-circuit.pdf` 처럼 파일 주소를 직접 치면 GitHub Pages가 그대로 내려줍니다. 도면 유출이 실제 리스크라면 정적 사이트에 파일을 올리는 것 자체를 재검토해야 합니다 (§11 참고).

---

## 9. 원본 문서 정정 사항

작업표준서(DnK-250915-001~010) 원본의 오탈자를 QR 페이지에는 아래와 같이 정정하여 반영했습니다 (원본 문서 자체는 별도 정식 개정 필요, 각 페이지에 노란색 콜아웃으로 정정 근거를 명시):

| 대상 | 원본 표기 | QR 페이지 반영 내용 |
|---|---|---|
| 공정No.180 작업순서 6단계 | "조립전 전체계 리크 테스트 공정으로 이송한다" | "조립후 전체계 리크 테스트 공정으로 이송한다" |
| 공정No.190 작업순서 6단계 | "조립전 전체계 리크 테스트 공정으로 이송한다" | "테이퍼 플러그 돌출높이 검사(공정No.200) 공정으로 이송한다" |
| 공정No.200 설비명 | "PIN-DOWEL 압입기" | "TAPER PLUG 높이검사기" |

---

## 10. 버전 관리 (Git) 이력 요약

| 커밋 | 날짜 | 내용 |
|---|---|---|
| (최신) | 2026-09-22 | 챗봇 위젯 탐색 구조 개편 — 헤더 홈 버튼 신설, 닫을 때 대화 초기화, 답변마다 반복되던 4버튼 메뉴(`quickChips()`) 제거 |
| `2012c1d` | 2026-09-22 | 챗봇 위젯 글로우 펄스를 PC hover 전용 → 모든 화면 상시 재생으로 전환(터치 기기에서 반응 없던 문제 해결) |
| `1214c68` | 2026-09-22 | 챗봇형 안내 위젯 버튼(`assistant.js`)에 PC 마우스오버 전용 시안 글로우 펄스 애니메이션 추가 |
| `209f61f` | 2026-09-22 | 설비 페이지 10개 `.id-bar` 텍스트를 지정 지점에서 강제 줄바꿈(`<br>`) — `DnK MOBILITY`/`후공정 생산기술팀`, `e-AWD 70kW`/`MOTOR HOUSING ASS'Y` |
| `594ecb6` | 2026-09-22 | 설비 페이지 10개 `.id-bar`에 index.html로 가는 홈 아이콘 버튼(`.home-btn`) 추가, index.html 자체 인라인 스타일의 PC 가로폭 불일치 수정 (아코디언 섹션 색상 구분은 시도 후 사용자 피드백으로 롤백) |
| | 2026-09-22 | 설비 부착용 QR 라벨(`설비QR라벨_70x100mm_A4인쇄용.pdf`)에 "전체 설비 인덱스"(index.html) 라벨 추가 — 기존 개별 설비 10개 라벨에서 11개로, 3페이지 유지 |
| `b54d5e6`, `6f9a2f5` | 2026-09-22 | PC에서 기술문서 PDF 2건(전기회로도·기계도면)이 안 열리던 문제·모바일에서 흐릿하게 보이던 문제 수정 — 기술문서 PDF를 `tools/pdf_to_page_images.py`로 사전 래스터화해 이미지로 보여주도록 전환(`.pages/` 폴더), pdf.js 캔버스 렌더링에 devicePixelRatio 반영 |
| `e476824` | 2026-09-22 | 위텍 원본 기계도면 8개 파일 병합·압축·업로드(`leak-pre-flow-drawing.pdf`), `pdf-viewer.html`에 페이지별 20초 제한시간 방어 로직 추가 |
| `c92dba9` | 2026-09-22 | 첫 기술문서 PDF 업로드분 파일명 오류 정정(`leak-pre-flow-circuit.pdf`) + §6에 파일명 정확 일치 경고 추가 |
| `affd872` | 2026-09-21 | 기술문서 PDF 2차 암호 반복 요구 버그 수정 — PDF 링크 `target="_blank"` 제거(같은 탭에서 열도록 변경), `pdf-viewer.html`에 뒤로가기 버튼 추가 |
| `447114a` | 2026-09-18 | 첨부문서를 프로젝트 루트 평면 구조 → **설비별 폴더 구조**(`첨부문서/<번호>_<설비명>/`)로 재편, 링크 약 50개·기존 파일 5개 이동 |
| `3bbad86` | 2026-09-18 | 우측 하단 "맨 위로 이동" 버튼(`scroll-top.js`) 신규 추가 — 챗봇 버튼과 겹치지 않게 바로 위에 배치 |
| `43f0204` | 2026-09-18 | 설비 9개소 "트러블슈팅 이력" 대량 보강 — 1차: 카카오톡 대화 로그 + 사용자 제공 이력표·PPTX 분석. 2차: 사용자 제공 엑셀 원본(OPEN ISSUE 시트1~3) 대조로 스크린샷 오독 정정 + 신규 사례 7건 추가 |
| `cd262f4` | 2026-09-18 | "⑨ 마스터 샘플·보정 이력" 섹션(리크테스터 4개소) 삭제 — 누설값관리시스템으로 이관 결정, 인계 문서 작성 |
| `e115ddc` | 2026-09-17 | 설비 부착용 QR 라벨을 100×100mm 정사각형 → **70×100mm 직사각형**으로 재조정(폭만 축소, 내용 유지) — 재검증 후 PDF 교체 |
| `7120afc` | 2026-09-17 | 진입 인트로(`intro.js`)를 홈 전용에서 설비 10개 페이지까지 확장 — QR을 설비별로 직접 찍어 들어가는 운영에 맞춤 |
| `89f2add` | 2026-09-17 | 설비 부착용 QR 라벨(100×100mm) 디자인 확정 및 10개소 인쇄 PDF 생성 — `tools/make_labels.py` 추가 (§18) |
| `80c3a9d` | 2026-09-17 | 모바일 재검증 — 위젯 버튼이 푸터를 가리던 문제·같은 페이지 섹션 이동 시 채팅창이 비던 문제·이론 질문 응답에 버튼이 안 붙던 문제 3건 수정 |
| `557bc22` | 2026-09-17 | 시스템 전반 지속관리 점검 — `assistant.js` 정규식 버그 수정(`\b`가 한글에 안 먹히는 문제), §17 재발방지 체크리스트 신설 |
| `ec4e932` | 2026-09-17 | 안내 위젯 채팅창이 답변마다 계속 길어지던 버그 수정(이전 메시지 제거 후 새 응답 표시) |
| `434a93c` | 2026-09-17 | 우측 하단 챗봇형 안내 위젯(`assistant.js`) 신규 추가 — 설비 검색·섹션 안내·FAQ·공정 상식 (§16) |
| `677902d` | 2026-09-17 | `ls-r700-manual.pdf`(5MB 상한 초과)를 설치/조작 매뉴얼로 분할(1.74MB/3.69MB), 3개 페이지 첨부문서 링크 갱신 |
| `d720f1b` | 2026-09-17 | `span`>`div` 무효 중첩 정정(11개 파일 64곳), JS 비활성화 대비 noscript 추가, 게이트 접근성(`role`/`aria-*`) 보강, `compress_pdf.py` 인코딩 크래시 수정, `machine-spec.pdf` 압축본 반영(45%↓) |
| `7e825df` | 2026-09-16 | 저장소를 개인 계정 → `dnk-mobility` 조직으로 이전(`dnk-mobility.github.io`), QR코드 11종 재생성 |
| `7c582f5` | 2026-09-03 | 개선이력.md·PDF 압축 기준·`tools/compress_pdf.py` 추가, 문서 갱신 |
| `1f9c11f` | 2026-09-03 | 공용 `styles.css` 추출 (설비 10개 페이지 중복 CSS 제거), 구번호 참조 3건 정정 |
| `aac8a5f` | 2026-09-03 | 시스템 재점검 — robots.txt+noindex, 핀치 줌 허용, favicon, 404 페이지 (상세: `개선이력.md`) |
| `690df6c`~`7f0547a` | 2026-09-03 | 인트로 속도 조정 (텍스트 2초, 완성 후 정지 1초, 페이드아웃 1.2초) |
| `3b1a791` | 2026-09-03 | 인트로를 암호창 **앞**으로 이동 (QR → 인트로 → 암호 → 인덱스) |
| `7371cc0` | 2026-09-03 | 홈 진입 인트로(`intro.js`) 신규 추가 |
| `c14cd96` | 2026-09-03 | hover/tap 인터랙션 도입 (§4) |
| `3ed7cd7` | 2026-09-03 | "③ 기술문서" 섹션 신설 + 2단계 암호 게이트 |
| `586b736` | 2026-09-02 | 마스터 샘플 이력 표를 전/후 열로 분할, 공정No.130 실데이터 입력 |
| `486dc6b` | 2026-08-17 | 암호 게이트 인증 유지 방식을 30일 저장 → 탭/세션 단위 재인증으로 변경 |
| `5183c06` | 2026-08-17 | 암호 게이트를 "페이지 전체 차단" → "섹션 펼칠 때만 확인" 방식으로 전환, 암호 교체 |
| `71bbca0` | 2026-08-17 | 전 페이지에 간이 접속 암호 게이트(`gate.js`) 최초 추가 |
| `1140588` | 2026-07-30 | 홈 화면 제목을 "전체 설비 인덱스"로 변경 |
| `e024d6f` | 2026-07-30 | "공정 설비 정보 시스템" → "공정 설비 정보관리 시스템"(+영문) 전 페이지 일괄 변경 |
| `8a49c52` | 2026-07-30 | 기존 index.html → leak-pre-flow.html 이름 변경, 신규 홈 메뉴 페이지 추가, QR코드 11종 커밋 |
| `a399dbe` | 2026-07-29 | 설비 9개 페이지 신규 추가 (공정No.110~200) |
| `f098b7e` 이전 | 2026-07-29 이전 | 최초 저장소 클론 원본 (기존 index.html 1개 + PDF 4개) |

브랜치는 `main` 하나만 사용하며, 모든 변경사항은 로컬에서 커밋 후 바로 `origin/main`에 push하는 단순한 흐름입니다. 별도의 PR·리뷰 프로세스는 없습니다.

**실측 소요 시간** (커밋 타임스탬프 기준, 사내 원가절감 비교 자료에도 사용): 설비 10개소 + 홈 화면 + QR 전체 구축은 2026-07-29 오후~07-30 오전, 약 **1일** 소요. 접속 암호 게이트 추가는 2026-08-17 오전, 약 **20분** 소요.

---

## 11. 알려진 제한사항

- **접근 제어는 간이 수준**: §8 참고. 페이지 진입 즉시 전체를 가리고 민감 항목은 2차 암호까지 두지만, 클라이언트 JS 방식이라 개발자도구·`curl`·view-source로는 우회 가능합니다. 공유 암호 2개(1차/2차)이며 실제 보안 장치가 아닙니다 — 도면류 반입 시 이 수준으로 충분한지는 §15 참고.
- **PDF 미완성**: 최초 4개(leak-pre-flow용)를 제외한 첨부문서 26개 + 기술문서 20개(§6)가 아직 업로드되지 않음. 링크는 걸려 있으나 클릭 시 404.
- ~~**CSS/JS 중복**~~: **2026-09-03 해소** — 설비 페이지 10개의 공용 스타일을 `styles.css` 하나로 추출 (§4). 홈·PDF 뷰어만 각자의 인라인 스타일 유지.
- **빌드/테스트 없음**: 정적 HTML이라 별도 빌드·테스트 파이프라인이 없고, 배포 전 수동 확인에 의존.
- **안내 위젯은 자연어 이해가 아닌 키워드 매칭**: §16 참고. 애매한 질문은 폴백 응답이나 후보 목록으로 처리하며, 실제 AI 대화는 아님.

## 12. 향후 계획

- [ ] 설비별 PART LIST·기계설명서·매뉴얼 PDF 첨부 완료 (§6 미업로드 항목)
- [x] **신규 항목 추가**: 설비전기회로도·기계도면 → "③ 기술문서" 섹션으로 전 설비 추가 완료 (2026-09-03). PDF 파일 20개는 업로드 대기
- [x] **항목별 접속 제한 세분화**: 2단계 암호로 구현 완료 (2026-09-03). 현재 잠금 대상 = ③ 기술문서 (마스터 샘플·보정 이력 섹션은 2026-09-18 삭제·이관, §3 참고)
- [ ] QR코드 인쇄 후 실제 설비에 부착
- [ ] 사내 개선제안서 제출 (외주 구축 대비 원가절감 비교자료 포함 — §13 참고)
- [ ] 타 라인·타 공정으로 확대 적용 검토
- [ ] 다국어(영/중) 페이지 지원 검토
- [ ] (검토) 개인 계정·역할별 열람이력이 필요해지면 별도 백엔드 도입 여부 결정 (현재는 인원 규모상 불필요 판단)
- [x] **챗봇형 안내 위젯 추가** — 설비 검색·섹션 안내·FAQ·공정 상식 (2026-09-17, §16)

---

## 13. 외주 견적 대비 자체 구축 비교 (사내 개선제안서용)

외주업체(WISHKET CONSULTING, 2026-08-14자 제안서) 견적과 비교한 근거 자료. 상세 표는 대화 이력 및 `공정 설비 정보관리 시스템_구축과정 설명자료.docx` 참고.

**요약**:
- 외주 견적: 초기구축(기본형) 476~560만원 ~ (확장형) 1,194~1,404만원, 연 유지관리 36~180만원, 최소 납기 3주
- 자체 구축: 초기 0원, 유지관리 0원, 실제 소요 약 1일(§10) + 접속 제한 추가 20분
- 외주 "운영형"의 변경이력·접근제한 항목은 git 커밋 이력(§10)과 §8 암호 게이트로 이미 자체 충족
- 미충족 항목(개인계정·역할권한 등 "확장형")은 현재 사용 인원 규모(작업자 1~2명, 관리자 2명)상 필요성 낮다고 판단 — 외주 제안서 자체에도 동일하게 명시됨

---

## 14. 관련 산출물

기준 폴더: `C:\Users\user\Desktop\후공정 설비 QR 코드 스마트폰 관리 시스템\` (이하 "관리 폴더")
git 저장소는 그 안의 `후공정 설비 정보 시스템 프로젝트\` 폴더 하나뿐이며, 아래 PPT·WBS 등은 git 관리 대상이 아닌 로컬 파일입니다.

| 파일 | 위치 | 용도 |
|---|---|---|
| `*.html`, `gate.js` | 저장소 | 설비 페이지 10 + 홈 1 + 2단계 암호 게이트 |
| `intro.js` | 저장소 | 진입 인트로 (§5) — 홈 + 설비 10개 페이지에서 로드 (2026-09-17부터, PDF 뷰어·404 제외) |
| `assistant.js` | 저장소 | 챗봇형 안내 위젯 (§16) — 설비 10개 + 홈에서 로드, PDF 뷰어·404는 제외 |
| `scroll-top.js` | 저장소 | 맨 위로 이동 버튼(2026-09-18 추가) — 설비 10개 + 홈에서 로드, PDF 뷰어·404는 제외. 스크롤 480px 이상 시 우측 하단 챗봇 버튼 바로 위(16+54+12=82px)에 표시, 챗봇과 겹치지 않게 z-index를 한 단계 낮춤(8990) |
| `styles.css` | 저장소 | 설비 페이지 10개 공용 스타일시트 (§4) — 디자인 수정은 이 파일 하나만 |
| `robots.txt` | 저장소 | 검색엔진 수집 전면 차단 (§8) |
| `개선이력.md` | 저장소 | 개선 항목별 상세 기록 (문제→조치→검증→효과) — 사내 개선보고 자료 원본 |
| `인트로_암호게이트_구현상세.md` | 저장소 | 인트로·암호 게이트 구현 상세(타이밍 수치·모바일 함정 등) — 다른 시스템(누설값 관리 시스템 등) 이식용 벤치마킹 문서, §5·§8의 하위 참고자료 |
| `tools/compress_pdf.py` | 저장소 | PDF 업로드 전 재압축 도구 (§6 압축 기준) |
| `tools/make_labels.py` | 저장소 | 설비 부착용 QR 라벨(100×100mm) 인쇄 PDF 생성 (§18) — 설비명·주소가 바뀌면 이 파일의 `EQUIPMENT`만 고치고 재실행 |
| `qr-codes/설비QR라벨_70x100mm_A4인쇄용.pdf` | 저장소 | 위 스크립트 결과물 — A4 3장, 한 장당 4개(2×2), 재단선 포함 |
| `favicon.svg` | 저장소 | 브라우저 탭 아이콘 (네이비 바탕 + 시안 QR 조준선, 인트로와 같은 모티프). 380바이트 |
| `404.html` | 저장소 | 잘못된 주소 진입 시 GitHub Pages가 자동으로 띄우는 페이지. **암호 게이트를 걸지 않음** — 오타로 들어온 사람에게 암호를 물을 이유가 없고 사내 정보도 없음 |
| `pdf-viewer.html`, `pdfjs/` | 저장소 | 다운로드 버튼 없는 PDF 뷰어 (§6) |
| `첨부문서/` | 저장소 | 설비별 PDF 첨부문서·기술문서 폴더 (§6, 2026-09-18 신설) — 설비 10개 폴더 + `공용_LS-R700디텍터/` |
| `qr-codes/*.png` | 저장소 | 설비별 QR코드 이미지 11개 |
| `qr-codes/QR라벨_전체11개_통합.pdf`, `files.zip` | 저장소(미커밋) | 사용자가 직접 넣은 인쇄용 라벨 — git 추적 안 됨 |
| 본 문서 (`PROJECT_OVERVIEW.md`) | 저장소 | 기술 참고 문서 (이 파일) |
| `QR코드 기반 ... 구축(개선 제안서).pptx` | 관리 폴더 | 개선제안서 표지 1장 (다른 4개 ppt를 OLE 개체로 첨부) |
| `3. 사내보고\시스템 개요/화면구성/주요기능/사용방법.ppt` | 관리 폴더 | 개선제안서에 첨부되는 상세 슬라이드 4종 |
| `공정 설비 정보관리 시스템 도입 보고.pptx` | 관리 폴더 | 임원 보고용 PPT (10장) |
| `공정 설비 정보관리 시스템_구축과정 설명자료.docx` | 관리 폴더 | 비개발자용 용어 설명 + Q&A |
| `2. wbs\QR코드_후공정설비정보관리시스템_WBS.xlsx` | 관리 폴더 | 진행관리 WBS (간트 + 개정이력 시트) |
| `1. 업체 조사\` | 관리 폴더 | 외주업체(WISHKET) 견적 제안서 등 |
| `09_설비페이지_제작_지시서.md` | 관리 폴더 | 9개 설비 페이지 최초 제작 지시서 (원본 사양) |

---

## 15. 현재 상황 및 이어서 할 일 (2026-09-17 기준)

### 완료된 상태

- **저장소**: 최신 커밋까지 전부 push 완료, GitHub Pages 빌드(`built`) 확인 (미커밋 파일은 사용자가 직접 넣은 `qr-codes/QR라벨_전체11개_통합.pdf`, `files.zip` 두 개뿐 — 의도적으로 건드리지 않음).
- **웹 시스템**: 11개 페이지 전부 배포·동작 중. **2단계 암호**(1차 진입 `dnkm8721` / 2차 기술자료 `dnkm8721*`), PDF는 다운로드 버튼 없는 뷰어 경유, 전 설비에 "③ 기술문서" 섹션 신설, 우측 하단 챗봇형 안내 위젯(§16) 신규 추가. (2026-09-18: 리크테스터 4개 설비의 마스터 샘플·보정 이력 표는 삭제하고 누설값관리시스템으로 이관 — §3 참고)
- **지속관리 체계**: §17에 재발방지 체크리스트 신설 — 새 문제 발생 시 `개선이력.md`에 기록하고 체크리스트에도 반영하는 흐름 확립.
- **보고자료**: 개선제안서(표지+첨부 4종), 임원보고 PPT, 설명자료 DOCX, WBS 모두 반영 완료 — **단, 개선3~4 내용은 아직 미반영** (아래 "이어서 할 일" 참고). 이 항목은 로컬 산출물(관리 폴더)이라 이 세션에서 실제 갱신 여부를 확인하지 못했으므로, 다음에 해당 파일을 열 때 직접 확인할 것.
- **WBS**: 전체 목표 완료일 2026-09-30, "개정이력" 시트에 **개선1(v2.0, 08-17)** / **개선2(v3.0, 09-02)** 등록됨. **개선3(v4.0, 09-03) 이후분 미등록.**

### 개선 차수 구분 (사내 보고용)

| 차수 | 범위 |
|---|---|
| **개선1** | QR 시스템 최초 구축 — 설비 10개소 페이지·홈 화면, QR코드 생성, GitHub Pages 배포, 보고자료(PPT/DOCX)·WBS |
| **개선2** | 보안·이력 강화 — 전 페이지 접속 암호 게이트, PDF 다운로드 버튼 제거(뷰어), 마스터 샘플·보정 이력 섹션 신설 |
| **개선3** | 기술자료 항목 확장 — "③ 기술문서"(전기회로도·기계도면) 전 설비 신설, **2단계 암호**로 항목별 접근 차등화, 보정 이력 표 전/후 열 분할, hover/tap 인터랙션 도입(§4), 홈 진입 인트로 추가(§5) |
| **개선4** | 코드 정밀 재점검 및 최적화, 저장소 조직 계정 이전, 챗봇형 안내 위젯(`assistant.js`) 신규 추가 (§16) |

### 이어서 할 일 (우선순위 순)

1. **기술문서 PDF 20개 업로드** — §6 "기술문서 PDF" 표. 파일명 규칙(`-circuit.pdf` / `-drawing.pdf`)을 반드시 지켜야 2차 암호가 정상 적용됨.
2. **첨부문서 PDF 26개 업로드** — §6 첫 표의 미업로드 항목.
3. ~~마스터 샘플·보정 이력 데이터 추가 입력~~ — **2026-09-18 결정으로 대체됨.** 이 시스템은 정적 페이지라 데이터를 계속 쌓기에 적합하지 않다고 판단, 해당 섹션 자체를 삭제하고 별도 개발 중인 누설값관리시스템으로 이관하기로 함. 이관에 필요한 내용은 [`인계_마스터샘플_보정이력.md`](인계_마스터샘플_보정이력.md) 참고.
4. **보고자료에 개선3 반영** — WBS 개정이력 시트에 개선3(v4.0) 행 추가, 개선제안서·임원보고 PPT의 보안 설명(현재 "공유 암호 1개" 기준)을 2단계 구조로 갱신.
5. **QR코드 현장 부착** (WBS 7.1, 목표 08-21) 및 **개선제안서 결재 상신** (WBS 7.2, 목표 08-25) — 아직 "작업 대기" 상태.
6. **WBS 개정이력의 작성자/승인일/승인자 칸** — 의도적으로 공란. 실제 담당자명은 사용자가 직접 기입 필요.
7. **위젯 콘텐츠 최신화** — 설비 정보(품번·문서번호 등)나 섹션 제목을 바꿀 때마다 `assistant.js`의 `EQUIPMENT`/`SECTION_TOPICS`도 같이 갱신할 것 (§16 알려진 한계 참고, 자동 동기화 없음).

> 도면류 민감도에 대해서는 사용자와 이미 합의됨: **사내 열람 수준이면 충분** = 현행 간이 잠금 유지. 단 §8 마지막 항목(파일 주소 직접 접근 시 다운로드 가능)은 도면 업로드 전에 한 번 더 확인할 것.

### 작업 시 참고사항 (환경)

- 이 PC에는 LibreOffice가 설치되어 있어 `"C:\Program Files\LibreOffice\program\soffice.exe" --headless --convert-to pdf 파일명` 으로 PPT/XLSX 렌더링 검증이 가능함 (skill 스크립트의 `soffice.py` 래퍼는 Windows에서 `AF_UNIX` 오류로 못 씀 — 위 경로로 직접 호출할 것).
- PDF→이미지 변환은 `pdftoppm` 대신 Python `pymupdf`(fitz) 사용. `python3`는 Windows 스토어 스텁이라 실패하므로 항상 `python` 사용.
- 웹페이지 동작 검증은 Playwright(설치됨) + `python -m http.server` 로컬 서버 사용. `file://` 로 열면 ES module CORS 정책 때문에 PDF 뷰어가 동작하지 않음에 주의.
- Korean 경로/문자열이 많아 Bash 인라인 `python -c` 는 인코딩이 깨짐 — 스크립트 파일로 저장해 실행할 것.

---

## 16. 설비 정보 도우미 (챗봇형 안내 위젯, 2026-09-17 추가)

우측 하단에 고정되는 챗봇형 안내 버튼 → 클릭 시 채팅 패널이 열림. 서버·API 없이 클라이언트 JS(`assistant.js`) 하나로 동작하는 **규칙 기반(키워드 매칭)** 위젯이며, 실제 AI 대화가 아닙니다 — §13의 "빌드/백엔드 없는 정적 사이트, 유지비 0원" 원칙을 지키기 위한 의도적 선택입니다.

**버튼 뒤로 시안(`#37c6e0`) 후광이 은은하게 진해졌다 흐려지길 반복하는 글로우 애니메이션(`@keyframes dnk-asst-glow`, 1.8초 주기, 2026-09-22 추가)이 항상 재생됩니다.** 처음엔 PC 마우스 hover 시에만(`@media (hover:hover) and (pointer:fine)`) 재생하도록 만들었으나, 사용자가 "모든 화면에서" 보이길 원했고 스마트폰(터치)에는 애초에 hover 자체가 없어 반응하지 않는다는 걸 확인한 뒤 — **hover 조건을 걷어내고 항상 재생되는 앰비언트(ambient) 애니메이션으로 전환**했습니다. 터치 기기에서도 문제없는 이유: sticky hover는 "터치 직후 hover 상태가 들러붙어 안 사라지는" 문제인데, 이 글로우는 애초에 hover 상태와 무관하게 항상 도는 애니메이션이라 그 문제 자체가 성립하지 않습니다. `prefers-reduced-motion:reduce`에서는 애니메이션 없이 은은한 고정 밝기(`opacity:.45`)로 대체합니다.

### 할 수 있는 것

- 설비 이름·공정No.·품번으로 검색 → 해당 설비 페이지로 이동
- 페이지 안 특정 섹션(점검표·매뉴얼 등)까지 자동으로 펼쳐서 안내 — 다른 페이지에 있는 섹션이면 `<파일명>.html?goto=<토픽키>`로 이동시킨 뒤 그 페이지에서 이어서 처리
- 자주 묻는 질문(FAQ) 답변
- 설비별 공정 이론·상식(자동차 실무 위주) 안내

### 구조

- `assistant.js` 한 파일에 설비 메타데이터(`EQUIPMENT`)·섹션 토픽 맵(`SECTION_TOPICS`)·FAQ·이론(`THEORY`)이 전부 내장되어 있음 — 새 설비·FAQ·이론을 추가/수정하려면 이 파일의 해당 배열·객체만 고치면 됨.
- `gate.js`/`intro.js`와 같은 패턴(자체 `<style>` 주입, `<body>` 상단에 스크립트 태그 한 줄)으로 설비 10개 페이지 + `index.html`에 로드됨. `pdf-viewer.html`(문서 열람 방해)과 `404.html`(안내할 콘텐츠 없음)은 제외.
- `window.dnkGate.ready`가 풀린 뒤에만 위젯을 생성하므로, 암호 게이트 통과 전에는 다른 콘텐츠와 마찬가지로 보이지 않음(문서 루트의 `visibility:hidden`을 그대로 물려받음).
- 섹션 자동 펼침은 새 `id`를 추가하지 않고 각 `<summary>` 텍스트 부분일치로 찾음 — 설비 10개 페이지의 섹션 제목 문구가 완전히 동일하다는 것을 확인하고 설계함.
- 이미 인증된 세션에서 다른 페이지로 이동하면 게이트가 마이크로태스크만으로 거의 즉시 풀리는데, 이 경우 `<main>`의 아코디언이 아직 파싱되기 전에 스크립트가 실행될 수 있어(개발 중 실제로 겪은 버그, 상세는 `개선이력.md` 참고) `gate.js`가 `wireSections()`에 쓰는 것과 같은 `onReady()`(readyState 확인 후 필요시 `DOMContentLoaded`까지 대기) 패턴을 재사용함.

### 탐색(내비게이션) 구조 (2026-09-22 개편)

패널 헤더에 **홈 버튼**(🏠, `.dnk-asst-home`)이 닫기(✕) 버튼 옆에 항상 떠 있습니다. 누르면 `showHome()`이 실행돼 대화 내용을 지우고 처음 인사말+메뉴(설비 목록/점검표 보기/자주 묻는 질문/공정 상식)로 되돌아갑니다. 패널을 **닫을 때마다** 자동으로 이 상태로 리셋되므로, 다시 열면 항상 처음 화면부터 시작합니다(직전 대화가 남아있지 않음).

이 구조는 처음에 없었고, 사용자가 실사용 중 겪은 불편 3가지를 계기로 추가했습니다(상세: `개선이력.md`):
1. "설비 목록"을 누른 뒤 되돌아갈 버튼이 없었음 → 헤더 홈 버튼이 항상 떠 있어서 어느 화면에서든 누르면 처음으로 돌아감.
2. 패널을 닫았다가 다시 열면 직전에 보던 화면이 그대로 남아있어서 "처음 화면"이 아니었음 → 닫을 때 대화를 초기화하도록 변경.
3. 이론(공정 상식)·FAQ 답변마다 "설비 목록/점검표 보기/자주 묻는 질문/공정 상식" 4개짜리 메뉴가 매번 답변 아래 반복돼서 지저분했음 → 개별 답변에서 이 메뉴(`quickChips()`)를 떼어내고, 대신 헤더 홈 버튼 하나로 통일. **단, "이해하지 못했어요" 폴백 응답에는 여전히 이 메뉴를 붙여둠** — 봇이 질문을 못 알아들은 상황에서는 사용자가 막막해지지 않도록 선택지를 보여주는 게 낫다고 판단.

### 지켜야 하는 제약 (보안)

- **잠금 섹션(`data-lock="2"`: 기술문서)은 위젯이 절대 강제로 펼치지 않음** — 스크롤만 시키고, 실제로 펼치는 것은 사용자가 직접 눌러 `gate.js`의 2차 암호 절차를 그대로 거치게 함(`openOrScrollSection()`이 이 규칙을 지킴). 앞으로 위젯을 수정할 때도 이 제약은 반드시 유지해야 함 — 그렇지 않으면 2차 암호 게이트 자체가 무의미해짐.
- **비밀번호는 어떤 질문으로도 알려주지 않음** — FAQ의 "암호" 관련 답변은 항상 "후공정 생산기술팀에 문의" 안내로 고정.

### 알려진 한계

- 규칙 기반이라 자연어 이해에 한계가 있음 — 키워드가 애매하면 "이해하지 못했어요" 폴백이나 설비 후보 여러 개를 동시에 제시함(잘못 단정하지 않는 쪽을 택함).
- 섹션 자동 펼침은 `<summary>` 텍스트 부분일치이므로, 향후 섹션 제목 문구를 바꾸면 `SECTION_TOPICS`의 `match` 값도 같이 고쳐야 함.
- `assistant.js`의 `EQUIPMENT` 배열(설비명·문서번호·품번 등)은 각 페이지 HTML과 별개로 손으로 입력해둔 값 — 설비 정보를 페이지에서 고치면 `assistant.js`도 같이 고쳐야 함(자동 동기화 없음).

---

## 17. 재발방지 체크리스트 (2026-09-17 신설)

이 시스템을 지속적으로 유지관리하기 위해, 지금까지 실제로 겪은 문제들(상세 경위·원인·조치는 전부 `개선이력.md`에 기록됨)을 "다음 작업 전에 무엇을 확인해야 하는가"로 압축한 목록입니다. **새로운 문제가 생기면 `개선이력.md`에 문제→원인→조치→검증을 기록한 뒤, 재발 방지에 도움이 될 항목이면 이 체크리스트에도 한 줄 추가하세요.** 이 문서는 그 자체로 끝나는 감사(audit) 결과가 아니라, 다음 사람(또는 다음 세션의 나)이 같은 실수를 반복하지 않기 위한 살아있는 목록입니다.

### 코드 작성 시

- [ ] 섹션 번호(①~⑩)를 바꿨다면 제목뿐 아니라 **본문 속 참조**(`[①-⑩]항`, `상기 [①-⑩]` 패턴)까지 전수 검색했는가? (2026-09-03 [6])
- [ ] 게이트(`gate.js`) 통과 후에 실행되는 스크립트를 새로 추가했다면 `onReady()`(readyState 확인 후 필요시 `DOMContentLoaded`까지 대기) 패턴을 적용했는가? 이미 인증된 세션은 마이크로태스크만으로 즉시 통과되어 `<main>`이 아직 파싱되기 전에 실행될 수 있다 (2026-09-17 [1])
- [ ] `<span>` 안에 `<div>` 등 block 요소를 넣지 않았는가? (2026-09-17, span→div 정정 64곳)
- [ ] 새 잠금 항목(`data-lock="2"`)을 추가했다면, 어떤 스크립트도 `.open = true`를 직접 호출해 강제로 펼치지 않는가? 2차 암호를 거치지 않고 펼치면 그 게이트가 통째로 무의미해짐
- [ ] 버튼을 눌러 내용을 계속 보여주는 UI(채팅형 등)를 만든다면, "이전 내용을 지울지 계속 쌓을지"를 미리 정했는가? (2026-09-17 [3], 안내 위젯이 무한정 길어졌던 사례)
- [ ] 정규식에 `\b`(단어 경계)를 쓰기 전에 — **한글 문자열에는 사실상 안 먹힌다.** JS의 `\b`는 ASCII `\w` 기준이라 한글 뒤에서는 거의 항상 경계가 안 잡힘(2026-09-17, `왜\b`가 전혀 매칭되지 않던 사례). 한글 키워드는 그냥 `indexOf`/부분일치로 처리할 것.
- [ ] 같은 종류의 수정을 여러 지점에 적용했다면, 수정 후 **그 패턴이 나타나는 모든 곳을 grep으로 다시 훑어 빠진 지점이 없는지** 확인했는가? (2026-09-17, "막다른 답변에 버튼 붙이기"를 4곳은 고치고 1곳을 놓쳤다가 실사용 중 발견됨)
- [ ] 버튼 클릭 핸들러가 "이전 내용을 지운 뒤" 실행된다면, 연결한 함수가 **반드시 새 메시지/피드백을 만드는지** 확인했는가? DOM만 조작하고 끝나는 함수를 그대로 연결하면 "지우기만 하고 안 채우는" 빈 화면이 생긴다 (2026-09-17, 같은 페이지 섹션 이동 시 채팅창이 비어 보이던 사례)
- [ ] `position:fixed` 오버레이(플로팅 버튼 등)를 추가했다면, 그 요소가 차지하는 화면 영역만큼 **페이지 콘텐츠 쪽에도 여백을 확보**했는가? 요소 존재 여부만 테스트해서는 안 드러나고, 페이지 맨 아래까지 스크롤한 화면을 실제로 봐야 드러난다 (2026-09-17, 위젯 버튼이 푸터 문구를 가리던 사례)
- [ ] `sessionStorage`로 인증/상태를 저장하는 페이지로 가는 링크에 **`target="_blank"`를 쓰지 않았는가?** `sessionStorage`는 탭마다 완전히 별개라(특히 `rel="noopener"`와 같이 쓰면 새 탭이 원래 탭의 세션 정보를 전혀 못 물려받음), 새 탭으로 열면 방금 통과한 게이트를 처음부터 다시 물어보게 된다 (2026-09-21, 기술문서 PDF 링크가 매번 2차 암호를 다시 묻던 사례)
- [ ] `<canvas>`에 이미지·문서를 그려서 화면에 보여주는 코드를 새로 짠다면 `window.devicePixelRatio`를 반영했는가? 캔버스의 실제 해상도(`canvas.width`/`height`)를 화면 표시 크기(CSS px)와 그대로 같게 두면, DPR이 2~3인 스마트폰 화면에서 확대돼 흐릿하게 보인다 — 표시 크기는 CSS로 고정하고 해상도만 DPR배 올릴 것 (2026-09-22, PDF 뷰어가 모바일에서 흐릿하게 보이던 사례)
- [ ] 새 상단/하단 블록(`.id-bar`, `.doc-header` 같은 `body` 직계 요소)을 추가했다면 `main`/`footer`와 같은 `max-width:640px; margin:0 auto;`를 같이 넣었는가? 안 넣으면 뷰포트가 640px보다 넓은 PC에서 그 블록만 화면 끝까지 늘어나 다른 섹션과 가로폭이 어긋난다 — **모바일 375px 뷰포트 검증만으로는 이 문제가 드러나지 않으므로, 640px보다 넓은 데스크톱 폭에서도 한 번은 확인할 것** (2026-09-22, 상단 헤더가 PC에서만 본문보다 넓게 늘어나던 사례)
- [ ] `.id-bar`/`.doc-header` 관련 CSS를 고칠 때 — **`styles.css`(설비 10개 공용)만 고치고 `index.html`(자체 인라인 스타일)을 빠뜨리지 않았는가?** 두 곳에 사실상 같은 규칙이 중복 존재한다(§4). 2026-09-22에 이 폭 불일치 버그를 설비 페이지에서만 고치고 `index.html`은 빠뜨렸다가 사용자가 "전체설비인덱스도 안 맞다"고 재차 지적한 사례가 있음
- [ ] 파이썬 등으로 **여러 파일에 같은 텍스트 치환을 거는 일회성 스크립트**를 짤 때 — 새 내용을 전부 계산·검증(예: 결과 길이가 비정상적으로 짧지 않은지)한 뒤에만 `open(path, "w")`으로 쓰기 모드를 여는가? `open(..., "w")`은 여는 순간 파일을 비운다 — 그 다음에 값 계산 중 예외(예: 반환값 언패킹 순서 실수)가 나면 원본이 그대로 날아간다(2026-09-22, `re.subn()` 반환값을 반대로 언패킹해서 `laser-marking.html`이 빈 파일이 됐다가 `git checkout`으로 복구한 사례)
- [ ] "hover 효과"를 요청받았다면 — **이 사이트는 QR 스캔 → 스마트폰이 주 사용 환경이고, 터치 기기에는 `hover` 자체가 존재하지 않는다.** `@media (hover:hover) and (pointer:fine)`로만 감싸면 PC에서만 보이고 폰에서는 절대 재현 안 되는 게 정상 동작이다 — "모든 화면에서" 같은 요청이면 hover에 의존하지 말고 처음부터 상시 재생 애니메이션(ambient) 등 터치에서도 통하는 방식을 검토할 것. 애매하면 사용자에게 PC 전용인지 모든 기기용인지 먼저 확인 (2026-09-22, 글로우 펄스 효과를 PC hover 전용으로 만들었다가 "스마트폰에서 전혀 반응 없다"는 재보고를 받은 사례)

### PDF·첨부파일 추가 시

- [ ] 파일당 목표 3MB 이하, **5MB 초과 금지** — 넘으면 `tools/compress_pdf.py`로 재압축, 그래도 크면 분할(§6)
- [ ] 재압축 결과가 원본보다 크면(이미 최적화된 파일) 도구가 원본을 자동 유지 — 파일마다 다르므로 일괄 적용 금지
- [ ] **재압축 후 실제로 원본 파일을 교체(git에 커밋)했는지 확인.** 2026-09-17에 `machine-spec.pdf`를 로컬에서 압축까지 해두고 원본 교체를 깜빡해, 문서에는 "압축 완료"라고 적혀 있는데 저장소에는 몇 주간 미압축 원본이 그대로 커밋되어 있던 사례가 있었음(§6 [4])
- [ ] 기술문서(`-circuit.pdf`/`-drawing.pdf`) 파일명 규칙을 지켰는가 — `gate.js`의 `TECH_PDF_RE`가 이 규칙으로만 2차 암호 대상을 판정하므로, 다른 이름을 쓰면 조용히 2차 암호가 안 걸림
- [ ] PDF를 폴더에 넣을 때 **§6 표에 적힌 파일명과 글자 하나까지 정확히 같은지** 확인했는가? 이 시스템은 파일명을 유사매칭하지 않고 정확히 일치해야만 찾는다 — "알아보기 쉬운 이름"으로 올리면 조용히 "문서를 찾을 수 없음" 에러가 난다 (2026-09-22, `leak-pre-flow-electrical circuit diagram.pdf`로 올려서 안 뜨던 사례)
- [ ] 원본이 여러 파일로 나뉜 기계도면을 병합했다면, **실제 스마트폰/PC 브라우저로 전 페이지가 다 넘어가는지 직접 확인**했는가? pdf.js 뷰어가 드물게 특정 페이지에서 멈추는 현상이 있었다(§6, 2026-09-22) — 최종적으로는 `tools/pdf_to_page_images.py`로 사전 래스터화해서 이 문제 자체를 우회했으니, 기술문서(`-circuit.pdf`/`-drawing.pdf`) 파일을 새로 올리거나 교체했다면 **이 스크립트를 실행해 `.pages/` 폴더를 만들어야** pdf.js 실시간 렌더링을 타지 않는다 (§6)
- [ ] 기술문서 PDF(`-circuit.pdf`/`-drawing.pdf`)를 교체했다면, 옛 `.pages/` 폴더가 새 내용과 페이지 수까지 정확히 일치하도록 **먼저 옛 `.pages/` 폴더를 지우고** `tools/pdf_to_page_images.py`를 다시 실행했는가? 안 지우고 다시 생성하면 옛 파일 수가 더 많았을 때 뒷페이지 이미지가 남아 엉뚱한(구버전) 페이지가 끝에 덧붙어 보일 수 있다 (2026-09-22, §6)

### 인쇄물(PDF) 만들 때

- [ ] 한글이 들어가는 PDF를 만들었다면 **저장 전 `doc.subset_fonts()`를 호출**했는가? 안 하면 폰트가 페이지마다 통째로 임베드돼 파일이 수백 배로 커진다 (2026-09-17, 라벨 PDF가 131MB → 530KB)
- [ ] PDF 라이브러리 **기본 폰트로 한글을 넣지 않았는가?** base14 폰트는 라틴 전용이라 한글이 전부 `?`로 나온다 — 반드시 한글 폰트를 지정하고, 생성 후 렌더링해서 눈으로 확인할 것
- [ ] 실물 크기가 중요한 인쇄물이면 **PDF 좌표에서 배치 크기를 직접 측정**해 확인했는가? (2026-09-17 라벨: 100.0×100.0mm 확인)
- [ ] QR을 여러 개 만들었다면 **전부 디코딩해서 각각 맞는 주소로 가는지 대조**했는가? 라벨이 섞이면 현장에서 엉뚱한 설비 정보가 뜬다

### 배포 전

- [ ] `gate.js`의 `HASH_1`/`HASH_2`가 실제 사용 중인 암호의 SHA-256과 일치하는지 터미널에서 재계산해 확인 (§4.4 명령어는 `인트로_암호게이트_구현상세.md` 참고)
- [ ] 로컬 서버(`python -m http.server`, `.claude/launch.json`에 구성됨)로 실제 브라우저에서 동작 확인 — `file://`로 열면 ES module CORS 정책 때문에 PDF 뷰어가 안 됨
- [ ] 로컬 서버로 확인했는데 "분명히 고쳤는데 반영이 안 된 것처럼" 보인다면 — **먼저 브라우저 캐시부터 의심할 것.** `python -m http.server`는 `Cache-Control` 헤더를 안 보내서 브라우저가 `.js`/`.css` 파일을 적극적으로 캐싱한다. 콘솔에서 `fetch(url,{cache:'no-store'})`로 서버 쪽 최신 내용을 먼저 확인하고, 다르면 `fetch(url,{cache:'reload'})` 후 새로고침하거나 새 탭에서 여는 것으로 해결 (2026-09-22, `assistant.js` 수정이 반영 안 된 것처럼 보였던 사례)
- [ ] 모바일 뷰포트(375px)에서 레이아웃이 깨지지 않는지 확인
- [ ] 데스크톱 폭(640px 초과, 예: 1400px)에서도 상단 헤더와 본문·푸터의 가로폭이 서로 맞는지 확인 (2026-09-22 사례, §17)
- [ ] `git push` 후 GitHub Pages 빌드가 `built` 상태로 끝났는지 확인 — `gh api repos/dnk-mobility/dnk-mobility.github.io/pages/builds/latest`. 몇 분 지연될 수 있고, 빌드 전에는 새 파일이 404가 남
- [ ] 사용자에게 "고쳤다"고 보고하기 전에 — **로컬 검증만으로는 부족하다.** `git commit`/`push`를 안 하면 로컬에서 아무리 잘 동작해도 실제 사이트(`dnk-mobility.github.io`)는 옛날 코드 그대로다. 커밋·푸시·Pages 빌드 `built` 확인까지 끝내고, 가능하면 배포된 실제 주소에서 한 번 더 확인한 뒤에 "수정했다"고 말할 것 (2026-09-22, 로컬에서만 고쳐두고 배포를 안 해서 사용자가 똑같은 문제를 다시 보고한 사례)

### 문서화

- [ ] 구조·설계가 바뀌면 `PROJECT_OVERVIEW.md`(현재 상태 스냅샷)를, 문제→조치 이력이 생기면 `개선이력.md`(누적 로그)를 **그 자리에서** 갱신했는가? 나중으로 미루면 누락되기 쉬움 — §10 이력 표에서도 한 번 최신 커밋 반영을 빠뜨렸다가 다음 세션에서 발견한 전례가 있음
- [ ] 새로 만든 참고 문서(예: 벤치마킹 자료)가 있다면 §14 관련 산출물 표에 등록했는가?

---

## 18. 설비 부착용 QR 라벨 (2026-09-17 확정, 2026-09-17 70×100mm로 재조정)

현장 설비에 직접 붙이는 라벨. 생성은 [`tools/make_labels.py`](tools/make_labels.py), 결과물은 `qr-codes/설비QR라벨_70x100mm_A4인쇄용.pdf`.

### 규격

| 항목 | 값 |
|---|---|
| 라벨 크기 | **가로 70mm × 세로 100mm** (직사각형) — 최초 100×100mm 정사각형에서 부착 현장 여건에 맞춰 폭만 축소 |
| QR 크기 | 44mm (원안과 동일하게 유지), 오류정정 레벨 **H(30%)** — 현장 오염·긁힘에 대비 |
| 용지 배치 | A4 한 장에 4개(2열 × 2행), 라벨 간 여백 4mm, **설비 10개소 + 전체 인덱스 1개 = 11개(2026-09-22 추가)이므로 3페이지**(마지막 페이지는 3칸만 채움) |
| 재단 | 각 라벨 모서리에 ㄱ자 재단선, 가위로 잘라 사용 |
| 해상도 | 12px/mm(약 305dpi) — 840×1200px |

폭을 70mm로 줄일 때 QR·글자 크기·간격은 100×100mm 원안 그대로 두고 **좌우 여백만 축소**했습니다(내용을 다시 그리지 않음). 가장 긴 설비명(No.140)도 줄바꿈 추가 없이 한 줄에 들어가 본문 높이는 원안과 동일하게 87.5mm/100mm입니다.

### 디자인 구성 (위→아래)

`S C A N`(시안) → QR 카드(흰 배경 + 시안 코너 브래킷) → **공정No.**(큰 숫자) → 설비명 → "스캔하면 설비정보를 확인할 수 있습니다" → `DnK MOBILITY`

색상·코너 브래킷 모티프는 파비콘·진입 인트로(§5)와 동일해서 시스템 전체가 같은 아이덴티티로 보입니다. 시안 5개를 만들어 비교한 뒤 확정한 안입니다(네이비 풀블리드 기반 + 회사명 + SCAN 문구).

### 인쇄 시 주의

- 프린터 대화상자에서 반드시 **"실제 크기(100%)"** — "여백에 맞춤"을 켜면 크기가 맞지 않습니다. 출력 후 자로 재보면 확실합니다(가로 70mm, 세로 100mm).
- PDF 각 페이지 머리말·꼬리말에 같은 주의사항이 인쇄되어 있습니다.

### 내용이 바뀌면

`tools/make_labels.py`의 `EQUIPMENT` 목록(공정No.·설비명·페이지 파일명)만 고치고 다시 실행하면 전체가 재생성됩니다. 설비명이 길어지면 자동 줄바꿈되며, 라벨을 넘치면 스크립트가 에러로 알려줍니다. QR 주소 규칙은 `tools/make_qr.py`와 동일합니다(도메인이 바뀌면 두 파일 모두 수정).

### 전체 설비 인덱스(index.html) 라벨 추가 (2026-09-22)

기존에는 개별 설비 10개 라벨만 있고 `index.html`(전체 설비 목록 홈)로 바로 가는 라벨이 없었다(더 예전에 만든 `qr-codes/QR라벨_전체11개_통합.pdf`에는 있었지만, 2026-09-17에 지금의 70×100mm 디자인으로 새로 만들면서 빠졌던 것). 사용자가 "공정별 QR코드 말고 전체설비인덱스로 바로가는 QR코드"를 요청해서 다시 추가함.

- `tools/make_labels.py`의 `EQUIPMENT` 목록 맨 앞에 `INDEX_LABEL = ("전체", "전체 설비 인덱스", "")`를 추가 — `file=""`이면 `BASE_URL`(루트, 곧 `index.html`)로 QR이 연결됨. 공정No. 자리에는 숫자 대신 "전체"를 표시.
- 목록 맨 앞에 둔 이유: 개별 설비 라벨과 구분되는 "입구용/대표" 라벨이라는 의미. 현장 배치상 뒤가 낫다면 `EQUIPMENT` 리스트에서 위치만 옮기면 됨.
- 페이지 머리말(`No.110, No.120 ...`) 생성 로직이 숫자만 가정하고 있어서, 문자열 공정No.는 "No." 접두어 없이 설비명 그대로 표기하도록 분기 추가.
- 검증: `pyzbar`로 실제 PDF에서 QR을 디코딩해 `https://dnk-mobility.github.io/`(index.html 루트)로 정확히 연결되는 것, 기존 개별 설비 QR(예: No.110 → `laser-marking.html`)은 그대로인 것을 확인.
