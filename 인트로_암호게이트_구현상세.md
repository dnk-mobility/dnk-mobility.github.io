# 진입 인트로 · 암호 게이트 구현 상세 (벤치마킹용)

> 후공정 생산기술팀 · 작성 2026-09-17
> 대상: **e-AWD 모터 하우징 누설값 관리 시스템**(별도 저장소)에 같은 방식의 진입 연출·접속 제한을 이식할 때 참고
> 원본 구현: 이 저장소의 [`intro.js`](intro.js) · [`gate.js`](gate.js)
> 시스템 전체 구조는 [`PROJECT_OVERVIEW.md`](PROJECT_OVERVIEW.md) §5(인트로) / §8(게이트) 참고

이 문서는 "왜 이렇게 만들었는지"와 "수치를 바꾸면 무엇이 달라지는지"까지 적어둔 이식용 참고서입니다. 값만 베끼면 동작은 하지만, 아래 **§9 타이밍 함정**과 **§7 모바일 고려사항**을 건너뛰면 실제 현장에서 문제가 생깁니다.

---

## 1. 전체 진입 흐름

```
QR 스캔 (또는 주소 직접 입력)
   ↓
[인트로] 약 4.5초 연출              ← 인트로가 걸린 모든 페이지. 세션당 1회(첫 진입 페이지 무관)
   ↓
[1차 암호] 전체 화면 차단 + 입력창   ← 모든 페이지. 세션당 1회
   ↓
콘텐츠 표시
   ↓
[2차 암호] 잠금 항목을 펼칠 때만     ← 해당 항목이 있는 페이지에서만
```

- 인트로는 **암호창보다 먼저** 나옵니다. 순서가 반대면 "암호부터 묻고 연출"이 되어 어색합니다.
- 인트로가 없는 페이지(PDF 뷰어·404 등)는 인트로를 건너뛰고 곧바로 암호창이 뜹니다.
- 두 화면 모두 **배경색이 같아야**(`#0a2540`) 전환 이음새가 안 보입니다. 이 부분은 §3.5 참고.

> ⚠ **홈에만 넣을지, 콘텐츠 페이지에도 넣을지는 "QR이 홈을 거치는가"로 결정하세요.** 이 시스템도 처음엔 "홈에만" 설계였다가(§3.7 참고), 이후 **설비마다 QR을 붙여 홈을 거치지 않고 바로 들어가는 운영**으로 바뀌면서 그 경로로 들어간 사람은 인트로를 아예 못 보는 문제가 생겨 콘텐츠 페이지들에도 확장했습니다. sessionStorage 세션당 1회 제한이 이미 있어서 "여러 페이지에 걸어도 반복 재생되지 않는" 구조이므로, **QR이 개별 페이지로 직접 연결된다면 처음부터 전 진입점에 거는 것을 권장**합니다.

---

## 2. 파일 구성과 로드 방식

| 파일 | 로드 대상 | 역할 |
|---|---|---|
| `intro.js` | QR 진입점이 되는 페이지 전부(이 시스템은 홈+설비 10개) | 진입 연출. 끝나면 `window.dnkIntro.done`(Promise) resolve |
| `gate.js` | 전 페이지 | 1차·2차 암호. `window.dnkGate.ready`(Promise) 노출 |

```html
<body>
<script src="intro.js"></script>   <!-- QR로 바로 열릴 수 있는 페이지엔 전부. gate.js보다 반드시 먼저 -->
<script src="gate.js"></script>
<noscript>...</noscript>
<!-- 이하 실제 콘텐츠 -->
```

- **`<head>`가 아니라 `<body>` 최상단**에 둡니다. `<head>`에 두면 `document.documentElement`에 요소를 붙일 때 시점 문제가 생길 수 있고, 본문보다 먼저 실행되어야 하므로 body 첫 줄이 가장 안전합니다.
- `async`/`defer`를 **붙이지 않습니다.** 동기 실행이어야 콘텐츠가 그려지기 전에 화면을 가릴 수 있습니다.
- 두 파일 모두 자기 `<style>`을 JS로 주입합니다(외부 CSS 의존 없음). 그래서 어떤 페이지에 붙여도 그대로 동작합니다.

---

## 3. 인트로 애니메이션 상세 (`intro.js`)

### 3.1 화면 구성

네이비(`#0a2540`) 전체 화면 위에 4개 요소가 순서대로 등장합니다.

| 요소 | 내용 | 스펙 |
|---|---|---|
| `.ret` | QR 조준선 (코너 브래킷 4개) | 88×88px, 각 코너 20×20px, `2px solid #37c6e0`, 아래 여백 26px |
| `.scan` | 스캔 라인 | 높이 2px, 좌우 -7px씩 삐져나옴, 시안 그라데이션(가운데만 진함), `box-shadow:0 0 12px rgba(55,198,224,.65)` |
| `.brand` | `DnK MOBILITY` | 11px, `letter-spacing:.16em`, `#8fb3d6` |
| `.title` | `공정 설비 정보관리` | 19px, `font-weight:800`, `#fff`, 위 여백 7px |
| `.rule` | 제목 밑줄 | 2px 높이, `#37c6e0`, 0 → **92px**로 늘어남, 위 여백 13px |

### 3.2 애니메이션 타임라인 (핵심)

`.run` 클래스가 붙는 순간이 0ms 기준입니다.

| 순서 | 요소 | 지연(delay) | 길이(duration) | 이징 | 종료 시점 | 효과 |
|---|---|---|---|---|---|---|
| 1 | `.ret` | 0ms | **450ms** | `ease` | 450ms | opacity 0 → 1 |
| 2 | `.scan` | 280ms | **1,000ms** | `cubic-bezier(.4,0,.2,1)` | 1,280ms | top 0 → 100% 통과, 양 끝에서 페이드 |
| 3 | `.brand` | 800ms | **2,000ms** | `ease` | 2,800ms | opacity 0→1 + `translateY(7px)`→0 |
| 4 | `.title` | 1,000ms | **2,000ms** | `ease` | **3,000ms** ← 가장 늦음 | 위와 동일 |
| 5 | `.rule` | 1,350ms | **1,000ms** | `cubic-bezier(.4,0,.2,1)` | 2,350ms | width 0 → 92px |

스캔 라인 키프레임은 양 끝을 잘라 자연스럽게 만듭니다:
```css
@keyframes dnkScan{
  0%  {top:0;    opacity:0;}
  14% {          opacity:1;}
  86% {          opacity:1;}
  100%{top:100%; opacity:0;}
}
```

### 3.3 종료·페이드아웃 구간

| 상수 | 값 | 의미 |
|---|---|---|
| `TAIL_MS` | **1,000ms** | 모든 애니메이션이 끝난 뒤 **화면이 그대로 멈춰 있는 시간** |
| `FADE_MS` | **1,200ms** | 페이드아웃(opacity 1→0) 길이 |
| `TIMEOUT_MS` | **6,000ms** | `animationend`가 안 올 때를 대비한 안전 종료 |
| DOM 제거 | 페이드 시작 + `FADE_MS + 60ms` | 여유 60ms 후 요소·스타일 제거 |

**이론상 전체 길이** = 3,000(마지막 애니메이션) + 1,000(TAIL) + 1,200(페이드) ≈ **5.2초**

**실측값** (페이지 로드 = `performance.now()` 0 기준, 실제 브라우저 측정):

| 시점 | 측정 A (2026-09-03) | 측정 B (2026-09-17, 로컬서버 첫 로드) |
|---|---|---|
| 밑줄 완성 | 2,791ms | — |
| 제목 완전 등장 | 3,271ms | — |
| 페이드아웃 시작 = **암호창 등장** | 4,531ms | **6,028ms** |
| 인트로 DOM 제거 | 5,821ms | **7,303ms** |
| (페이드 시작 → DOM 제거 간격) | 1,290ms | **1,275ms** |

> ⚠ **절대 시각은 환경에 따라 1.5초까지 차이가 납니다.** 위 두 측정의 간격(1,290 / 1,275ms)은 설정값 `FADE_MS(1,200) + 60ms`와 정확히 일치하지만, 페이지 로드 기준 절대 시각은 서버 응답·스크립트 로드·CSS 애니메이션 시작 지연에 따라 통째로 밀립니다.
>
> **따라서 이식할 때는 절대 시각을 목표로 잡지 말고, `delay`/`duration`/`TAIL_MS`/`FADE_MS` 의 상대 구조를 그대로 가져간 뒤 실제 단말에서 체감으로 조정하세요.** 절대 시각을 기준으로 다른 처리를 물려놓으면(예: "4.5초 뒤에 무엇을 한다") 느린 회선에서 어긋납니다. 이 시스템이 고정 타이머 대신 `animationend` 카운팅을 쓰는 이유이기도 합니다(§3.5).

### 3.4 ⚠ 가장 중요한 설계 포인트 — "읽을 시간"은 `TAIL_MS`가 결정한다

처음에 "글자가 너무 빨리 사라진다"는 피드백을 받고 **페이드인 시간(2초)을 늘려봤지만 해결되지 않았습니다.** 등장 시간을 늘리면 *글자가 완전히 또렷해지는 시점만 뒤로 밀릴 뿐*, 또렷한 상태로 멈춰 있는 시간은 그대로이기 때문입니다.

| `TAIL_MS` | 완전 가독 구간 | 결과 |
|---|---|---|
| 300ms | 570ms | "읽기 전에 사라진다" 피드백 |
| **1,000ms** | **1,260ms** | 현재 값. 여기에 페이드아웃 1.2초 동안도 어느 정도 읽힘 |

**연출 속도를 조정할 때는 `TAIL_MS`부터 만지세요.**

### 3.5 종료 판정 로직 (고정 타이머를 쓰지 않는 이유)

```js
var animated = ov.querySelectorAll(".ret,.scan,.brand,.title,.rule");
var pending = animated.length;              // 5개
var safety = setTimeout(finish, TIMEOUT_MS); // 안전장치
ov.addEventListener("animationend", function () {
  pending -= 1;
  if (pending > 0) return;
  clearTimeout(safety);
  setTimeout(finish, TAIL_MS);              // 전부 끝난 뒤 TAIL_MS 정지
});
```

두 가지 이유로 **개수를 세는 방식**을 씁니다.

1. 고정 타이머(`setTimeout(finish, 4500)`)로 재면, 최초 로드 때 애니메이션 시작이 수백 ms 밀려 **마지막 동작이 잘립니다.**
2. "가장 늦게 끝나는 하나"만 지켜보면, 나중에 delay·duration을 바꿨을 때 **순서가 뒤바뀌어** 다른 동작이 잘립니다. (실제로 현재는 밑줄이 아니라 *제목*이 가장 늦게 끝납니다.)

### 3.6 인트로 → 암호창 전환 이음새

```js
function finish() {
  if (closed) return;
  closed = true;
  mark();
  resolveDone(true);        // ① 먼저 resolve → gate.js가 암호창을 올림
  ov.classList.add("out");  // ② 그 다음 인트로 페이드아웃 시작
  setTimeout(function () { ov.remove(); style.remove(); }, FADE_MS + 60);
}
```

- **resolve를 페이드아웃보다 먼저** 합니다. 암호창(z-index 99999)이 인트로(99998) 위에 이미 올라온 상태에서 인트로가 걷히므로 중간에 본문이 번쩍 보이지 않습니다.
- 두 화면 배경이 같은 `#0a2540`이라 페이드 중에도 경계가 안 보입니다. **색을 바꾸려면 두 파일을 같이 바꿔야 합니다.**

### 3.7 재생 조건

| 조건 | 동작 |
|---|---|
| `sessionStorage["dnk_intro_shown_v1"] === "1"` | **재생 안 함** (세션당 1회) |
| `prefers-reduced-motion: reduce` | **재생 안 함** + 본 것으로 기록 |
| 화면 아무 곳이나 탭/클릭 | 즉시 종료(스킵) |
| sessionStorage 접근 실패(사생활 보호 모드 등) | 안전하게 "이미 본 것"으로 처리 → 재생 안 함 |

> 현장에서 하루에 QR을 여러 번 찍는데 매번 5초를 보게 하면 방해가 됩니다. 그래서 **세션당 1회**로 제한했습니다 — 이 제한 덕분에 지금은 진입점이 될 수 있는 페이지 전부(홈+설비 10개)에 걸어놔도 "같은 근무자가 같은 설비를 반복 스캔"하는 상황에서 매번 뜨지는 않습니다. (초기에는 홈 1개에만 넣었었는데, §1의 경고 참고 — QR이 개별 페이지로 바로 연결되는 운영으로 바뀌면서 확장했습니다.)

---

## 4. 1차 암호 게이트 상세 (`gate.js`)

### 4.1 화면 차단 방식

```js
document.documentElement.style.visibility = "hidden";   // 스크립트 최상단, 동기 실행
```

- `body`가 아니라 **`documentElement`(`<html>`)** 에 겁니다. 나중에 JS로 붙이는 오버레이가 `<html>` 바로 아래에 들어가도 같이 숨겨지게 하려는 의도입니다.
- 암호창 자신만 `visibility:visible`로 되살립니다(`.dnk-gate` 규칙). 이 한 줄이 빠지면 암호창까지 같이 숨겨져 **새까만 화면**이 됩니다.
- 통과 시 `visibility = "visible"`로 되돌립니다.
- `display:none`이 아니라 `visibility`를 쓰는 이유: 레이아웃은 그대로 두어 통과 직후 리플로우가 튀지 않게 하기 위함입니다.

### 4.2 모달 스펙

| 항목 | 값 |
|---|---|
| 배경 | `#0a2540` 전체 화면 (`position:fixed; inset:0`) |
| z-index | **99999** |
| 카드 폭 | `max-width:300px`, 좌우 패딩 24px |
| 브랜드 문구 | 11px, `#8fb3d6`, `letter-spacing:.06em` |
| 제목 | 17px, `font-weight:800`, `#fff` |
| 보조 설명 | 12px, `#8fb3d6`, `line-height:1.5` |
| 입력창 | 패딩 12/14px, `border-radius:8px`, 테두리 `#1a4971`, 배경 `#0f3255`, 글자 `#fff`, **`font-size:16px`**, 가운데 정렬 |
| 오류 문구 | 12px, `#f3a6a1`, `min-height:16px` (글자가 생겨도 레이아웃이 안 밀리게 자리를 미리 확보) |
| 확인 버튼 | 전체 폭, 패딩 12px, 배경 `#0f6cb0`, 14px/700, `:active{opacity:.85}` |
| 취소 버튼(2차만) | 전체 폭, 배경 없음, 12px, `#8fb3d6` |

### 4.3 입력·제출 동작

| 동작 | 처리 |
|---|---|
| 창이 뜬 직후 | `setTimeout(..., 50)` 후 입력창 자동 포커스 |
| Enter 키 | 제출 (`keydown`에서 `e.key === "Enter"`) |
| 확인 버튼 클릭 | 제출 |
| **정답** | sessionStorage 기록 → 모달 DOM 제거 → 콘텐츠 표시 |
| **오답** | `"암호가 올바르지 않습니다"` 표시 → 입력창 비움 → 다시 포커스 → **모달은 그대로 유지** |
| 시도 횟수 | **제한 없음** (잠금·대기시간 없음) |
| 자동완성 | `autocomplete="off"` — 공용 암호라서 개인 비밀번호 관리자에 저장되지 않게 함 |

### 4.4 암호 비교 방식

평문을 코드에 남기지 않기 위해 **SHA-256 해시**로 비교합니다.

```js
function sha256Hex(text) {
  var buf = new TextEncoder().encode(text);
  return crypto.subtle.digest("SHA-256", buf).then(function (hash) {
    return Array.prototype.map.call(new Uint8Array(hash),
      function (b) { return ("0" + b.toString(16)).slice(-2); }).join("");
  });
}
```

새 암호의 해시 만들기:
```bash
node -e "console.log(require('crypto').createHash('sha256').update('새암호','utf8').digest('hex'))"
```

> ⚠ **`crypto.subtle`은 HTTPS 또는 localhost에서만 동작합니다.** 사내에서 `http://` 주소로 열면 암호 확인이 아예 안 되므로, 이식 대상 시스템이 http면 이 방식을 못 씁니다.
>
> ⚠ 해시라도 **소스에 그대로 노출**되므로 오프라인 대입은 가능합니다. §11 한계 참고.

---

## 5. 세션 유지 규칙 — "언제 다시 물어보는가" ★

가장 자주 헷갈리는 부분이라 따로 정리합니다. 저장소는 **`sessionStorage`** 입니다.

| 키 | 용도 |
|---|---|
| `dnk_gate_ok_v1` | 1차 통과 여부 |
| `dnk_gate2_ok_v1` | 2차 통과 여부 |
| `dnk_intro_shown_v1` | 인트로 재생 여부 |

### 5.1 상황별 동작

| 상황 | 다시 물어보나? | 이유 |
|---|---|---|
| 같은 탭에서 다른 페이지로 이동 | **아니오** | sessionStorage는 같은 탭·같은 출처면 공유 |
| 새로고침(F5) | **아니오** | 탭이 유지되므로 그대로 |
| 뒤로 가기 / 앞으로 가기 | **아니오** | 동일 |
| **탭을 닫았다가 다시 열기** | **예, 1차부터** | 탭이 닫히면 sessionStorage 소멸 |
| **브라우저를 완전히 종료 후 재실행** | **예, 1차부터** | 동일 |
| **QR을 다시 스캔** (보통 새 탭으로 열림) | **예, 1차부터** | 새 탭 = 새 sessionStorage |
| 주소를 복사해 새 탭에 붙여넣기 | **예** | 완전히 새로 만든 탭은 항상 빈 sessionStorage |
| 같은 탭에서 PDF 뷰어 열기(`target="_blank"` 아님) | 아니오 | 동일 탭 |
| **첨부문서 링크로 새 탭 열기**(현재 구현: `target="_blank" rel="noopener"`) | **브라우저에 따라 다름 — 아래 주석 확인** | |
| 시간이 오래 지남 (탭은 열어둔 채) | **아니오** | 시간 만료 개념 없음 |

> ⚠ **`target="_blank"`로 열린 새 탭은 확인이 필요합니다.**
> HTML 표준상 기존 페이지에서 만들어진 새 탭은 **원래 탭의 sessionStorage를 복사해서** 시작합니다(= 암호를 다시 안 물음). 그런데 이 시스템의 첨부문서 링크처럼 **`rel="noopener"`가 붙으면** 새 탭이 생성 관계 없이 열려 **빈 sessionStorage로 시작(= 다시 물음)** 하는 경우가 있고, 이 동작은 브라우저·버전마다 차이가 있습니다.
> 개발 중 확인 환경에서는 팝업이 같은 탭 이동으로 처리되어 재현하지 못했습니다. **누설값 시스템에서 이 동작이 중요하다면(예: 첨부 열 때마다 암호를 또 묻게 할지 말지), 실제 현장 단말(안드로이드 크롬 / iOS 사파리)에서 직접 눌러보고 결정하세요.**
> 매번 다시 묻는 게 싫다면 `target="_blank"`를 빼서 같은 탭에서 열면 확실하게 유지됩니다.

> **"창을 닫으면 다시 암호를 묻는다"가 의도된 동작입니다.** 현장 공용 스마트폰에서 이전 사용자의 인증 상태가 남아 있지 않게 하려는 목적입니다.

### 5.2 이력: 왜 localStorage를 안 쓰는가

| 시기 | 방식 | 문제 |
|---|---|---|
| 최초 | `localStorage` + 30일 만료 | 공용 기기에서 **한 달 동안 아무나 열람 가능** |
| 2026-08-17~ | **`sessionStorage`** (현재) | 탭을 닫으면 초기화 |

localStorage(브라우저를 닫아도 유지)를 쓰면 편하지만, 공용 단말에서는 사실상 잠금이 풀린 상태가 됩니다. **누설값 관리 시스템도 공용 단말에서 쓴다면 sessionStorage를 권장합니다.**

### 5.3 "조금 더 오래 유지"가 필요하다면

현장에서 재입력이 잦아 불편하면 아래 중 하나를 검토하세요(현재 시스템은 1번을 선택).

1. **현행 유지** — 탭 단위. 가장 안전.
2. `sessionStorage` + 마지막 활동 시각 기록 → N분 미사용 시 재인증. 코드 약간 추가.
3. `localStorage` + 만료 시각 저장 → 편하지만 공용 단말에서 위험.

---

## 6. 2차 암호 (항목별 잠금) 상세

### 6.1 마킹 방식 — HTML 한 줄이면 끝

```html
<details class="section" data-lock="2">
  <summary><span class="num">③</span>기술문서<span class="lock">잠금</span><span class="chev">▾</span></summary>
  ...
</details>
```

`gate.js`는 **`data-lock="2"` 속성만 보고** 동작합니다. 새 항목을 잠그고 싶으면 이 속성과 배지만 추가하면 되고 JS는 손댈 필요가 없습니다.

### 6.2 동작 흐름

| 단계 | 동작 |
|---|---|
| 1 | `<summary>` 클릭을 가로채 `e.preventDefault()` — 기본 펼침을 막음 |
| 2 | **반투명 모달**(`.dim`: `rgba(10,37,64,.72)` + `backdrop-filter:blur(2px)`) 표시. 1차와 달리 뒤 화면이 비쳐 "이 항목만 잠겨 있다"는 게 보임 |
| 3 | **취소 버튼 제공** — 취소하면 아무 일도 없이 닫힘(1차에는 취소가 없음) |
| 4 | 정답이면 `dnk_gate2_ok_v1` 기록 → **페이지 안의 모든 잠금 항목이 동시에 해제** → 클릭했던 항목을 자동으로 펼침 |
| 5 | 배지가 주황 `잠금` → 초록 `해제`로 바뀜 (`.section.unlocked` 클래스) |

### 6.3 파일 직접 접근 대응

첨부 PDF를 주소로 직접 열어도 2차를 묻도록, **파일명 규칙**으로 판정합니다.

```js
var TECH_PDF_RE = /-(circuit|drawing)\.pdf$/i;   // *-circuit.pdf, *-drawing.pdf
```

뷰어 페이지는 게이트 통과를 기다린 뒤에야 파일을 내려받습니다(게이트 전에는 네트워크 요청조차 나가지 않음):

```js
await window.dnkGate.ready;
// 이 뒤에서 PDF 로드
```

> 파일명 규칙이 곧 보안 기준이므로, **다른 이름으로 올리면 2차가 안 걸립니다.** 이식할 때 규칙을 먼저 정하세요.

---

## 7. 모바일 전용 고려사항 ★

실제 현장(장갑 낀 손, 스마트폰)에서 문제가 됐던 것들입니다. **이식할 때 이 절은 꼭 반영하세요.**

### 7.1 iOS 입력창 자동 확대 — 16px 규칙

iOS Safari는 **글자 크기가 16px 미만인 입력창에 포커스하면 화면을 멋대로 확대**합니다. 확대된 화면은 사용자가 직접 되돌려야 해서 매우 거슬립니다.

- 해결: 암호 입력창 `font-size:16px` (15px에서 올림). 지금은 위젯 입력창도 동일하게 16px입니다.
- 반대로 `maximum-scale=1.0`으로 확대 자체를 막는 방식은 **쓰지 마세요**(아래 7.2).

### 7.2 핀치 줌은 허용

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0">
```

- `maximum-scale=1.0`을 넣으면 자동 확대는 막히지만 **사용자가 손가락으로 확대하는 것도 막힙니다.** 접근성 관점에서 권장되지 않습니다.
- 이 시스템은 12px 미만 글자가 190곳이라 현장에서 확대가 꼭 필요했습니다. 누설값 표도 비슷할 테니 확대는 열어두는 쪽을 권장합니다.

### 7.3 hover가 터치에서 눌어붙는 문제

안드로이드 크롬 등에서 한 번 탭한 요소에 hover 상태가 **계속 남아** 강조된 것처럼 보입니다.

```css
/* 마우스 환경에서만 hover */
@media (hover:hover) and (pointer:fine) {
  .card:hover { ... }
}
/* 터치 피드백은 :active로 따로 */
.card:active { transform: scale(.985); }
```

### 7.4 안전영역(노치·홈 인디케이터)

하단 고정 요소는 iPhone 홈 인디케이터에 가리지 않게:

```css
bottom: calc(16px + env(safe-area-inset-bottom, 0px));
```

### 7.5 동작 줄이기 설정 존중

```js
window.matchMedia("(prefers-reduced-motion: reduce)").matches  // 이면 인트로 생략
```
CSS 쪽도 `@media (prefers-reduced-motion:reduce){ transition:none!important; }`.

### 7.6 인앱 브라우저 주의

카카오톡·메신저 인앱 브라우저로 열면 `sessionStorage`가 제한되거나 매번 새 컨텍스트로 열려 **암호를 계속 다시 묻는** 경우가 있습니다. QR은 보통 기본 브라우저로 열리지만, 링크를 메신저로 공유하는 운영이라면 미리 확인하세요. (코드는 `try/catch`로 감싸 실패해도 죽지 않게 되어 있습니다.)

---

## 8. 레이어(z-index) 정리

| 요소 | z-index |
|---|---|
| `<noscript>` 안내(JS 꺼짐) | 999999 |
| 암호 모달 `.dnk-gate` | **99999** |
| 인트로 `#dnk-intro` | **99998** |
| 챗봇형 안내 위젯 | 9000 |
| 일반 콘텐츠 | 지정 없음 |

암호 모달이 항상 최상단이어야 합니다. 나중에 위젯·툴팁 등을 추가할 때 이 값을 넘지 않게 하세요.

---

## 9. ⚠ 타이밍 함정 (실제로 겪은 버그)

### 9.1 게이트 이후 실행되는 스크립트는 DOM 완성을 보장받지 못한다

`gate.js`는 `window.dnkGate.ready`(Promise)를 노출하고, 뒤따르는 스크립트는 이걸 기다렸다가 동작합니다.

```js
(window.dnkGate && window.dnkGate.ready ? window.dnkGate.ready : Promise.resolve(true))
  .then(function () { /* ... */ });
```

문제는 **이미 인증된 세션에서 다른 페이지로 이동할 때**입니다. 이때는 사용자 입력이 필요 없어 인증 확인이 **마이크로태스크만으로 즉시** 끝나므로, `ready`가 브라우저가 본문을 다 파싱하기도 **전에** resolve될 수 있습니다. 그 시점에 `document.querySelectorAll(...)`을 하면 빈 결과가 나와 조용히 실패합니다.

- 첫 방문(암호 입력 필요)에는 사용자가 타이핑하는 동안 파싱이 끝나 **증상이 안 보입니다.** 그래서 재현이 간헐적으로 보입니다.
- 해결: 아래 패턴을 **기본으로** 쓰세요.

```js
function onReady(fn) {
  if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", fn);
  else fn();
}
gateReady.then(function () { onReady(init); });
```

### 9.2 `animationend`는 탭이 백그라운드면 안 올 수 있다

브라우저가 숨겨진 탭의 애니메이션·`requestAnimationFrame`을 멈추기 때문에, 인트로 종료가 영영 안 될 수 있습니다. → `TIMEOUT_MS` 안전장치가 반드시 필요합니다(현재 6초).

---

## 10. 이식 체크리스트

누설값 관리 시스템에 가져갈 때 **반드시 바꿔야 하는 것**:

- [ ] `sessionStorage` 키 3개 — `dnk_gate_ok_v1` / `dnk_gate2_ok_v1` / `dnk_intro_shown_v1` → 시스템별로 다르게(두 시스템을 같은 도메인에 두면 키가 충돌합니다)
- [ ] `HASH_1` / `HASH_2` — **새 암호의 해시로 교체** (§4.4 명령어)
- [ ] 인트로 문구 — `DnK MOBILITY` / `공정 설비 정보관리` → 새 시스템명
- [ ] 암호창 문구 — `사내 전용 페이지입니다`, 2차 안내 문구
- [ ] 2차 잠금 대상 — `data-lock="2"`를 어떤 항목에 붙일지 (누설값이면 *원본 측정 데이터*·*보정 이력* 등이 후보)
- [ ] 직접 접근 방어가 필요한 파일이 있다면 `TECH_PDF_RE` 파일명 규칙 재정의
- [ ] 색상 — 인트로 배경과 암호창 배경을 **같은 값**으로 (§3.6)
- [ ] 인트로를 넣을 페이지 선택 — **QR이 각 설비/항목 페이지로 직접 연결된다면 그 페이지들 전부에 넣으세요.** "진입점 1개만"으로 시작했다가 QR을 개별 페이지에 붙이는 운영으로 바뀐 뒤에야 확장한 전례가 있습니다(§1 경고 참고). 세션당 1회 제한이 있어 여러 페이지에 걸어도 반복 재생되지 않으니, 처음부터 넣는 편이 낫습니다.

**그대로 가져가도 되는 것**: 타이밍 상수(`TAIL_MS`/`FADE_MS`/`TIMEOUT_MS`), 애니메이션 delay·duration, 16px 입력창, `onReady` 패턴, z-index 체계, 세션 규칙.

---

## 11. 한계 (반드시 같이 인지할 것)

이 방식은 **실제 보안 장치가 아닙니다.** "우연히/장난으로 들여다보는 것"을 막는 수준입니다.

| 항목 | 내용 |
|---|---|
| 소스 노출 | 개발자도구로 `gate.js`를 열면 로직과 **해시가 그대로 보입니다.** 8자리 영숫자 암호면 오프라인 대입으로 복원 가능 |
| JS 비활성화 | 화면 가리기가 JS로 이뤄지므로 **JS를 끄면 그대로 노출**됩니다 → `<noscript>`로 본문을 숨기고 안내를 띄우는 방어를 추가해 두었습니다(완전한 차단은 아님) |
| 크롤러 | JS를 실행하지 않는 크롤러에는 게이트가 무의미 → `robots.txt` + `<meta name="robots" content="noindex,...">` 병행 필요 |
| 파일 직접 접근 | 첨부파일 주소를 알면 게이트와 무관하게 내려받아집니다. 진짜 민감한 파일은 정적 호스팅에 올리는 것 자체를 재검토해야 합니다 |
| 개인 식별 없음 | 공용 암호 방식이라 **누가 봤는지 기록이 남지 않습니다.** 열람 이력이 필요하면 백엔드(계정 체계)가 필요합니다 |

> 누설값 관리 시스템은 **측정 원본 데이터**를 다루므로, 위 한계 중 "개인 식별 없음"과 "파일 직접 접근"이 이 시스템보다 더 중요할 수 있습니다. 구글시트 연동을 쓴다면 시트 자체의 공유 권한이 실질적인 방어선이 되므로, 게이트는 어디까지나 *화면 노출 억제* 용도로만 보세요.

---

## 12. 최소 이식 스켈레톤

가장 단순한 형태(1차 암호만, 인트로 없음)입니다. 여기에 필요한 것만 얹으세요.

```js
(function () {
  "use strict";
  var KEY  = "leak_gate_ok_v1";                  // ← 시스템별로 변경
  var HASH = "여기에_새_암호의_SHA256_해시";       // ← 반드시 교체

  function isOk() { try { return sessionStorage.getItem(KEY) === "1"; } catch (e) { return false; } }
  function markOk() { try { sessionStorage.setItem(KEY, "1"); } catch (e) {} }

  function sha256Hex(t) {
    return crypto.subtle.digest("SHA-256", new TextEncoder().encode(t)).then(function (h) {
      return Array.prototype.map.call(new Uint8Array(h),
        function (b) { return ("0" + b.toString(16)).slice(-2); }).join("");
    });
  }

  document.documentElement.style.visibility = "hidden";   // 즉시 가림

  function show() {
    var style = document.createElement("style");
    style.textContent =
      ".gate{position:fixed;inset:0;background:#0a2540;display:flex;align-items:center;" +
      "justify-content:center;z-index:99999;visibility:visible;}" +
      ".gate-card{width:100%;max-width:300px;padding:0 24px;text-align:center;}" +
      ".gate-input{width:100%;box-sizing:border-box;padding:12px 14px;border-radius:8px;" +
      "border:1px solid #1a4971;background:#0f3255;color:#fff;font-size:16px;text-align:center;}" +
      ".gate-btn{margin-top:6px;width:100%;padding:12px;border:none;border-radius:8px;" +
      "background:#0f6cb0;color:#fff;font-size:14px;font-weight:700;}" +
      ".gate-err{color:#f3a6a1;font-size:12px;min-height:16px;margin-top:8px;}";
    document.documentElement.appendChild(style);

    var w = document.createElement("div");
    w.className = "gate";
    w.innerHTML =
      '<div class="gate-card">' +
      '<div style="color:#fff;font-size:17px;font-weight:800;margin-bottom:12px;">사내 전용 페이지입니다</div>' +
      '<input class="gate-input" type="password" placeholder="접속 암호" aria-label="접속 암호" autocomplete="off">' +
      '<div class="gate-err" role="alert"></div>' +
      '<button class="gate-btn" type="button">확인</button>' +
      '</div>';
    document.documentElement.appendChild(w);

    var input = w.querySelector(".gate-input");
    var err   = w.querySelector(".gate-err");

    function tryUnlock() {
      sha256Hex(input.value).then(function (hex) {
        if (hex === HASH) {
          markOk();
          w.remove();
          document.documentElement.style.visibility = "visible";
        } else {
          err.textContent = "암호가 올바르지 않습니다";
          input.value = "";
          input.focus();
        }
      });
    }
    w.querySelector(".gate-btn").addEventListener("click", tryUnlock);
    input.addEventListener("keydown", function (e) { if (e.key === "Enter") tryUnlock(); });
    setTimeout(function () { input.focus(); }, 50);
  }

  if (isOk()) document.documentElement.style.visibility = "visible";
  else show();
})();
```
