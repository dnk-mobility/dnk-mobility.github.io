# 마스터 샘플·보정 이력 — 누설값관리시스템 인계 문서

> 작성일: 2026-09-18
> 인계 사유: "공정 설비 정보관리 시스템"(이 저장소, 정적 QR 페이지)은 실측값을 계속 누적 입력할 방법이 없는 **읽기 전용 시스템**입니다. 마스터 샘플 보정값처럼 현장에서 계속 쌓이는 측정 데이터는 별도 개발 중인 **누설값관리시스템**에서 다루는 것이 맞다고 판단해, 기존 섹션을 삭제하고 이 문서로 내용을 인계합니다.
> 원래 위치: 리크테스터 설비 4개소 페이지의 "⑨ 마스터 샘플 · 보정 이력" 아코디언 섹션 (2026-08-31 신설 → 2026-09-18 삭제, 상세 이력은 `개선이력.md` 참고)

---

## 1. 무엇을 기록하던 화면인가

리크(누설) 검사 설비의 **디텍터(LS-R700) 보정 작업** 이력을 기록하는 표였습니다. "마스터 샘플"(리크값이 이미 알려진 기준 시료)을 디텍터에 흘려 측정값이 기준과 맞는지 점검하고, 안 맞으면 Fixed comp value(보정계수)를 수정하는 작업의 **전/후 값**을 남겨 추적성을 확보하는 용도입니다.

### 대상 설비 (4개소 — 전부 LS-R700 디텍터 사용)

| 공정No. | 설비명 | 원래 페이지 |
|---|---|---|
| 120 | 조립전 유로계 리크검사기 | `leak-pre-flow.html` |
| 130 | 조립전 전체계 리크검사기 | `leak-pre-full.html` |
| 180 | 조립후 유로계 리크검사기 | `leak-post-flow.html` |
| 190 | 조립후 전체계 리크검사기 | `leak-post-full.html` |

대상 부품은 4개소 공통으로 품번 `47750-0E000`(HOUSING ASS'Y-MOTOR), e-AWD 70kW 모터 하우징입니다.

---

## 2. 데이터 구조 (원본 표 컬럼)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| 일자 | 날짜 | 보정 작업 수행일 |
| 샘플명 | 문자열 | 예: "OK 마스터(하우징)", "OK 마스터(인버터)" — 설비별로 여러 기준 시료를 쓸 수 있음 |
| 리크값 — 전 | 숫자 (mL/min) | 보정 작업 **전** 측정된 리크값 |
| 리크값 — 후 | 숫자 (mL/min) | 보정 작업 **후** 측정된 리크값 |
| 함침여부 | Y / 공란 | 마스터 샘플을 **재함침 처리**했으면 Y로 표기 |
| Fixed comp value — 전 | 숫자 | 보정 작업 **전** 디텍터 설정값 |
| Fixed comp value — 후 | 숫자 | 보정 작업 **후** 디텍터 설정값 |
| 설비주변온도 | 숫자 (℃) | 측정 시점의 설비 주변 온도 |
| 판정 | OK / NG | 정상(OK) · 이상(NG) |
| 확인자 | 문자열 | 담당자명 — 기존 데이터는 전부 공란(담당자 직접 기입 필요했던 항목) |
| 비고 | 문자열 | 자유 기재 |

- 리크값·Fixed comp value는 "보정 전/후"를 각각 별도 열로 관리했습니다(2026-09-02 개정 — 원래는 한 값만 기록했다가, 보정 작업의 효과를 비교하려면 전/후가 다 필요하다는 게 드러나 전/후 2열로 분할함). **새 시스템에서도 전/후 값을 각각 기록할 수 있는 구조를 유지하는 걸 권장합니다.**
- 이력은 **누적 기록**(새 행을 계속 맨 아래에 추가)이 전제였습니다 — 덮어쓰기가 아니라 타임라인/이력 형태로 설계해야 합니다.

---

## 3. 기존에 실제로 입력되어 있던 데이터 (마이그레이션 대상)

**공정No.130 (조립전 전체계 리크검사기, `leak-pre-full.html`)에만** 실데이터 2건이 입력되어 있었고, 나머지 3개소(120·180·190)는 전부 빈 표였습니다. 새 시스템으로 이 2건은 그대로 옮겨야 합니다.

| 일자 | 샘플명 | 리크값-전 | 리크값-후 | 함침여부 | comp value-전 | comp value-후 | 설비주변온도 | 판정 | 확인자 | 비고 |
|---|---|---|---|---|---|---|---|---|---|---|
| 2026-09-02 | OK 마스터(하우징) | -0.14 | 0.64 | Y | 3.5 | 3.0 | 22.4℃ | OK | (공란) | (공란) |
| 2026-09-02 | OK 마스터(인버터) | 2.84 | 1.17 | Y | 0 | 2 | 22.4℃ | OK | (공란) | (공란) |

두 행 모두 확인자 칸이 비어 있던 상태 그대로입니다 — 담당자명은 실제 작업자에게 확인 후 채워 넣어야 합니다.

---

## 4. 접근 권한 — 반드시 유지해야 할 요건

원래 이 섹션은 **2단계 암호(2차 암호)로 잠근 항목**이었습니다(`data-lock="2"`, 기술문서와 동일 등급). 즉 시스템 접속 후에도 이 데이터를 보려면 별도 암호를 한 번 더 요구했습니다. 보정값·설비 내부 파라미터라는 민감도 때문입니다.

**누설값관리시스템에서도 이 데이터는 일반 조회보다 한 단계 높은 접근 제한을 두는 걸 권장합니다** (구체적인 구현 방식은 그 시스템의 인증 구조에 맞춰 설계하되, "아무나 못 보게" 하는 요건 자체는 유지).

---

## 5. 참고 — 원본 화면에 있던 안내 문구

- 섹션 설명: "마스터 샘플 리크값 점검, 디텍터(LS-R700) Fixed comp value 수정 이력을 아래 표에 계속 누적 기록합니다 (새 이력은 맨 아래에 행 추가)."
- 표 하단 각주: "리크값 · Fixed comp value: 보정 작업 전/후 값을 각각 기록 · 함침 여부: 마스터 샘플 재함침 처리 시 Y로 표기 · 설비주변온도: 측정 시점의 설비 주변 온도 · 판정: 정상(OK)/이상(NG)"

---

## 6. 이 저장소 쪽 처리 결과 (참고용)

- 4개 설비 페이지에서 "⑨ 마스터 샘플 · 보정 이력" 섹션 전체 삭제, 이후 섹션("최근 공지") 번호를 ⑩→⑨로 조정
- 관련 CSS(`table.hist`, `.table-scroll`), 챗봇 위젯(`assistant.js`)의 검색 토픽·잠금 대상 언급, `gate.js` 주석에서도 참조 제거
- 상세 경위는 `개선이력.md`의 "2026-09-18 마스터 샘플·보정 이력 섹션 삭제 및 누설값관리시스템 이관" 항목 참고
