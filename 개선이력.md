# 공정 설비 정보관리 시스템 — 개선 이력 (상세)

> 후공정 생산기술팀 · 사내 개선보고 자료용 원본 기록
> 이 문서는 개선 항목별로 **문제 → 발견 경위 → 위험도 → 조치 → 검증 → 효과**를 남기는 누적 기록입니다.
> 시스템 구조 전반은 `PROJECT_OVERVIEW.md` 참고.

---

## 2026-09-03 시스템 전면 재점검 (개선3 후속)

배경: 기술문서 섹션 신설·2단계 암호·인트로 등 개선3 작업 완료 후, 시스템 전체를 재점검하여 아래 6건을 발견·조치함. 관련 커밋: `aac8a5f`(1~4번), 이후 커밋(5~6번).

### 점검 방법

- 저장소 전 파일 자동 검사 스크립트 작성·실행: HTML 태그 짝·중복 ID·링크 무결성·섹션 번호 순서·페이지 간 CSS 일치 여부·QR↔페이지 매핑·메타 태그 전수 확인
- 실제 운영 주소(`https://dongnam959.github.io`)에 **비인증 GET 요청**을 보내 크롤러 관점의 노출 범위 실측
- 로컬 서버 + 브라우저(모바일 375px 뷰포트)로 수정 전후 동작 검증

### 이상 없음으로 확인된 항목

| 항목 | 결과 |
|---|---|
| HTML 구조 | 12개 파일 전부 여는/닫는 태그 짝 정상, 중복 ID 없음 |
| 외부창 링크 | `target="_blank"` 링크 전부 `rel="noopener"` 부착됨 |
| 섹션 번호 | 설비 10개 페이지 모두 ①부터 연속 (일반 8개 / 리크테스터 10개) |
| QR 매핑 | QR PNG 11개 ↔ 페이지 11개 1:1 대응, 누락·잉여 없음 |
| CSS 일치 | 설비 10개 페이지의 스타일 완전 동일 (리크테스터 전용 표 규칙 유무만 차이 — 정상) |

---

### [1] 검색엔진 노출 차단 — robots.txt + noindex (위험도: 높음)

**문제.** 접속 암호 게이트(`gate.js`)는 브라우저에서 실행되는 클라이언트 JavaScript다. 검색엔진 크롤러는 JS를 실행하지 않고 HTML 원문만 받아가므로, **암호가 크롤러에게는 전혀 걸리지 않는 상태**였다.

**발견 경위(실측).** 재점검 중 운영 주소에 암호 없이 GET 요청 1회를 보내 확인:

```
GET https://dongnam959.github.io/leak-pre-full.html  →  200 OK, 24,242 bytes
응답 본문에 포함: 마스터 샘플 보정값(Fixed comp value), 설비주변온도(22.4℃),
                  트러블슈팅 이력, 작업표준·검사규격 전문
```

**위험.** 방치 시 네이버·구글 등에 사내 작업표준·설비 데이터가 그대로 색인되어 검색 결과에 노출될 수 있음. QR 도입 이후 페이지 수와 데이터가 늘수록 노출 면적도 커지는 구조.

**조치.**

1. `robots.txt` 신규 작성 — `User-agent: * / Disallow: /` 로 수집 전면 차단 (HTML·PDF 포함)
2. 12개 페이지 전부 `<meta name="robots" content="noindex, nofollow, noarchive, nosnippet, noimageindex">` 추가 — robots.txt를 무시하는 크롤러 대비 이중 방어

**검증.** 로컬 서버에서 robots.txt 200 응답·전 페이지 meta 존재 확인 후 배포.

**한계(기록).** 이 조치는 "검색 결과에 안 뜨게" 하는 것이지 접근 차단이 아니다. 주소를 아는 사람은 여전히 받아갈 수 있다. 또한 robots.txt로 수집을 막으면 크롤러가 페이지 안의 noindex를 읽지 못하므로, 외부에 링크가 걸릴 경우 "내용 없는 URL만" 색인될 이론적 여지는 남는다 — 본 시스템은 외부 링크될 일이 없어 내용 유출 차단을 우선함.

---

### [2] 화면 확대(핀치 줌) 차단 해제 (위험도: 중간 — 현장 사용성)

**문제.** 뷰포트 메타에 `maximum-scale=1.0`이 걸려 있어 스마트폰에서 **손가락 확대가 불가능**했다. 전수 조사 결과 12px 미만 글자 선언이 **190곳**(최소 7.5px, 마스터 샘플 표 본문 11.5px·헤더 10px)으로, 확대 없이는 현장에서 읽기 어려운 수준.

**원인.** 초기 제작 시 iOS의 "입력창 포커스 시 자동 확대" 현상을 막으려고 확대 자체를 금지한 것. 접근성 관점에서 권장되지 않는 방식.

**조치.**

1. 12개 페이지 뷰포트를 `width=device-width, initial-scale=1.0`으로 변경 (확대 허용)
2. iOS 자동 확대의 원인이던 암호 입력창 글자 크기를 15px → **16px**로 상향 (iOS는 16px 미만 입력창 포커스 시 강제 확대함 — 사이트의 유일한 입력창)

**검증.** 브라우저에서 뷰포트 메타·입력창 계산 크기(16px) 확인, 인트로→암호→인덱스 전체 흐름 재검증.

**효과.** 현장 작업자가 이력 표·점검표를 자유롭게 확대해 볼 수 있음. iOS 자동 확대 부작용 없음.

---

### [3] 파비콘 추가 (위험도: 낮음)

**문제.** 파비콘이 없어 **모든 페이지 로드마다 `/favicon.ico` 요청이 404**로 떨어지고, 브라우저 탭·홈화면 추가 시 기본 아이콘이 표시됨.

**조치.** `favicon.svg` 신규 제작(380바이트) — 네이비 바탕 + 시안 QR 조준선, 진입 인트로와 동일 모티프. 12개 페이지에 `<link rel="icon">` 추가.

**효과.** 불필요한 404 제거, 탭·즐겨찾기에서 시스템 식별 가능.

---

### [4] 404 안내 페이지 추가 (위험도: 낮음)

**문제.** QR 라벨 손상·주소 오타로 없는 주소에 진입하면 GitHub 기본 404(영문)가 표시되어, 현장 사용자가 상황을 판단할 수 없었음.

**조치.** `404.html` 신규 제작 — 브랜드 톤(404 숫자를 QR 조준선 안에 배치), "QR코드가 손상되었을 수 있다"는 안내와 **전체 설비 인덱스로 이동** 버튼 제공. GitHub Pages는 루트의 404.html을 자동 사용.

**설계 판단(기록).** 이 페이지에는 암호 게이트를 걸지 않았다 — 사내 정보가 전혀 없고, 길을 잃은 사용자에게 암호를 묻는 것은 목적에 반함.

---

### [5] CSS 공용화 — 중복 제거 (위험도: 중간 — 유지보수성)

**문제.** 설비 페이지 10개가 **동일한 `<style>` 블록(10.6KB)을 각자 복사**해서 갖고 있었다 (페이지 용량의 39%). 디자인을 수정할 때마다 10개 파일을 일괄 수정해야 했고(수정 누락 시 페이지 간 디자인 불일치 위험), 사용자는 페이지를 옮길 때마다 같은 CSS를 다시 내려받았다.

**사전 검증.** 추출 전에 10개 파일의 스타일을 줄 단위로 전수 비교 — 완전 동일하며, 유일한 차이는 이력 표가 없는 6개 페이지에 `table.hist` 규칙(14줄)이 없는 것뿐임을 확인. 해당 규칙은 마크업이 없으면 무시되므로 공용 파일에 포함해도 무해.

**조치.** 공용 `styles.css`(11.2KB) 신규 생성, 10개 페이지의 `<style>` 블록을 `<link rel="stylesheet" href="styles.css">` 한 줄로 교체. 홈(index.html)과 PDF 뷰어는 레이아웃이 달라 각자의 스타일 유지.

**효과.**

| 항목 | 이전 | 이후 |
|---|---|---|
| 디자인 수정 범위 | HTML 10개 파일 일괄 수정 | `styles.css` 1개 파일 |
| 설비 페이지 용량 | 21.3~28.5KB | 11.7~17.9KB (평균 약 40%↓) |
| 페이지 간 이동 | 매번 CSS 재전송 | 최초 1회 후 캐시 재사용 |
| CSS 중복 총량 | 10.6KB × 10 = 106KB | 11.2KB × 1 |

**검증.** 리크테스터·일반 설비 페이지 각각에서 공용 CSS 로드(88개 규칙)·주요 계산 스타일(섹션 테두리·이력 표 글꼴·헤더 패딩)·렌더링 화면 비교로 동일함 확인.

---

### [6] 본문 내 구번호 참조 정정 (위험도: 낮음 — 내용 정확성)

**문제.** 기술문서 섹션 신설(2026-09-03) 때 섹션 번호가 한 칸씩 밀렸는데, 자동 재번호는 **섹션 제목만** 갱신하고 본문 문장 속 참조는 남겨 두었다. 리크테스터 3개 페이지의 검사 규격 표에 "리크 테스트 조건 — **상기 ③항 참고**"가 남아 있었고, 현재 ③은 기술문서이므로 잘못된 안내였다.

**발견 경위.** CSS 공용화 작업 중 파일 본문을 확인하다 발견 → 원 번호 참조 패턴으로 전수 검색하여 3건 전부 확인.

**조치.** 3건 모두 "상기 **④항(표준 작업 순서)** 참고"로 정정 — 번호와 함께 섹션명을 병기해 향후 번호가 또 밀려도 뜻이 통하도록 함.

**교훈(기록).** 섹션 번호를 바꾸는 작업은 제목뿐 아니라 **본문 속 참조까지 전수 검색**해야 한다. 검색 패턴: `[①-⑩]항`, `상기 [①-⑩]`.

---

## 운영 기준 신설 — PDF 업로드 압축 기준 (2026-09-03)

향후 첨부문서 26개 + 기술문서 20개 업로드에 대비해 실측 기반으로 기준을 정함. 상세는 `PROJECT_OVERVIEW.md` §6, 도구는 `tools/compress_pdf.py`.

**실측 근거** (저장소의 기존 파일 2개로 테스트):

| 파일 | 원본 | 150dpi/품질75 재압축 | 결과 |
|---|---|---|---|
| `machine-spec.pdf` (스캔 규격서 31p) | 4.82MB | **2.63MB (45%↓)** | 재압축 효과 큼 |
| `ls-r700-manual.pdf` (매뉴얼 142p) | 6.47MB | 9.16MB (오히려 42%↑) | 이미 최적화된 파일 — 재압축 금지 |

**기준.**

1. 업로드 전 파일당 **3MB 이하 목표, 5MB 초과 시 재압축 또는 분할 필수** (LTE 10Mbps 기준 3MB ≈ 2.4초)
2. 재압축은 `tools/compress_pdf.py`로 수행 — 150dpi/JPEG 품질 75. **결과가 원본보다 크면 도구가 자동으로 원본 유지 판정** (위 매뉴얼 사례처럼 파일마다 결과가 다르므로 일괄 적용 금지)
3. 재압축본은 업로드 전 스마트폰 화면에서 표·도면 글자 가독성 확인 후 교체

---

## 2026-09-16 GitHub 계정 소유권 이전 — 개인 계정 → 조직 계정

### 배경

별도로 진행 중이던 "e-AWD 모터 하우징 누설값 관리 시스템"(리크테스터 마스터 샘플 기록, 별도 저장소) 구축 과정에서, 구글시트 연동을 위해 새 구글 계정이 필요해졌다. 사용자가 "계정이 없다, 새로 만들어야 하나, 나중에 퇴사하면 또 새로 만들어야 하냐"고 문제를 제기 — **개인 명의 계정에 회사 시스템이 묶이는 구조 자체**를 짚은 것으로, 확인해보니 이 QR 정보관리 시스템도 동일한 문제를 안고 있었다: 저장소가 `dongnam959`(개인 계정) 소유였다.

### 문제

| 항목 | 문제 |
|---|---|
| 저장소 소유 | `dongnam959/dongnam959.github.io` — 개인 계정 1개에 전적으로 의존 |
| 담당자 퇴사 시 | 그 개인 계정 접근권이 없으면 저장소 관리(코드 수정·배포) 자체가 불가능해질 위험 |
| 구글시트 연동(신규 시스템) | 개인 Gmail 계정을 새로 만들면 동일한 문제가 처음부터 재현됨 |

### 조치

**GitHub — Organization으로 이전.** 개인 계정과 달리 조직(Organization)은 공동 관리자(owner)를 여러 명 등록할 수 있어, 특정 개인의 퇴사·계정 정지와 무관하게 운영 가능하다.

1. `dnk-mobility` 조직 신규 생성 (무료 플랜, `dongnam959` 개인 계정이 최초 owner)
2. `dongnam959/dongnam959.github.io` → `dnk-mobility` 조직으로 transfer (REST API `POST /repos/{owner}/{repo}/transfer`, `gh repo transfer` 서브커맨드는 존재하지 않아 `gh api` 직접 사용)
3. 조직 루트 도메인 특수 처리를 그대로 받기 위해 저장소명을 `dongnam959.github.io` → **`dnk-mobility.github.io`** 로 rename (`<계정/조직>.github.io` 형식이어야 루트 URL에 자동 배포되는 GitHub Pages 규칙 때문 — 이름을 안 바꾸면 `dnk-mobility.github.io/dongnam959.github.io/` 하위 경로가 되어버림)
4. 로컬 저장소의 `git remote` 주소를 새 저장소로 갱신
5. `tools/make_qr.py` 신규 작성, 11개 QR 전부 새 주소(`https://dnk-mobility.github.io/...`)로 재생성
6. `PROJECT_OVERVIEW.md`의 저장소·URL 참조 갱신

같은 원칙을 별도 저장소인 "e-AWD 모터 하우징 누설값 관리 시스템"에도 동일 적용 — 그쪽도 같은 `dnk-mobility` 조직으로 이전 (상세는 그 프로젝트의 `PROJECT_OVERVIEW.md` §9 참고).

**구글시트 연동 계정(신규 시스템)은 다른 해법을 택함.** 구글 조직 계정(Workspace)은 유료이고 회사 도메인 이메일도 없어 GitHub과 같은 방식을 쓸 수 없다. 대신: 부서명 의미의 신규 계정을 만들고, 복구 이메일만 "현재 담당자의 회사 개인 메일"로 걸어 둔다 — 담당자 교체 시 계정을 새로 만드는 대신 복구 이메일만 갱신하고 비밀번호를 인계한다.

### 검증

| 항목 | 결과 |
|---|---|
| 저장소 이전 | `gh api repos/dnk-mobility/dongnam959.github.io` 조회로 owner가 `dnk-mobility`로 바뀐 것 확인 |
| Rename 후 Pages | 별도 재설정 없이 `html_url`이 자동으로 `https://dnk-mobility.github.io/`로 갱신됨 (`status:"building"` → 완료 후 200 확인) |
| 홈 페이지 | 새 주소에서 인트로 → 암호 게이트 → 설비 카드 10개 정상 렌더링 |
| 설비 상세 페이지 | `leak-pre-full.html`(공정No.130) 접속 시 섹션 10개, 기술문서 잠금(`data-lock`) 정상 |
| 404 페이지 | 존재하지 않는 경로 접속 시 커스텀 404가 그대로 서빙됨 (`custom_404:true` 유지 확인) |
| QR 재생성 | 11개 전부 새 주소로 재생성. **라벨을 아직 현장에 부착하기 전이라 재출력 비용 0원** |

### 교훈 (기록)

- 사이드 프로젝트(구글시트 계정 문제)를 풀다가 **기존 시스템에 잠재해 있던 같은 종류의 리스크를 발견**했다. "이거 하나 해결하려고 보니 저것도 같은 문제였다"는 패턴 — 계정/소유권 구조는 시스템별로 따로 점검하지 말고 회사 전체 인프라 관점에서 한 번에 점검하는 편이 낫다.
- **물리적 라벨(QR)을 인쇄·부착하기 전이 계정/도메인 구조를 바꿀 수 있는 마지막 타이밍이다.** 이번엔 운 좋게 아직 안 붙어 있어 비용 없이 처리했지만, 늦게 발견했다면 라벨 재출력 비용이 발생했을 것 — 다음에 새 QR 시스템을 만들 때는 **QR을 인쇄하기 전에 계정 소유권 구조부터 먼저 확정**할 것.
- `gh repo transfer`라는 서브커맨드는 존재하지 않는다 (`gh repo --help`로 실제 지원 커맨드 확인 필요) — REST API(`gh api -X POST repos/{owner}/{repo}/transfer`)를 직접 써야 한다.

---

## 2026-09-17 디자인·기능·보안 정밀 재점검

배경: 사용자 요청으로 디자인·기능·접속보안 전반을 코드 레벨에서 재점검하고 최적화 진행. 문서 읽기에 그치지 않고 실제 브라우저(로컬 서버)로 1차/2차 게이트·아코디언·PDF 렌더링을 직접 조작해 검증했고, `gate.js`의 HASH_1/HASH_2도 문서상의 평문 암호를 직접 SHA-256 해시해 대조함. 관련 커밋: `d720f1b`(1~4번), 이후 커밋(5번).

### [1] JavaScript 비활성화 시 게이트 전면 우회 (위험도: 중간)

**문제.** 암호 게이트(`gate.js`)는 `document.documentElement.style.visibility = "hidden"`을 **JS로** 실행해서 화면을 가린다. JS 자체가 꺼져 있으면 이 코드가 아예 실행되지 않아, 12개 페이지 전체가 **암호 확인 없이 그대로 노출**된다. 기존에 알려진 한계("개발자도구로 우회 가능")보다 훨씬 쉬운 우회 경로였다.

**조치.** 게이트가 걸리는 12개 페이지(설비 10 + 홈 + PDF 뷰어)에 `<noscript>`를 추가 — JS가 꺼져 있을 때만 `body{visibility:hidden}`을 적용하고 "자바스크립트가 필요합니다" 안내를 표시.

**검증.** 브라우저에서 실제 1차/2차 게이트 흐름(암호 입력 → 오답 → 정답 → 잠금 섹션 동시 해제)을 끝까지 조작해 기존 동작에 변화가 없음을 확인.

---

### [2] `<span>` 안에 `<div>`를 넣은 무효 HTML 중첩 (위험도: 낮음 — 표준 준수)

**문제.** 첨부문서·기술문서 목록(`doc-item`)과 홈 화면 설비 카드(`eq-card`)의 제목/부제 래퍼가 `<span class="doc-text">`/`<span class="eq-text">`였는데, 그 안에 `<div>` 두 개를 담고 있었다. `span`은 phrasing content만 허용하므로 HTML5 스펙 위반 — 브라우저가 관대하게 렌더링해 눈에 띄지 않았을 뿐.

**조치.** 두 래퍼를 `<div>`로 교체 (11개 파일, 총 64곳). CSS는 클래스 선택자라 수정 불필요.

**검증.** 스크립트 처리 전후 `<span class="doc-text">`/`<span class="eq-text">` 잔존 0건 확인, 브라우저로 실제 렌더링 결과 시각적 차이 없음 확인.

---

### [3] `tools/compress_pdf.py`가 Windows에서 크래시 (위험도: 낮음 — 도구 신뢰성)

**문제.** 5MB 초과 경고 문구에 쓰인 `⚠` 문자를 Windows 콘솔 기본 코드페이지(cp949)가 인코딩하지 못해 `UnicodeEncodeError`로 스크립트가 죽었다. 여러 파일을 한 번에 넘기면 문제 파일 이후 나머지는 아예 처리되지 못하고 중단됨 — 실제로 `ls-r700-manual.pdf` 처리 중 죽어서 뒤의 `manual.pdf`/`partlist.pdf`가 처리되지 못하는 것을 재현.

**조치.** 스크립트 시작 부분에서 `sys.stdout`/`sys.stderr`를 UTF-8(`errors="replace"`)로 재설정.

**검증.** 4개 파일을 한 번에 넘겨 끝까지 정상 처리·경고 출력되는 것을 확인.

---

### [4] `machine-spec.pdf`가 압축 전 원본 그대로 커밋되어 있던 상태 (위험도: 낮음 — 용량)

**문제.** §6/§10 기록상 2026-09-03에 `machine-spec.pdf`를 4.82MB→2.63MB로 재압축했다고 되어 있었으나, 실제 저장소에 커밋된 파일은 여전히 압축 전 4.82MB 원본이었다. 압축본이 로컬에 만들어져 있었지만 교체·커밋이 누락된 것으로 보임.

**조치.** [3]의 수정된 도구로 재압축(동일하게 2.63MB, 45%↓) 후 페이지 수(31p) 일치·도면 이미지 픽셀 단위 비교로 화질 무손실 확인하고 원본과 교체.

**효과.** 해당 파일 실전송 용량 **45% 절감** (LTE 10Mbps 기준 약 3.9초 → 2.1초).

---

### [5] `ls-r700-manual.pdf` 5MB 상한 초과 상태 방치 (위험도: 낮음 — 용량, 사용자 확인 후 조치)

**문제.** §6 압축 기준상 "5MB 초과 시 재압축 또는 분할 필수"인데, `ls-r700-manual.pdf`(142p, 6.47MB)는 2026-09-03에 재압축이 역효과(9.16MB)임을 확인만 하고 분할은 미실시 상태로 남아 있었다.

**발견 경위 및 조치.** PDF 자체에 내장된 북마크(TOC)를 확인하니 **43페이지를 경계로 "설치매뉴얼"(1~42p)/"조작매뉴얼"(43~142p)**로 원저작자가 이미 구획해 둔 구조였다. 이 경계로 분할 후 각각 재압축:

| 파일 | 페이지 | 원본 | 재압축 |
|---|---|---|---|
| `ls-r700-manual-install.pdf` | 1~42 | 1.80MB | **1.74MB** |
| `ls-r700-manual-operation.pdf` | 43~142 | 4.53MB | **3.69MB** |

사용자에게 분할 방식(원본 목차 경계 기준)과 파일 구성을 확인받은 뒤 진행. `leak-pre-flow.html`(120)·`leak-pre-full.html`(130)·`leak-post-flow.html`(180) 3개 페이지의 "② 첨부 문서"에서 기존 "디텍터 매뉴얼" 1항목을 "디텍터 설치 매뉴얼"/"디텍터 조작 매뉴얼" 2항목으로 교체하고, 이제 아무 페이지도 참조하지 않는 원본 `ls-r700-manual.pdf`는 삭제.

**검증.** 분할 전후 페이지 수 합(42+100=142) 일치, 분할된 파일 각각 표지·목차 텍스트가 올바른 구간과 일치함을 렌더링 이미지로 확인.

**효과.** 두 파일 모두 5MB 상한 이내로 들어옴 (조작매뉴얼은 3MB 목표는 살짝 초과 — 원본 자체의 이미지 비중이 높아 이 이상 줄이면 도면 가독성이 떨어질 것으로 판단해 여기서 중단).

---

## 2026-09-17 설비 정보 도우미(챗봇형 안내 위젯) 신규 추가

배경: 사용자 요청으로 우측 하단 고정 챗봇형 안내 위젯을 신규 개발. 서버·API 없이 규칙 기반(키워드 매칭)으로 구현해 §13에 정리된 "빌드/백엔드 없는 정적 사이트, 유지비 0원" 원칙을 그대로 유지함. 상세 구조·데이터 위치는 `PROJECT_OVERVIEW.md` §16 참고.

### 기능

설비 검색(이름·공정No.·품번) → 페이지 이동, 페이지 안 섹션까지 자동 안내(다른 페이지면 `?goto=` 이동 후 이어서 처리), 자주 묻는 질문 답변, 설비별 공정 이론·상식 안내.

### [1] 발견한 버그 — 인증된 세션에서 페이지 이동 시 자동 섹션 펼침이 조용히 실패 (위험도: 낮음 — 기능성)

**문제.** 위젯의 "다른 페이지 섹션으로 안내" 기능(`?goto=<토픽>`)이 개발 중 테스트에서 간헐적으로 아무 동작도 하지 않았다 — 에러도 없이 그냥 섹션이 안 열림.

**원인 분석.** `gate.js`·`intro.js`·`assistant.js`는 전부 `<body>` 최상단의 동기 `<script src>`라서, 스크립트 자체의 실행(코드 등록)은 즉시 끝나지만 실제 로직은 Promise 체인을 통해 나중에 실행된다. **이미 같은 탭에서 1차 암호를 통과한 상태로 새 페이지로 이동하면**, `gate.js`의 인증 체크(`isOk(KEY_1)`)가 사용자 입력 없이 마이크로태스크만으로 즉시 풀린다. 이 경우 `window.dnkGate.ready`가 매우 빨리 — 브라우저가 `<main>`의 아코디언 HTML을 아직 파싱하기도 전에 — resolve될 수 있어, 그 시점에 실행되는 `assistant.js`의 섹션 찾기 로직(`document.querySelectorAll("details.section")`)이 빈 목록을 받아 조용히 실패했다. 반면 첫 방문처럼 실제 암호 입력이 필요한 경우는 사용자가 입력하는 동안 파싱이 이미 끝나 있어 문제가 드러나지 않았다 — 그래서 재현이 간헐적으로 보였다.

**조치.** `gate.js`가 `wireSections()`에 이미 쓰고 있는 것과 같은 `onReady()`(readyState 확인 후 필요하면 `DOMContentLoaded`까지 대기) 패턴을 `assistant.js`의 초기화에도 그대로 적용.

**검증.** 새 탭(첫 방문, 실제 암호 입력 필요)과 기존 인증된 탭(즉시 통과) 양쪽 경로 모두에서 크로스 페이지 섹션 이동이 정상 동작하는 것을 확인.

**교훈(기록).** 이번 프로젝트에서 "게이트 통과 후 실행"이 필요한 스크립트가 늘어날수록(`gate.js`→`pdf-viewer.html`→`assistant.js`) 이 레이스 컨디션이 반복될 여지가 있다. 앞으로 게이트 이후 동작하는 스크립트를 추가할 때는 항상 "DOM이 이미 다 파싱된 상태로 실행된다"고 가정하지 말고 `onReady()` 패턴을 기본으로 적용할 것.

### [2] 잠금 섹션 보호 확인 (회귀 없음)

위젯이 기술문서·마스터 샘플 같은 잠금 섹션으로 안내할 때 **강제로 펼치지 않고 스크롤만** 하는지, 사용자가 직접 눌렀을 때는 여전히 `gate.js`의 2차 암호 절차가 정상적으로 뜨는지 별도로 확인했다 — 위젯 추가로 인한 보안 회귀 없음. FAQ에서 "암호"를 물어도 실제 암호를 답하지 않고 문의 안내로만 응답하는 것도 함께 확인.

### [3] 실사용 중 발견 — 답변이 쌓여 채팅창이 계속 길어짐 (위험도: 낮음 — 사용성)

**문제.** 배포 후 사용자가 실제 모바일 화면에서 위젯을 써보다 발견: "공정 상식" → 설비 목록에서 하나를 눌러 답을 본 뒤, 답이 사라지지 않은 채로 그 아래에 새 답이 계속 쌓여 채팅창이 세로로 한없이 길어짐. 원인은 버튼을 눌러도 이전 메시지를 지우지 않고 `addMessage()`로 매번 새 메시지만 아래에 추가했기 때문.

**조치.** 메시지 영역을 비우는 `clearMessages()`를 추가하고, ① 액션 버튼 클릭 시, ② 텍스트를 입력해 질문할 때 각각 새 응답을 추가하기 **전에** 먼저 호출하도록 변경 — 항상 "가장 최근 질문/답변 1건"만 화면에 남는다. 이 변경을 `addMessage()`가 버튼에 클릭 리스너를 붙이는 지점 한 곳에만 적용해서, 위젯 어디서 만들어진 버튼이든 전부 동일하게 동작함.

**부가 조치.** 그런데 이론·FAQ처럼 더 이어질 액션이 없는 "막다른" 답변은 지우고 나면 아무 버튼도 안 남아 대화를 이어가려면 다시 타이핑해야 하는 문제가 생김 — 그런 답변 끝에도 처음 인사말과 같은 추천 버튼 4개(설비 목록·점검표 보기·자주 묻는 질문·공정 상식)를 함께 붙여서, 타이핑 없이 다음 질문으로 바로 넘어갈 수 있게 함.

**검증.** "공정 상식→레이저→공정 상식→리크" 순서로 눌러도 화면에 메시지가 항상 1개만 남는 것, FAQ·설비 카드 흐름도 동일하게 동작하는 것, 텍스트로 직접 물었을 때는 "질문+답변" 2개로 고정되고 더 쌓이지 않는 것을 확인.

---

## 2026-09-17 지속관리 체계 점검 — 전반 재점검 + 재발방지 체계 신설

배경: 시스템을 앞으로 계속 유지관리해야 하는 단계에 접어들어, 그동안 쌓인 문제들을 "그때그때 고치고 끝"내지 않고 **재발방지 체크리스트**로 압축해 다음 작업에 실제로 참고되도록 구조화. 상세는 `PROJECT_OVERVIEW.md` §17.

### 점검 방법

- HTML 구조 전수 확인: `<script>` 태그·`<noscript>`·`data-lock` 개수를 페이지 유형별로 grep해 전 11개 페이지가 설계대로 일관되는지 확인(설비 8종 페이지 4개 요소, 리크테스터 4종 페이지 5개 요소, 홈 4개, PDF 뷰어 2개, 404는 0개 — 전부 일치)
- `gate.js`의 `HASH_1`/`HASH_2`를 실제 암호(`dnkm8721`, `dnkm8721*`)로 재계산해 대조
- 전체 PDF 5종 용량 재확인 (모두 5MB 이내 유지 중)
- `assistant.js` 전체 재독해 — 로직 재검토
- `console.log`/`debugger`/`TODO`/`FIXME` 등 디버그 흔적이 남아있는지 전 `.js` 파일 검색

### [1] `assistant.js`의 "왜" 트리거가 실제로는 전혀 매칭되지 않음 (위험도: 낮음 — 기능성)

**문제.** 공정 상식(이론) 응답을 유도하는 정규식에 `왜\b`(단어 경계)가 들어 있었는데, 실제로는 "다월핀은 왜 압입하나요" 같은 자연스러운 한글 문장에서 단 한 번도 매칭되지 않았다.

**원인.** JS 정규식의 `\b`는 **ASCII `\w`(`[a-zA-Z0-9_]`) 기준**으로 단어 경계를 판단한다. 한글 문자는 `\w`가 아니므로, 한글 뒤에 오는 문자(공백·다른 한글·문장부호 등)도 전부 `\w`가 아니라서 "한쪽만 `\w`"라는 경계 조건이 성립하지 않는다. 즉 **한글 문자열에서 `\b`는 사실상 항상 실패한다.**

**조치.** `왜\b`를 그냥 `왜`로 변경(다른 키워드들과 동일하게 부분일치로 통일). 함께 있던 `무슨\s*원리`/`어떤\s*원리`도 이미 `원리` 하나로 커버되는 중복이라 정리.

**검증.** "다월핀은 왜 압입하나요"로 물었을 때 다월핀 압입 이론 설명이 정상적으로 나오는 것을 로컬에서 확인.

**교훈(기록).** **한글이 포함된 문자열에는 JS 정규식의 `\b`를 쓰지 말 것.** 정말 "단어 단위"로 끊어야 한다면 공백이나 구두점을 직접 명시하는 패턴을 쓰거나, 그냥 부분일치(`indexOf`)로 처리하는 편이 이 프로젝트의 나머지 매칭 로직과도 일관된다. §17 체크리스트에 반영.

### 이상 없음으로 재확인된 항목

| 항목 | 결과 |
|---|---|
| 게이트 해시 | `HASH_1`/`HASH_2`가 실제 암호(`dnkm8721`/`dnkm8721*`)의 SHA-256과 정확히 일치 |
| 페이지 구조 일관성 | 11개 페이지의 스크립트 태그·noscript·잠금 개수가 설계와 전부 일치 |
| PDF 용량 | 5개 파일 모두 5MB 상한 이내 (1.74/3.69/2.63/0.97/0.13MB) |
| 디버그 흔적 | `.js` 파일 전체에 `console.log`/`debugger`/`TODO` 등 잔존 없음 (gate.js 주석 속 예시 명령어 1건 제외, 실제 코드 아님) |
| XSS | 위젯이 사용자 입력을 표시할 때 전부 `textContent` 사용, `innerHTML`에 사용자 값을 넣는 곳 없음 |

### 재발방지 체계화

이번 점검부터, 개별 문제 기록(이 파일)과는 별도로 `PROJECT_OVERVIEW.md` §17에 **"다음 작업 전에 확인할 것"** 형태의 체크리스트를 신설했다. 기존에는 문제가 생길 때마다 이 파일에 상세히 기록은 했지만, 그 교훈이 실제 "다음 작업 시작 전 확인 항목"으로 압축·재사용되는 장치가 없었다. 앞으로는:

1. 문제 발생 → 이 파일(`개선이력.md`)에 문제·원인·조치·검증을 상세 기록 (기존과 동일)
2. 재발 방지에 도움이 되는 교훈이면 → `PROJECT_OVERVIEW.md` §17 체크리스트에 한 줄 추가 (신규)
3. 새 작업을 시작하기 전에는 §17을 먼저 훑어보는 것을 기본 절차로 함

---

## 2026-09-17 모바일 재검증 — 안내 위젯 추가 버그 3건

배경: 위 점검 직후 사용자 요청으로 실제 모바일 화면(375×812)에서 안내 위젯을 다시 꼼꼼히 확인하다가, 자동화 테스트만으로는 드러나지 않던 실사용 시나리오 버그 3건을 추가로 발견.

### [1] 이론 질문에 직접 타이핑으로 답했을 때만 "돌아갈 버튼"이 없음 (위험도: 낮음 — 사용성)

**문제.** "다월핀은 왜 압입하나요"처럼 **타이핑으로 설비+이론 질문을 동시에** 하면, 답변에 "OO 페이지 열기" 버튼 하나만 붙고 추천 버튼(설비 목록 등)이 없어 대화를 이어가려면 다시 타이핑해야 했다. 같은 이론 답변이라도 "공정 상식" 메뉴를 거쳐 들어간 경우에는(2026-09-17 앞선 항목에서 이미 수정) 추천 버튼이 붙어 있어 경로에 따라 동작이 달랐다.

**원인.** `buildReply()`에서 설비+이론 키워드가 동시에 매칭되는 경로 하나만 `quickChips()`를 안 붙이고 지나감 — 앞서 다른 4곳(FAQ 직접 응답, FAQ 목록 클릭, 이론 목록 클릭, 설비 카드의 "공정 상식 보기")은 전부 고쳤는데 이 경로 하나를 놓쳤음.

**조치.** 해당 경로의 `actions`에도 `.concat(quickChips())`로 추천 버튼을 추가.

**교훈(기록).** 같은 종류의 수정(여기서는 "막다른 답변에 버튼 붙이기")을 여러 곳에 적용할 때는 수정 후 **그 패턴이 나타나는 모든 지점을 grep으로 다시 훑어 빠진 곳이 없는지 확인**해야 한다. 2026-09-17 앞선 수정 때 놓친 지점이 이번에 실사용 중 발견됨.

### [2] 같은 페이지 섹션으로 이동하는 버튼을 누르면 채팅창이 빈 화면이 됨 (위험도: 중간 — 사용성)

**문제.** "점검표 보기" 등으로 안내받은 뒤 "여기서 바로 보기 →" 버튼(같은 페이지 안 섹션으로 이동하는 경우)을 누르면, 실제로는 섹션이 정상적으로 펼쳐지는데도 **채팅창에는 아무 메시지도 남지 않고 텅 비어** 보였다.

**원인.** 버튼 클릭 시 공통으로 이전 메시지를 지우는 `clearMessages()`가 먼저 실행된 뒤 `gotoSection()`이 호출되는데, `gotoSection()`은 같은 페이지인 경우 `openOrScrollSection()`으로 DOM만 조작할 뿐 **새 메시지를 추가하지 않았다.** 다른 페이지로 이동하는 경우는 `location.href` 이동으로 페이지 자체가 새로고침되니 문제가 안 보였고, 같은 페이지인 경우에만 "지우기만 하고 아무것도 안 채우는" 상태가 드러났다.

**조치.** `gotoSection()`의 같은 페이지 분기에 결과 확인 메시지("OO 항목으로 이동했어요" 등, 잠금 항목이면 안내 문구 추가)와 `quickChips()`를 추가.

**검증.** 리크테스터 페이지(190)에서 "점검표 보기" → "여기서 바로 보기" 클릭 시 확인 메시지가 뜨고 실제 섹션이 열리는 것, 잠금 항목(마스터 샘플)에서는 "잠금 항목이라 암호 확인이 필요"라는 문구와 함께 섹션은 그대로 잠겨 있는 것을 확인.

**교훈(기록).** "메시지를 지우고 다시 채운다"는 패턴을 넣을 때는, **지운 뒤 반드시 뭔가를 채우는 경로인지** 모든 호출 지점에서 확인해야 한다. DOM 조작만 하고 끝나는 함수(`openOrScrollSection`)를 액션 핸들러로 직접 연결하면 "지우기만 하고 안 채우는" 빈 화면이 생기기 쉽다.

### [3] 위젯 버튼이 페이지 맨 아래 푸터 문구를 가림 (위험도: 낮음 — 사용성)

**문제.** 짧은 페이지든 긴 페이지든, 화면을 맨 아래까지 스크롤하면 우측 하단 위젯 버튼이 푸터의 마지막 줄("문의는 후공정 생산기술팀으로 부탁드립니다")과 겹쳐 글자가 가려졌다. 자동화 테스트(요소 존재 여부만 확인)로는 안 걸러지고, 실제 화면을 눈으로 봐야 드러나는 종류의 문제였다.

**원인.** 위젯 버튼이 `position:fixed`로 항상 우측 하단 고정 위치에 있는데, 정작 페이지의 `footer`는 버튼이 차지하는 공간을 고려하지 않고 그 자리까지 콘텐츠를 채웠음.

**조치.** `assistant.js`가 주입하는 스타일에 `footer{padding-bottom:calc(80px + env(safe-area-inset-bottom,0px))!important;}`를 추가 — 위젯이 로드되는 모든 페이지의 푸터 하단에 버튼 높이만큼 여백을 항상 확보.

**검증.** 8단계 페이지(200)와 10단계 페이지(190) 양쪽에서 맨 아래로 스크롤한 스크린샷으로 푸터 전체 문구가 버튼에 가리지 않고 보이는 것을 확인.

**교훈(기록).** `position:fixed` 오버레이 요소(플로팅 버튼·배너 등)를 페이지에 추가할 때는, **그 요소가 항상 차지하는 화면 영역만큼 페이지 콘텐츠 쪽에도 여백을 확보**해야 한다. 요소 자체의 존재·클릭 가능 여부만 테스트해서는 이런 "겹침" 문제가 드러나지 않고, 실제 화면을 스크롤해서 눈으로 확인해야 한다. §17 체크리스트에 반영.

---

## 2026-09-17 설비 부착용 QR 라벨 제작 (현장 적용 준비)

배경: QR 시스템을 실제 현장에 적용하기 위해 설비에 붙일 라벨이 필요해짐. 시안 5개를 만들어 비교한 뒤 확정하고, 설비 10개소 전체 인쇄 파일까지 제작. 규격·디자인 상세는 `PROJECT_OVERVIEW.md` §18.

### 진행 방식

1. 디자인 시안 5개(미니멀 / 네이비 풀블리드 / 배지·씰 / 라벨 스트립 / 프레임형)를 같은 조건으로 만들어 비교
2. 사용자가 **네이비 풀블리드 기반 + 회사명(1안) + SCAN 문구(5안)** 조합으로 확정
3. 확정안으로 10개소 전체 생성 → A4 3장(한 장당 4개) 인쇄 PDF로 조판
4. 재현 가능하도록 `tools/make_labels.py`로 스크립트화 (설비명·주소 변경 시 재실행만 하면 됨)

### 검증

| 항목 | 방법 | 결과 |
|---|---|---|
| 실제 인쇄 치수 | PDF 좌표에서 배치된 이미지 크기 직접 측정 | 10개 전부 정확히 100.0 × 100.0mm (A4 210×297mm) |
| QR 연결 주소 | 10개 라벨의 QR을 **전부 디코딩**해 설비별 기대 주소와 대조 | 10개 모두 일치, 중복·누락 없음 |
| 긴 설비명 | 가장 긴 140번("오일니쁠 압입기 / 테이퍼 플러그 체결기(1개소)") 렌더링 확인 | 잘림 없이 한 줄에 수용 |
| 레이아웃 | 라벨별 본문 높이를 계산해 100mm 초과 여부 자동 검사 | 전부 87.5mm 사용(여유 12.5mm) |

### [1] 한글 폰트 임베드로 PDF가 131MB까지 부풀었던 문제 (위험도: 낮음 — 파일 크기)

**문제.** 초기 인쇄 PDF 생성 시 파일 크기가 **131MB**로 나옴. 라벨 이미지는 수백 KB 수준인데 비정상적으로 큼.

**원인.** PyMuPDF에서 `insert_text(..., fontfile=...)`로 한글 폰트를 지정하면 **호출할 때마다 폰트 파일 전체를 새로 임베드**한다. 맑은 고딕은 파일 하나가 13MB(굵게 12MB)여서, 페이지마다 머리말·꼬리말을 넣자 폰트가 중복 임베드되며 100MB를 넘겼다.

**조치.** 저장 직전에 `doc.subset_fonts()`를 호출해 실제 사용된 글자만 남기고 병합하도록 변경. `garbage=4, deflate=True`도 함께 적용. → **131MB → 530KB** (약 250분의 1).

**교훈(기록).** PDF에 한글(CJK) 폰트를 임베드할 때는 **반드시 `subset_fonts()`로 서브셋 처리**할 것. 화면에서는 전혀 티가 나지 않아 파일을 열어보는 것만으로는 발견되지 않고, 인쇄소 전달이나 메일 첨부 단계에서야 문제가 된다. §17 체크리스트에 반영.

### [2] 기본 PDF 폰트로는 한글이 전부 깨지던 문제 (위험도: 중간 — 출력물 오류)

**문제.** PDF에 넣은 조립 안내문·라벨 캡션의 한글이 전부 `?`로 출력됨.

**원인.** PyMuPDF 기본 폰트(`helv` 등 PDF base14)는 **라틴 문자 전용**이라 한글 글리프가 없다.

**조치.** 모든 한글 텍스트 삽입에 `fontname`/`fontfile`로 맑은 고딕을 지정. 라벨 본문 자체는 PIL로 그려 이미지에 구워지므로 영향 없었고, PDF에 직접 넣는 머리말·꼬리말만 해당됐다.

**교훈(기록).** PDF 생성 라이브러리의 기본 폰트는 한글을 지원하지 않는다고 전제할 것. 생성 직후 **렌더링해서 눈으로 확인**하지 않으면 그대로 인쇄까지 갈 수 있다.

---

## 2026-09-17 진입 인트로를 설비 10개 페이지로 확장

배경: 라벨을 실제로 붙이기 시작하면서, 홈을 거치지 않고 **설비 QR을 직접 찍어 들어가는 게 실제 운영 방식**임이 분명해짐. 그런데 진입 인트로(`intro.js`)는 2026-09-03 도입 당시 "매번 나오면 현장에서 방해된다"는 이유로 **홈(`index.html`) 1개 페이지에만** 넣어뒀던 상태였다 — 그래서 설비 QR로 바로 들어가면 인트로 없이 곧장 암호창부터 떴다.

**문제.** 사용자가 직접 설비 QR로 들어가 보고 "홈은 애니메이션이 되는데 설비 페이지는 안 된다"고 지적. 확인해보니 실제로 `intro.js`가 설비 페이지 10개에는 로드조차 되지 않고 있었다 — 버그라기보다는, **QR 운영 방식이 바뀌었는데 인트로 적용 범위는 예전 가정(홈이 유일한 진입점) 그대로 남아있던 것**이었다.

**조치.** 설비 페이지 10개 전부에 `<script src="gate.js">` 앞에 `<script src="intro.js">`를 추가 — `index.html`과 완전히 같은 순서(`intro.js` → `gate.js` → `assistant.js`). 코드 변경은 `intro.js`/`gate.js` 자체에는 없음 — 두 스크립트는 처음부터 "여러 페이지에서 로드돼도 세션당 1회만 재생"되도록 설계돼 있었기 때문에, 로드하는 페이지만 늘리면 됐다.

**검증.** sessionStorage를 비운 뒤 설비 페이지(200번)로 직접 들어가 인트로가 재생되는 것을 스크린샷으로 확인 → 통과 후 같은 탭에서 다른 설비 페이지(150번)로 이동했을 때는 인트로가 재생되지 않고 곧바로 콘텐츠가 보이는 것 확인 (세션당 1회 제한이 여러 진입점에도 올바르게 적용됨).

---

## 2026-09-17 설비 부착용 QR 라벨 크기 재조정 (100×100mm → 70×100mm)

배경: 확정된 100×100mm 정사각형 라벨을 실제 인쇄해본 뒤, 현장 부착 여건상 가로 폭을 줄여야 한다는 요청. 사용자가 직접 방법까지 제안함 — "기존 QR코드와 글자는 그대로 유지하고 좌우 여백에서 줄이면 되지 않을까?" → 내용을 다시 디자인하지 않고 **캔버스 폭만 축소**하는 방식으로 채택.

### 진행 방식

`tools/make_labels.py`의 `LABEL_MM`(정사각형 한 변) 상수를 `WIDTH_MM=70` / `HEIGHT_MM=100`으로 분리. QR 크기(44mm)·글꼴 크기·상하 간격 등 `render_label()` 내부 수치는 전부 그대로 두고, 캔버스 폭(`WIDTH_PX`)만 좁혀서 렌더링. PDF 조판(`build_pdf()`)의 `CELL`도 `CELL_W`/`CELL_H`로 분리해 A4 그리드 계산에 반영. 출력 파일명도 `설비QR라벨_70x100mm_A4인쇄용.pdf`로 변경.

### 검증

| 항목 | 방법 | 결과 |
|---|---|---|
| 실제 인쇄 치수 | PDF 좌표에서 배치된 이미지 크기 직접 측정 | 10개 전부 정확히 70.000 × 100.000mm |
| QR 연결 주소 | 10개 라벨의 QR을 전부 디코딩해 설비별 기대 주소와 대조 | 10개 모두 일치, 중복·누락 없음 |
| 긴 설비명 | 가장 긴 140번("오일니쁠 압입기 / 테이퍼 플러그 체결기(1개소)") 렌더링 확인 | 폭이 좁아졌음에도 줄바꿈 추가 없이 한 줄에 수용 |
| 레이아웃 | 라벨별 본문 높이 자동 검사 | 10개 전부 87.5mm/100mm — 100×100mm 원안과 완전히 동일(높이는 안 바뀌었으므로 당연한 결과, 폭 축소가 세로 레이아웃에 영향 없음을 재확인) |

**교훈(기록).** 라벨처럼 "QR+텍스트가 이미 확정된 디자인"에서 한 변만 바꿔야 할 때, 내부 요소 크기를 전부 다시 계산하기 전에 **먼저 여백만 줄여서 되는지부터 확인**하는 편이 안전하다 — 재작업 범위가 훨씬 작고, 이미 검증된 폰트 크기·QR 크기를 건드리지 않아 회귀 위험이 없다. 이번 경우 실제로 폭을 30mm 줄여도 어떤 텍스트도 줄바꿈이 추가되지 않아, 처음부터 비율을 다시 설계할 필요가 없었다.

**교훈(기록).** "이 기능은 진입점이 하나일 때만 쓰인다"는 가정으로 기능 범위를 좁혀두면, **나중에 진입점이 여러 개로 늘어났을 때 그 가정을 다시 점검하는 절차가 없으면 조용히 방치된다.** 이번 경우는 세션당 1회 제한을 이미 잘 설계해둔 덕분에 범위만 넓히면 됐지만, 운영 방식이 바뀔 때(QR 배포 범위, 진입 경로 등) 그 변화가 이전에 "당연하다"고 가정했던 다른 기능에 영향을 주지 않는지 같이 점검할 것. `인트로_암호게이트_구현상세.md`(다른 시스템 이식용 문서)에도 이 경험을 반영해 "진입점 1개만 권장"이라던 조언을 "QR이 여러 페이지로 직접 연결되면 전부에 넣을 것"으로 고쳤다.

---

## 2026-09-18 마스터 샘플·보정 이력 섹션 삭제 및 누설값관리시스템 이관

배경: 리크테스터 4개소(120·130·180·190)에 있던 "⑨ 마스터 샘플·보정 이력" 섹션은 애초에 2026-08-31 임시로 만든 정적 표였다 — 사용자가 직접 물어본 것을 계기로, 이 데이터(디텍터 보정값·마스터 샘플 측정치)가 이 시스템의 성격과 맞는지 다시 점검했다.

**판단.** 이 QR 시스템은 백엔드 없이 정적 파일만으로 운영되는 **읽기 전용 참고 문서 시스템**이다. 반면 마스터 샘플·보정 이력은 현장에서 **계속 쌓이는 측정 데이터**로, 원래도 실제 입력 수단이 없어 HTML을 사람이 직접 고쳐야만 갱신되는 상태였다(공정No.130만 2026-09-02에 수동으로 2행 입력, 나머지 3개소는 전부 빈 표로 방치됨 — 애초에 "운영 가능한 기능"이 아니었다는 방증). 마침 같은 라인에 이런 데이터를 다루려고 개발 중인 **누설값관리시스템**이 있어, 데이터의 원천(source of truth)을 그쪽 하나로 모으기로 결정.

**조치.**
1. 리크테스터 4개 설비 페이지에서 "⑨ 마스터 샘플·보정 이력" `<details data-lock="2">` 섹션 전체 삭제, 이후 섹션("최근 공지") 번호를 ⑩→⑨로 조정
2. 이제 쓰이지 않는 CSS(`table.hist`, `.table-scroll`) 규칙을 `styles.css`에서 제거
3. `assistant.js`의 `SECTION_TOPICS`에서 `history` 토픽 제거, 관련 필터·주석 정리(`gate.js` 주석 포함)
4. **기존에 실제 입력돼 있던 데이터(공정No.130 2건)를 포함한 전체 데이터 구조·안내 문구·접근 권한 요건을 [`인계_마스터샘플_보정이력.md`](인계_마스터샘플_보정이력.md)에 문서화** — 누설값관리시스템 쪽에서 이 문서를 보고 동일한 기능을 재구현하면 데이터 손실 없이 이관 가능

**검증.** 4개 파일 전부 `<details>`/`</details>` 개수 일치(9쌍) 확인, 삭제 후 "⑩ 최근 공지"가 "⑨"로 정상 renumber됐는지 grep으로 4개 파일 전수 확인. `table.hist`·`.table-scroll`이 삭제 대상 4개 파일 외에는 쓰이지 않는 것을 사전에 grep으로 확인한 뒤 CSS 삭제(다른 페이지에 영향 없음).

**교훈(기록).** 급하게 만든 기능("일단 표만 만들어서 나중에 채우자")이 실제로는 아무도 채울 수 없는 구조였다면, 오래 방치하지 말고 **그 데이터를 실제로 다룰 수 있는 시스템으로 넘기는 게 맞다**는 판단을 내렸다. 이관할 때는 단순히 "삭제"만 하지 않고, 데이터 구조·기존 실데이터·잠금 등급 같은 요건을 받는 쪽이 그대로 재현할 수 있게 문서로 남기는 절차(`인계_*.md`)를 이번에 처음 도입했다 — 앞으로 다른 기능을 다른 시스템으로 옮길 일이 생기면 같은 방식을 따를 것.

---

## 2026-09-18 설비 8개소 트러블슈팅 이력 대량 보강

배경: "⑦/⑧ 트러블슈팅 이력" 섹션이 10개소 중 4개소(리크테스터)에만 채워져 있고 나머지 6개소는 "등록된 사례 없음" 상태로 오래 방치되어 있었다. 사용자가 사내 카카오톡 단톡방("X전동화(70,250kW) 단톡방", 2025-11-05~2026-09-18, 약 21,800줄) 대화 export 파일을 제공하며 각 설비에 넣을 만한 트러블슈팅 항목을 검토해달라고 요청.

### 1단계 — 대화 로그 검토

카카오톡 export가 1.2MB(21,805줄)로 Read 도구의 파일 크기 한도를 넘어 grep으로 설비별 키워드(유로계·전체계·니쁠·씰캡·테이퍼·다월핀·마킹 등 + NG·불량·이상·알람·고장 등 문제 키워드)를 조합 검색한 뒤, 매칭된 지점마다 앞뒤 맥락을 `Read`로 넓게 읽어 실제 원인·조치가 기록된 진짜 사건과 단순 수량 집계(일일 실적 보고)를 구분했다.

**중요한 필터링 기준**: 대화방이 70kW·250kW 두 라인을 함께 다루는데, **이 QR 시스템은 70kW 라인 전용**이다. "250kW 모터 하우징" 태그가 붙은 사건(예: 250kW 다월핀 압입 리크 불량)은 겉보기엔 같은 공정이라도 **물리적으로 다른 설비**이므로 전부 제외했다.

검토 결과를 먼저 설비별로 정리해 사용자에게 보고하고(원인/조치가 명확한 것은 "추천", 근거가 약한 것은 "보류 권장"으로 구분), 사용자가 채택 항목을 최종 선별했다.

### 2단계 — 사용자 제공 자료 반영

사용자가 추가로 제공한 자료:
- 레이저 마킹(9.1·9.2), 씰캡 압입(3.1·3.2), 테이퍼 플러그 체결(4.1~4.5, 1-1·1-2) — 설비 이상 이력표 스크린샷
- 다월핀 압입기(2-3) — 이력표 스크린샷 + 사내 공유폴더의 `다월핀 설비고장관련 수리 후 검증분석_260724.pptx`(작업자 조작 실수로 인한 서보프레스-공급장치 충돌 및 CMM 검증 보고서)

PPTX는 `markitdown`으로 먼저 추출했으나 한글이 전부 깨져(U+FFFD 치환문자) 나왔다. `zipfile`로 직접 열어 `ppt/slides/slide1.xml`을 UTF-8로 읽고 `<a:t>` 텍스트 노드를 정규식으로 추출하니 정상적으로 읽혔다 — **markitdown의 pptx 추출기가 이 파일의 한글 텍스트 런을 제대로 디코딩하지 못하는 케이스**로 확인, 원본 XML 직접 파싱으로 우회.

### 3단계 — 반영

사용자의 항목별 지시에 따라 8개 설비 페이지에 사례 추가:

| 설비 | 추가 사례 | 출처 |
|---|---|---|
| 레이저 마킹기(110) | 2건 (인터페이스 보드 교체, PLC 로직 수정) | 사용자 제공 이력표 |
| 조립전 전체계 리크검사기(130) | 1건 (장착기준홀 직각도 문제) | 대화 로그 |
| 오일니쁠 압입기(140) | 2건 (컬러센서 오판정, 경화제 재고) — 사용자가 "워크서포트부 알람" 항목은 제외 지시 | 대화 로그 |
| 씰캡 압입기(150) | 2건 (압입력 초과, 록타이트 도포불량) | 사용자 제공 이력표 |
| 테이퍼 플러그 체결기(160) | 7건 | 사용자 제공 이력표 |
| 다월핀 압입기(170) | 2건 (서보프레스 충돌, PLC 피드백 구조개선) | 사용자 제공 이력표 + PPTX |
| 조립전 유로계·조립후 유로계·조립후 전체계(120·180·190) | 신규 사례 없음(기존 사례로 충분 판단), 공통 참고문구만 추가 | — |

모든 리크테스터 4개소(120·130·180·190)에는 "항온룸 온도 관리 미흡 시 설비 문제로 오판하기 쉽다"는 공통 참고문구를 추가했다(대화 로그 6/26 기록 근거).

신규 태그로 `.tag.warn`(주황색, 기존 `--warn-bg`/`--warn-text` 재사용)을 `styles.css`에 추가 — 기존 "설비 정상"류 녹색 태그와 구분해 **실제 설비 이상·작업자 부주의** 사례를 표시.

### 검증

- 수정한 9개 파일 전부 `<div>`/`</div>`, `<details>`/`</details>` 개수 일치 확인
- 로컬 서버 + 브라우저로 taper-plug-fastening.html(사례 7건), dowel-pin-press.html(사례 2건), leak-pre-flow.html(공통 참고문구) 등 대표 페이지를 실제로 펼쳐 렌더링 확인, 콘솔 에러 없음 확인

**교훈(기록).** 스크린샷으로 전달된 표는 열 구분이 텍스트로 재구성되면서 모호해지는 경우가 있다(원인 칸과 조치 칸이 바뀌어 보이는 등). 이번에는 문장의 의미(동사가 "조정/교체"면 조치, "불일치/불량"이면 원인)로 최대한 정확히 구분했지만, **사내 실무자 검증 없이 그대로 신뢰하면 안 된다** — 특히 경영진도 보는 문서이므로, 이런 2차 가공 데이터를 반영할 때는 반드시 원본 작성자에게 한 번 더 확인받는 절차를 권장. PPTX 텍스트가 깨지는 문제는 markitdown 한 가지 도구만 믿지 말고, 이상하면 원본 XML을 직접 열어 대조하는 습관이 필요하다는 것도 재확인.

---

## 2026-09-18 트러블슈팅 이력 2차 보강 — 엑셀 원본 대조로 스크린샷 오독 정정

배경: 위 1차 작업(스크린샷 기반)을 마친 직후, 사용자가 그 스크린샷들의 원본인 `e-AWD 70kW M-HSG 후공정 설비 OPEN ISSUE.xlsx`(시트 1~3, 원본 열 구조가 살아있는 표)를 제공. 바로 위 항목에서 스스로 남긴 "실무자 검증 필요" 우려가 실제로 맞아떨어진 사례였다.

### 대조 결과 — 확인된 오독 6건

| 설비 | 오독 내용 | 정정 |
|---|---|---|
| 씰캡 압입기 사례1 | 원인/조치 칸을 반대로 읽음 | 원인=스핀들 캐치 안착 불안정(유격), 조치=가이드 무두볼트 조임 |
| 오일니쁠압입기 사례2 | 카톡 로그의 "경화제 부족" 건과 혼동해서 엉뚱한 내용을 씀 | 정확히는 "경화촉진제 파이프 에어누설·작동불가", 게다가 **아직 미해결(검토중)** 상태였는데 "완료"처럼 기재했었음 |
| 다월핀압입기 사례2 | 스크린샷 글자가 뭉개져 "클램프"/"팔레거이지"로 잘못 읽음 | 정확히는 "콜렛"/"필러게이지" |
| 테이퍼플러그체결기 사례3 | 방향전환을 "역→정" 2단계로 단순화 | 실제는 "역→정→역" 3단계 |
| 조립전 전체계 리크검사기 사례2 | 6/18 기준핀 교체로 "해결됨"처럼 기재 | 7/30 자료를 보니 **근본원인(인버터부 마스킹 시 제품 밀림)은 아직 검토중** — 기준핀 교체는 임시조치였을 뿐 |

### 신규 확인된 사례 (스크린샷에는 없었던 항목)

엑셀 시트 1~3에는 스크린샷으로 전달되지 않은 이슈도 있었다: 조립전 유로계·전체계 공통(소음기 오일누수, 간헐적 HMI 데이터초기화), 오일니쁠압입기(NUT 풀림, 테이퍼플러그 공급불량 재발), 다월핀압입기(공급 불량·호스 구부러짐), 조립전 유로계(안착 문제·티칭세팅), 조립후 전체계(상부 실린더 하강 증상). 사용자 확인 후 다월핀압입기의 "2-1"(하부·후면 슈팅 에러, 상태=진행중·미해결) 1건만 제외하고 전부 반영.

**조치.** 6개 기존 사례 정정 + 7개 신규 사례 추가(120·130·140·170·190 페이지). 아직 미해결로 확인된 항목(오일니쁠압입기 파이프 누설, 조립전 전체계 기준홀 문제)은 태그를 기존 "설비 이상"이 아닌 새로 만든 <code>진행중</code> 태그로 표시해 **완료된 것처럼 보이지 않도록** 구분.

**검증.** 수정 9개 파일 전부 div/details 태그 균형 확인, 로컬 서버로 130·140 페이지 렌더링 재확인(신규 반영 내용 텍스트 그대로 노출, 콘솔 에러 없음).

**교훈(기록).** 스크린샷(이미지)으로 전달된 표는 아무리 신중하게 읽어도 열 순서를 잘못 짚거나 글자를 오독할 위험이 실제로 있다 — 이번에 6건이나 확인됐다. **가능하면 원본 파일(엑셀 등)을 직접 요청해서 대조하는 편이 스크린샷 재해석보다 훨씬 안전하다.** 또한 "완료"로 표시된 이슈도 후속 자료(다른 시트, 다른 날짜)를 보면 사실은 미해결로 재오픈된 경우가 있었다 — 같은 이슈를 여러 자료에서 다룰 때는 **가장 최신 자료의 상태를 기준으로 삼아야** 한다.

**후속(같은 날).** 위에서 "진행중(미해결)"으로 남겨뒀던 오일니쁠압입기(경화촉진제 파이프 누설)·조립전 전체계 리크검사기(장착기준홀 직각도) 2건 모두 사용자 확인으로 개선 완료 처리 — 태그를 `진행중`에서 각각 "설비 정상"/"설비 이상"(개선 완료)으로 변경. 이로써 트러블슈팅 이력이 등록된 9개소 전체가 "개선 완료" 상태.

---

## 2026-09-18 "맨 위로 이동" 버튼 신규 추가

배경: 페이지가 길어져(특히 트러블슈팅 이력이 늘어난 설비들) 아래까지 내려간 뒤 다시 위로 스크롤하는 게 불편하다는 요청. 모바일·PC 모두에서 동작해야 하고, 이미 우측 하단에 떠 있는 챗봇 버튼과 겹치지 않아야 함.

**설계.** `gate.js`·`intro.js`·`assistant.js`와 같은 패턴으로 `scroll-top.js` 자기 완결형 파일을 신규 작성 — 자체 `<style>` 주입, 별도 CSS 파일 없음. 우측 하단 챗봇 버튼(`right:16px`/`bottom:16px`/54px 원형)과 같은 우측 열에서 그 바로 위(16+54+12=82px)에 40px 원형 버튼을 쌓았고, z-index도 챗봇(9000)보다 한 단계 낮게(8990) 둬서 챗봇 패널이 펼쳐졌을 때는 자연스럽게 그 아래로 가려지도록 했다. 스크롤이 480px 이상일 때만 페이드인으로 나타나고, 클릭하면 맨 위로 부드럽게 스크롤(`prefers-reduced-motion`이면 즉시 이동)한다. 인트로·챗봇이 로드되는 11개 페이지(홈+설비 10개)에 동일하게 추가, PDF 뷰어·404는 제외.

**버그 및 수정.** 최초 구현은 스크롤 이벤트를 `requestAnimationFrame`으로 스로틀링했는데, 브라우저 자동화 테스트 중 **탭이 백그라운드 상태일 때 예약된 rAF 콜백이 실행되지 않아 스로틀 플래그가 영원히 `true`로 멈추는 버그**를 발견했다 — 이후에는 스크롤을 아무리 해도 버튼이 다시는 나타나지 않았다. 버튼 표시 여부 판단은 숫자 비교 1번 + `classList` 토글뿐으로 매우 가벼운 작업이라 스로틀링 자체가 불필요하다고 판단, rAF 없이 스크롤 이벤트마다 바로 실행하도록 단순화해서 해결.

**검증.** 로컬 서버 + 브라우저로 실제 스크롤(프로그램상 `scrollTo()`가 아닌 사용자 제스처)로 버튼이 나타나는지, 클릭 시 맨 위로 이동 후 버튼이 다시 숨는지 확인. 홈(`index.html`, 자체 `<style>` 사용)과 설비 페이지(`styles.css` 공용) 양쪽에서 정상 동작 및 챗봇 버튼과 겹치지 않는 배치 확인, 콘솔 에러 없음.

**교훈(기록).** 스크롤/리사이즈 이벤트를 rAF나 `setTimeout`으로 스로틀링할 때, "예약된 콜백이 반드시 실행된다"고 가정하면 안 된다 — 탭이 숨겨지거나 폐기되는 등의 상황에서 콜백이 드롭되면 스로틀 플래그가 영구히 막힌 상태로 남을 수 있다. **작업 자체가 가벼우면 스로틀링을 아예 안 하는 게 더 안전하다.** 이런 종류의 버그는 정적 코드 리뷰로는 안 보이고, 실제 브라우저에서 스크롤을 실행해봐야 드러난다 — 이번에도 실제 `computer` 스크롤 제스처로 테스트하다 발견했다.

---

## 2026-09-18 첨부문서를 설비별 폴더 구조로 재편

배경: 설비별 첨부 PDF가 총 46개(첨부문서 26 + 기술문서 20)인데 아직 5개만 업로드된 상태 — 본격적인 업로드를 시작하기 전에 사용자가 "설비별 폴더 구분 없이 작업하면 나중에 뭐가 뭔지 모를 것 같다"며 루트에 평면으로 쌓는 대신 폴더로 나누자고 제안.

### 설계

`첨부문서/<공정No.>_<설비명>/` 폴더 10개 + 여러 설비가 같은 디텍터 매뉴얼(LS-R700 설치/조작)을 공유하는 경우를 위한 `첨부문서/공용_LS-R700디텍터/` 1개, 총 11개 폴더. 폴더명 앞에 공정No.를 붙여 사이트 표시 순서와 맞췄고, 파일명 자체는 기존 설비 접두어 규칙(`laser-marking-partlist.pdf` 등)을 그대로 유지해 폴더 밖으로 나와도 식별 가능하게 했다.

**사전 확인한 위험 요소**: `gate.js`의 2차 암호(기술문서) 판별 로직(`TECH_PDF_RE = /-(circuit|drawing)\.pdf$/i`)이 파일명 *끝부분*만 정규식으로 검사하는 것을 코드로 확인 — 폴더 경로가 앞에 붙어도 정상 동작함을 사전에 확인하고 진행.

### 실행

1. `첨부문서/` 하위 11개 폴더 생성
2. 기존 파일 5개를 `git mv`로 이동(+ 120번 설비의 구식 파일명 `partlist.pdf`/`machine-spec.pdf`/`manual.pdf`를 이 참에 다른 설비들과 같은 접두어 규칙으로 개명: `leak-pre-flow-partlist.pdf` 등)
3. 10개 설비 페이지의 `pdf-viewer.html?file=...` 링크 약 50개를 정규식 스크립트로 일괄 경로 변경

### 발견한 버그와 수정 — pdf-viewer.html 파일명 표시가 전체 경로를 노출

**문제.** 3단계 스크립트 적용 후 실제 링크를 브라우저로 열어보니, 뷰어 상단과 탭 제목에 `첨부문서/120_조립전유로계리크검사기/leak-pre-flow-partlist.pdf`처럼 **폴더 경로 전체**가 그대로 노출됨.

**원인.** `pdf-viewer.html`이 `file` 쿼리 파라미터 값을 그대로 화면에 표시하고 있었음(`fnameEl.textContent = decodeURIComponent(file)`) — 폴더가 없던 시절에는 파일명만 있어서 문제가 없었지만, 폴더 구조가 생기면서 그대로 회귀가 발생.

**조치.** `decodeURIComponent(file).split("/").pop()`으로 마지막 경로 조각(파일명)만 추출해서 표시하도록 수정.

**검증.** 로컬 서버로 실제 존재하는 파일(`leak-pre-flow-partlist.pdf`)을 열어 네트워크 요청이 200 OK로 정상 응답하는지, 화면·탭 제목에 파일명만 표시되는지 확인. 한글·특수문자(`·`, `(1개소)`)가 든 140번 폴더 경로도 브라우저가 자동으로 퍼센트 인코딩(`%EC%B2%A8...`, `%C2%B7`)해서 정상 요청되는 것을 네트워크 탭으로 직접 확인(해당 파일은 아직 미업로드라 404는 정상 — 인코딩 자체는 문제없이 됨을 확인하는 게 목적).

**교훈(기록).** 경로 구조를 바꾸는 리팩터링(평면 → 폴더)을 할 때는 "그 값을 화면에 표시하는 곳"도 함께 점검해야 한다 — 링크가 여전히 "작동"하는지만 확인하면 이런 표시 레이어의 회귀는 쉽게 놓친다. 이번에도 실제 브라우저로 열어보지 않았다면 배포 후에야 발견했을 것.

---

## 2026-09-21 기술문서 PDF 2차 암호 반복 요구 버그 수정

**배경.** 사용자가 실사용 중 발견해 보고: "조립전 유로계 리크 검사기에서 기술문서 터치해서 잠금해제하고 설비 전기회로도 터치하면 또 암호창이 뜬다. 이건 없애줘. 번거롭다."

**문제.** ③ 기술문서 섹션을 2차 암호로 정상적으로 잠금 해제한 직후, 그 안의 "설비 전기회로도"/"기계도면" PDF 링크를 누르면 **또다시 2차 암호를 물어봄.** 같은 탭 안에서 방금 통과했는데도 매번 반복됨.

**원인분석.** `gate.js`는 2차 암호 통과 여부를 `sessionStorage`(`dnk_gate2_ok_v1`)에 저장하고, 같은 탭에서는 이 값을 재사용해 다시 안 묻도록 설계돼 있다(§8). 그런데 "② 첨부 문서"/"③ 기술문서"의 모든 PDF 링크가 `target="_blank" rel="noopener"`로 걸려 있어 **클릭할 때마다 새 브라우저 탭이 열렸다.** `sessionStorage`는 탭(정확히는 최상위 브라우징 컨텍스트)마다 별개의 저장소이고, 같은 출처(origin)에서 새 탭을 열면 보통 열린 시점의 `sessionStorage`를 복사해서 물려받지만, **`rel="noopener"`를 쓰면 새 탭이 원래 탭과 아무 연관도 없는(opener 참조가 없는) 완전히 독립된 브라우징 컨텍스트로 열리기 때문에 이 복사가 일어나지 않는다.** 결과적으로 PDF를 열 때마다 매번 빈 `sessionStorage`로 시작해서 게이트가 처음부터 다시 인증을 요구한 것.

**조치.**
1. 설비 10개 페이지의 PDF 첨부 링크(약 54개) 전부에서 `target="_blank" rel="noopener"` 제거 — 이제 같은 탭에서 `pdf-viewer.html`로 이동하므로 `sessionStorage`가 그대로 유지됨
2. 같은 탭에서 열리게 되면서 "뒤로 갈 방법"이 없어지는 문제가 생기므로, `pdf-viewer.html` 상단에 뒤로가기 버튼(◀ 아이콘)을 새로 추가 — `document.referrer`가 같은 출처면 `history.back()`, 아니면(주소를 직접 입력해 들어온 경우 등) `index.html`로 이동

**검증.** 로컬 서버 + 브라우저로 실제 시나리오 재현: 1차 암호 통과 → ③ 기술문서 섹션 클릭(2차 암호 입력, `dnk_gate2_ok_v1` 세팅 확인) → "설비 전기회로도" 클릭 → **같은 탭에서 이동하고 암호창이 뜨지 않는 것 확인**(`document.querySelector('.dnk-gate')`가 `null`). 뒤로가기 버튼 클릭 시 원래 설비 페이지로 정상 복귀하는 것도 확인. 일반 첨부문서(잠금 없는 PART LIST 등) 링크도 같은 탭에서 정상적으로 열리는 것 확인.

**교훈(기록).** `sessionStorage`로 인증·상태를 유지하는 사이트에서는 **그 상태가 필요한 페이지로 가는 링크에 `target="_blank"`를 쓰면 안 된다** — 특히 `rel="noopener"`를 같이 쓰면(보안 모범사례라 무심코 붙이기 쉬움) 새 탭이 완전히 새로운 세션처럼 동작해 인증이 통째로 리셋된다. 이런 문제는 코드만 보면 "게이트 로직은 멀쩡한데 왜 안 되지"로 헷갈리기 쉽고, 실제로 새 탭을 열어보며 재현해봐야 원인이 링크의 `target`/`rel` 속성에 있다는 게 드러난다. §17 체크리스트에 반영.

---

## 2026-09-22 첫 기술문서 PDF 업로드분이 안 뜨던 문제 — 파일명 불일치

**배경.** 위 target="_blank" 수정 이후에도 사용자가 "모바일과 PC 모두 여전히 동일현상"이라고 재보고. Claude가 배포된 실제 사이트에서 존재하지 않는 파일과 존재하는 파일을 각각 열어 재현을 시도했으나 재현되지 않아 원인을 못 찾고 있었음 — 사용자가 직접 실제로 올려둔 파일 경로를 알려주면서 원인이 드러남.

**문제.** 사용자가 `첨부문서/120_조립전유로계리크검사기/`에 전기회로도 PDF를 올렸는데도 "문서를 불러오지 못했습니다"가 계속 뜸.

**원인분석.** 업로드된 실제 파일명이 `leak-pre-flow-electrical circuit diagram.pdf`였음 — 알아보기 쉽게 지은 이름이지만, `leak-pre-flow.html`의 링크와 `PROJECT_OVERVIEW.md` §6 표는 정확히 `leak-pre-flow-circuit.pdf`를 요구한다. 이 시스템은 파일명을 유사 검색하지 않고 **정확히 일치**해야만 찾으므로, 이름이 조금만 달라도 "파일 없음"으로 처리된다. 직전 세션에서 Claude가 "파일이 없어서 그렇다"고 설명은 했지만, "사용자가 이미 올렸는데 이름이 다르다"는 가능성은 검토하지 않고 배포 사이트 재현 시도에만 집중했던 것도 원인 파악이 늦어진 요인.

**조치.** `git mv`로 `leak-pre-flow-electrical circuit diagram.pdf` → `leak-pre-flow-circuit.pdf`로 파일명 정정. `PROJECT_OVERVIEW.md` §6 최상단에 "파일명은 표와 글자 하나까지 정확히 같아야 한다"는 경고 문구를 눈에 띄게 추가(남은 44개 파일을 업로드할 때 같은 문제가 반복되지 않도록).

**검증.** 로컬 서버로 `leak-pre-flow-circuit.pdf`를 pdf-viewer.html에서 직접 열어 정상 렌더링 확인(16페이지, "INDEX / CIRCUIT DIAGRAM / BOX DRAWING / PART LIST / NAME PLATE" 목차가 담긴 실제 회로도 문서임을 화면으로 확인). 파일 용량 1.42MB로 압축 기준(3MB 이하) 이내라 재압축 불필요.

**교훈(기록).** "재현이 안 된다"는 것은 "문제가 없다"는 뜻이 아니라 **재현하는 조건 자체를 잘못 짚었을 수 있다**는 신호다 — 이번엔 "파일이 아예 없는 경우"만 재현해보고 "파일은 있지만 이름이 다른 경우"는 시도해보지 않았다. 사용자가 "여전히 같은 문제"라고 재보고하면, 같은 테스트를 반복하기 전에 **사용자가 그 사이에 무엇을 했는지**(예: 파일을 실제로 올렸는지)부터 물어보는 게 더 빨랐을 것. 파일명 기반으로 찾는 시스템에서는 "그 파일이 실제로 그 정확한 이름으로 존재하는가"가 가장 먼저 확인할 항목.

---

## 2026-09-22 여러 파일로 나뉜 기계도면 병합 — pdf.js 다중 페이지 렌더링 멈춤 현상 조사

**배경.** 사용자가 조립전 유로계 리크검사기(120) 기계도면 원본 8개(`LK2522-00-000.pdf` ~ `LK2522-70-000.pdf`, 위텍 원본, 각 1페이지)를 네트워크 공유폴더에서 지정하며 "합치기 해서 업로드하자"고 요청.

**작업.** `pymupdf`로 파일명 번호 순서(00→70)대로 `insert_pdf()` 병합 → 8페이지, 5.0MB → `tools/compress_pdf.py`로 4.41MB로 재압축 → `첨부문서/120_조립전유로계리크검사기/leak-pre-flow-drawing.pdf`로 저장(§6 표의 정확한 파일명).

**문제(조사 과정에서 발견).** 병합한 PDF를 로컬 서버 + Claude의 자동화 브라우저로 열어 검증하던 중, **1페이지만 렌더링되고 2페이지부터 응답 없이 멈추는 현상**을 반복 관찰. 콘솔 에러도, pdf.js 경고도 전혀 없이 조용히 멈춤.

**원인분석(결론 내지 못함).** 상당히 깊게 조사함:
1. `pymupdf` 병합 대신 `pypdf`로 다시 병합 → 동일 증상
2. 벡터 대신 각 페이지를 이미지로 래스터화해서 병합 → 동일 증상
3. 페이지 콘텐츠와 무관한지 확인하려고 `pymupdf`로 순수 텍스트만 있는 3페이지짜리 최소 테스트 PDF를 새로 만들어 테스트 → **이 파일에서도 같은 증상이 간헐적으로 발생**(어떤 시도에서는 3페이지가 전부 정상 렌더링되기도 함)
4. `page.render()` 결과를 직접 `Promise.race`로 20초 타임아웃을 걸어도 타임아웃 콜백 자체가 실행되지 않는 경우가 있었음(반면 같은 탭에서 단순 `setTimeout` 단독 테스트는 정상 동작) — 메인 스레드가 완전히 멈춘 건 아니지만 특정 상황에서 타이머가 밀리는 것으로 추정

콘텐츠(벡터/래스터/순수 텍스트)를 바꿔도, 병합 라이브러리를 바꿔도 증상이 남아있고, 재현 성공/실패가 매번 달라지는 것으로 보아 **파일 자체의 결함이 아니라 Claude의 자동화 브라우저 테스트 환경(헤드리스/자동화 특유의 타이머·워커 처리 방식) 쪽 문제일 가능성이 높다**고 잠정 결론 — 그러나 실제 사용자의 스마트폰/PC 브라우저에서도 같은 문제가 나타날지는 이 세션에서 끝내 확인하지 못했다.

**조치(원인과 무관하게 적용하는 방어 로직).** 원인을 확정하지 못했으므로, "어떤 이유로든 특정 페이지 렌더링이 멈출 수 있다"는 가정 하에 `pdf-viewer.html`의 `renderNext()`에 **페이지별 20초 제한시간**을 추가. 시간 안에 렌더링이 끝나지 않으면 해당 페이지를 빈 캔버스 대신 "N번째 페이지를 표시하지 못했습니다" 안내 박스로 대체하고 다음 페이지로 계속 진행 — 최소한 사용자가 "설명 없는 무한 로딩"에 갇히는 일은 없도록 함. 단일 페이지 문서(기존 파일들)에는 영향 없음을 재확인.

**검증.** 단일 페이지 PDF(`leak-pre-flow-partlist.pdf`)가 새 코드에서도 정상 렌더링되는 것 확인, 콘솔 에러 없음, `pdf-viewer.html` 태그 균형 확인. **8페이지 병합 파일이 실제로 끝까지 넘어가는지는 실사용 환경에서 재확인이 필요함** — §6·§17에 이 사실을 명시해뒀고, 다음에 유사한 다중페이지 파일을 올릴 때 실제 기기로 확인하도록 체크리스트에 반영.

**교훈(기록).** 자동화 브라우저 도구로 재현한 결과라도 **항상 실제 사용 환경을 100% 대변하지는 않는다** — 특히 타이머·워커 스케줄링처럼 브라우저 엔진의 저수준 동작에 관련된 문제는 헤드리스/자동화 환경 특유의 differences가 있을 수 있다. 이런 경우 "원인을 완전히 규명하지 못했다"고 정직하게 인정하고, 원인과 무관하게 사용자 경험을 지켜주는 방어 코드(타임아웃 + 안내 메시지)를 넣는 것이 "원인을 찾을 때까지 무기한 파고드는 것"보다 실용적일 때가 있다. 다만 이는 근본 해결이 아니므로, 실사용 환경에서의 최종 확인 책임을 문서에 명시적으로 남겨 다음 사람(또는 다음 세션)이 놓치지 않도록 했다.

---

## 2026-09-22 PC 화면에서 상단 헤더와 본문 가로폭이 안 맞던 문제

**배경.** 사용자가 PC(가로로 넓은 화면)에서 설비 페이지를 보다가 스크린샷과 함께 보고: "pc 상에서 상단부와 하단부와 가로사이즈가 안맞다."

**문제.** 넓은 데스크톱 화면에서 상단 네이비 영역(식별 바 `.id-bar` + 문서 헤더 `.doc-header`, 공정 정보 표 포함)은 브라우저 창 끝까지 꽉 차게(full-bleed) 늘어나는데, 그 아래 아코디언 섹션(`<main>`)과 맨 아래 `<footer>`는 가운데 640px 폭으로만 좁게 표시됨 — 위아래 폭이 서로 안 맞아 화면이 어긋나 보임.

**원인분석.** `styles.css`에서 `main`과 `footer`에는 이미 `max-width:640px; margin:0 auto;`가 걸려 있어 데스크톱에서도 좁은 "앱 프레임" 폭으로 가운데 정렬되지만, 같은 처리가 `.id-bar`·`.doc-header`에는 빠져 있었다. 이 두 요소는 `width` 제한이 없는 채로 `body`(`background:var(--gray-100)`) 바로 아래 배치돼 있어, 뷰포트가 640px보다 넓어지는 순간(=PC) 화면 끝까지 그대로 늘어난다. 뷰포트가 640px보다 좁은 모바일에서는 어차피 모든 요소가 뷰포트 폭에 맞춰지므로 이 차이가 드러나지 않았고, 그래서 지금까지 (모바일 375px 뷰포트로만 검증해온) 재점검 과정에서 발견되지 못했다.

**조치.** `styles.css`의 `.id-bar`, `.doc-header` 규칙에 `main`/`footer`와 동일한 `max-width:640px; margin:0 auto;`를 추가. 10개 설비 페이지가 모두 이 공용 `styles.css`를 공유하므로 한 곳만 고치면 전체에 반영됨.

**검증.** 로컬 서버(`.claude/launch.json`의 `static-site`)로 `leak-pre-flow.html`을 열어, 데스크톱 폭(1400px)에서 상단 헤더와 아코디언 섹션의 좌우 끝이 정확히 일치하는 것을 스크린샷으로 확인. 모바일 폭(375px)에서도 기존과 동일하게 화면 끝까지 꽉 차는 레이아웃이 유지되는 것(회귀 없음)을 재확인.

**교훈(기록).** 이 시스템은 `main`/`footer`만 `max-width`로 좁혀 데스크톱에서 "가운데 정렬된 앱처럼" 보이게 설계돼 있는데, 새 상단 블록(`.id-bar`, `.doc-header`)을 추가할 때 이 폭 제한을 안 물려받으면 같은 페이지 안에서도 섹션마다 폭이 달라진다. **모바일 375px 뷰포트 검증만으로는 이런 폭 불일치가 절대 드러나지 않는다** — 640px보다 넓은 데스크톱 폭에서 최소 한 번은 확인해야 하는 종류의 버그. §17 체크리스트에 반영.

---

## 2026-09-22 기술문서 PDF(전기회로도·기계도면)가 PC에서 안 열리고 모바일에서는 흐릿함

**배경.** 사용자가 "조립전 유로계 리크 검사기"의 ③ 기술문서를 2차 암호로 해제한 뒤 "설비 전기회로도"·"기계도면" 2개 PDF를 눌러본 결과를 스크린샷 3장과 함께 보고: "1. 모바일로 전기회로도, 기계도면 모두 열람가능. 하지만 너무 흐릿하다. 2. pc로는 2개 클릭하면 열리지 않는다." PC 스크린샷은 `pdf-viewer.html`이 "불러오는 중..." 상태에서 멈춰 있는 화면이었다.

**문제.** 이 2개 파일은 직전(2026-09-22) 세션에서 이미 "여러 페이지로 합친 PDF에서 pdf.js가 특정 페이지 이후 응답 없이 멈추는 현상"으로 조사했던 파일들과 동일하다. 당시엔 Claude의 자동화 브라우저에서만 재현되고 원인을 못 찾아 "자동화 환경 특유의 문제일 수 있다"는 잠정 결론과 함께 페이지별 20초 제한시간 방어 로직만 추가하고 넘어갔었는데, 이번에 **실제 사용자의 PC에서도 동일하게 실패**한 것이 확인되어, 자동화 환경 한정 문제가 아니라 실재하는 렌더링 문제였음이 드러났다.

**원인분석.**
1. **PC "안 열림"**: `pdf-viewer.html`을 다시 열어 재현한 결과, `leak-pre-flow-circuit.pdf`(16페이지, A3 CAD 도면)·`leak-pre-flow-spec.pdf`(31페이지, 일반 문서) 둘 다 pdf.js로 캔버스에 그리는 속도가 페이지당 최대 20초(기존에 넣어둔 제한시간)까지 걸리는 경우가 실제로 있었고, 여러 페이지가 연달아 제한시간에 걸리면 사용자 입장에선 "몇 분간 아무것도 안 뜨는" 것과 다름없어 보인다. 근본 원인(pdf.js 4.10.38이 특정 페이지에서 왜 멈추는지)은 직전 세션에서도 못 찾았고, 이번에도 벡터 콘텐츠·병합 방식과 무관하게 재현되어 **pdf.js 자체의 한계로 결론짓고 더 이상 원인을 캐지 않기로 함.**
2. **모바일 "흐릿함"**: `pdf-viewer.html`의 캔버스 렌더링 코드가 `canvas.width`/`canvas.height`(실제 해상도)를 CSS 표시 크기와 1:1로만 맞추고 있었다. 그런데 `.pg{max-width:100%;height:auto}`로 화면 폭에 맞춰 표시되므로, DPR(기기 픽셀비)이 2~3인 스마트폰에서는 그 낮은 해상도의 캔버스를 물리적으로 2~3배 확대해서 그리는 셈이 되어 흐릿하게 보였다. `window.devicePixelRatio`를 렌더링에 전혀 반영하지 않은 게 원인.

**조치.**
1. **PC 문제(근본 회피)**: `tools/pdf_to_page_images.py`(pymupdf 기반, 신규 작성)로 두 파일을 200dpi PNG로 페이지별 사전 래스터화 — `leak-pre-flow-circuit.pages/1.png`~`16.png`(총 4.2MB), `leak-pre-flow-drawing.pages/1.png`~`8.png`(총 2.6MB)로 저장. [pdf-viewer.html](pdf-viewer.html)을 수정해 `<file>.pages/1.png`가 존재하면 pdf.js를 아예 거치지 않고 `<img>` 태그로 순서대로 붙여나가도록(다음 번호가 404면 종료) 우선 시도하고, 없으면 기존 pdf.js 경로로 자동 폴백하게 함 — 일반 첨부문서(PART LIST·매뉴얼 등)는 `.pages/` 폴더가 없으므로 지금까지처럼 pdf.js로 그대로 렌더링되어 영향 없음.
2. **모바일 흐릿함**: pdf.js 렌더링 경로(`renderWithPdfJs`)에서 `var outputScale = window.devicePixelRatio || 1;`을 구해 `canvas.width/height`는 `viewport.width/height * outputScale`로, `canvas.style.width/height`는 기존 CSS 표시 크기 그대로 별도 지정하고, `page.render()`에 `transform:[outputScale,0,0,outputScale,0,0]`을 넘겨 실제 해상도만 올렸다(화면 표시 크기는 그대로 유지). 이 fallback 경로는 모든 일반 첨부문서에도 적용되므로, 사전 래스터화 대상이 아닌 PDF의 흐릿함도 같이 해결됨.

**검증.** 로컬 서버에서:
- `leak-pre-flow-circuit.pdf`·`leak-pre-flow-drawing.pdf`를 열어 콘솔·네트워크 로그로 `<img>` 16장/8장이 전부 200 OK로 즉시 로드되고 pdf.js 경로(`pdf.worker.min.mjs` 요청)는 아예 발생하지 않는 것을 확인, 스크린샷으로 실제 도면이 선명하게 표시되는 것도 확인.
- `.pages/` 폴더가 없는 `leak-pre-flow-partlist.pdf`는 `1.png` 요청이 404로 확인된 뒤 정상적으로 기존 pdf.js 경로로 폴백해 렌더링되는 것 확인(회귀 없음).
- 모바일 프리셋(375px, DPR 2)에서 같은 partlist.pdf를 열어 `canvas.width`가 CSS 표시폭(359px)의 2배(718px)로 렌더링되는 것을 `devicePixelRatio` 값과 대조해 확인 — 수정 전이었다면 359px로만 그려져 확대 시 흐릿했을 상황.
- 실제 배포 사이트(`https://dnk-mobility.github.io`)에서도 수정 전 상태로 동일 증상(느린 렌더링·페이지별 20초 제한시간 발동)이 재현되는 것을 먼저 확인한 뒤 조치를 적용함 — 로컬 재현이 아니라 실제 운영 환경 기준으로 원인을 짚었다.

**교훈(기록).** ① "자동화 브라우저에서만 재현되고 실기기에서 확인 못 함"이라고 남겨둔 이슈는 **최종 결론이 아니라 미해결 상태**라는 뜻이다 — 실제 사용자가 같은 증상을 보고하면 바로 "역시 진짜 문제였다"로 전환하고, 근본 원인을 더 캐는 대신 문제 자체를 우회하는 방향(사전 래스터화)으로 선회하는 것이 실용적일 때가 있다. ② 벡터 PDF를 브라우저에서 실시간으로 그리는 방식은 내용이 복잡하거나 페이지가 많으면 얼마나 걸릴지 예측하기 어렵다 — 내용이 자주 안 바뀌는 문서(도면류)라면 업로드 시점에 한 번 이미지로 구워두는 쪽이 실시간 렌더링보다 훨씬 예측 가능하고 빠르다. ③ `<canvas>`로 뭔가를 그려서 화면에 보여줄 때는 표시 크기(CSS px)와 실제 해상도(`canvas.width`/`height`)를 같게 두면 안 된다 — DPR을 곱해 해상도를 올려야 고밀도 화면에서 흐리지 않다. 이 프로젝트에 캔버스 렌더링 코드가 또 생긴다면 반드시 적용할 것(§17 반영).

**후속(같은 날, 배포 누락).** 위 조치를 로컬 서버로 검증까지 마친 뒤 사용자에게 "수정했다"고 보고했는데, 사용자가 재테스트했더니 **PC·모바일 둘 다 증상이 그대로**였다. 원인은 어이없게도 **`git commit`/`push`를 안 해서** GitHub Pages(`dnk-mobility.github.io`)에는 옛날 코드가 그대로 있었던 것 — 로컬 작업 폴더만 고쳐놓고 "고쳤다"고 말한 셈. 사용자 확인 후 `git add`(관련 파일만 선별)→`commit`→`push`, `gh api .../pages/builds/latest`로 빌드가 `built`로 끝난 것까지 확인하고, 배포된 실제 주소에서 재차 렌더링을 검증했다. **교훈**: 로컬 검증은 "코드가 맞다"는 증거일 뿐 "사용자가 보는 화면이 바뀌었다"는 증거가 아니다 — 이 프로젝트처럼 정적 사이트를 배포해서 쓰는 경우, **커밋·푸시·Pages 빌드 완료까지 확인해야 비로소 "고쳤다"고 말할 수 있다.** §17에 반영.

**최종 확인(사용자, 같은 날).** 위 배포 이후 사용자가 실사용 환경에서 재확인 후 "기술문서 문제 해결되었다"고 확정 답변. 이 항목은 여기서 **해결 완료**로 종결한다.

---

## 2026-09-22 설비 부착용 QR 라벨에서 "전체 설비 인덱스" 라벨이 빠져 있던 문제

**배경.** 위 PDF 뷰어 수정을 확인하려던 사용자가 그 전에 `설비QR라벨_70x100mm_A4인쇄용.pdf`를 보다가 "인덱스는 없네?"라고 지적. 처음엔 "PDF 안의 목차 페이지가 없다"는 뜻으로 이해해 재확인 질문을 드렸더니, 실제로는 "**공정별 QR코드 말고 전체설비인덱스(index.html)로 바로 가는 QR코드**가 없다"는 뜻이었다.

**문제.** 현재 라벨 PDF(`tools/make_labels.py`가 생성)는 `EQUIPMENT` 목록에 개별 설비 10개(No.110~200)만 있고, `index.html`(전체 설비 목록 홈)로 연결되는 라벨이 없다. 반면 저장소에 남아있는 더 예전 산출물 `qr-codes/QR라벨_전체11개_통합.pdf`는 파일명부터 "11개"로, 10개 설비 + 인덱스 1개를 담고 있었다.

**원인분석.** 2026-08 이전에는 `tools/make_qr.py`로 `index` 포함 11개 PNG QR을 한 번에 만들었다(그중 `qr-codes/index.png`가 지금도 `BASE + ""`, 즉 루트 주소로 남아있음). 그런데 2026-09-17에 지금의 네이비 브랜드 디자인(70×100mm, SCAN 헤더·QR 카드·공정No.·설비명 레이아웃)으로 `tools/make_labels.py`를 새로 만들면서 `EQUIPMENT` 목록을 "설비 10개소"만 기준으로 다시 정의했고, 인덱스 항목은 옮겨오지 않았다 — 새 디자인이 "공정No.별 라벨"이라는 개념에 맞춰져 있어서 애초에 "설비가 아닌 항목"이 들어갈 자리를 고려하지 않았던 것으로 보인다. 즉 재설계 과정에서 조용히 빠진 회귀(regression)였다.

**조치.** `tools/make_labels.py`에 `INDEX_LABEL = ("전체", "전체 설비 인덱스", "")`를 정의해 `EQUIPMENT` 목록 맨 앞에 추가(`file=""` → `BASE_URL` 루트 = `index.html`로 연결). 공정No. 자리에는 숫자 대신 "전체"를 표시하도록 하고, 페이지 머리말 생성 로직(`"No.%d" % e[0]`, 숫자 전용)이 문자열 공정No.를 만나도 에러 없이 설비명으로 표기하도록 분기를 추가한 뒤 재생성 — 라벨 10개 → 11개, 페이지는 그대로 3장(마지막 페이지가 4칸 중 3칸만 채워짐).

**검증.** `pyzbar`로 생성된 PDF를 페이지 이미지로 렌더링해 QR을 실제 디코딩 — 새 "전체" 라벨이 `https://dnk-mobility.github.io/`(index.html 루트)로 정확히 연결되는 것, 기존 No.110 라벨(레이저 마킹기)이 여전히 `laser-marking.html`로 정상 연결되는 것(회귀 없음)을 확인. 페이지 1(4칸)·페이지 3(3칸, 빈 칸 없이 정상 배치)을 150dpi로 렌더링해 레이아웃도 육안으로 확인.

**교훈(기록).** 기존 기능을 "새 디자인으로 다시 만든다"고 할 때, 원래 있던 항목 목록을 통째로 베끼지 않고 새로 처음부터 정의하면(`EQUIPMENT` 목록을 "설비 10개소" 기준으로 새로 짠 것처럼) 예전엔 있었지만 새 개념 모델에 안 맞는 항목(이번엔 "설비가 아닌 인덱스")이 조용히 빠질 수 있다. 재설계 시엔 예전 산출물(`QR라벨_전체11개_통합.pdf` 같은)과 항목 개수를 대조해보는 습관이 이런 회귀를 미리 잡아줬을 것.

---

## 2026-09-22 전체 설비 인덱스(index.html)에서도 상단·본문 가로폭이 안 맞던 문제

**배경.** 앞서 같은 날 설비 페이지(`leak-pre-flow.html`)에서 PC 화면 상단 헤더와 본문 가로폭이 안 맞는 버그를 `styles.css`에서 고쳤는데(§ 위 "PC 화면에서 상단 헤더와 본문 가로폭이 안 맞던 문제" 항목), 사용자가 이번엔 `index.html`(전체 설비 인덱스, 홈 화면)에서 같은 증상을 지적함.

**문제.** `index.html`도 PC 등 640px보다 넓은 화면에서 상단 `.id-bar`·`.doc-header`(네이비 영역)는 화면 끝까지 꽉 차게 늘어나고, 그 아래 설비 목록(`<main>`)·`<footer>`는 가운데 640px로 좁게 표시되어 위아래 폭이 어긋남 — 앞서 고친 것과 **증상·원인이 완전히 동일**.

**원인분석.** `index.html`은 설비 10개 페이지와 달리 공용 `styles.css`를 쓰지 않고 **자기만의 인라인 `<style>` 블록**을 갖고 있다(§4 — "홈·PDF 뷰어는 레이아웃이 달라 각자의 인라인 스타일을 유지"). 그래서 앞서 `styles.css`의 `.id-bar`/`.doc-header`에 `max-width:640px; margin:0 auto;`를 추가한 조치가 `index.html`에는 전혀 적용되지 않았다 — **같은 버그를 고칠 때 "이 사이트에 이 CSS 블록이 몇 군데 있는지"를 확인하지 않고 한 곳만 고친 것이 원인.**

**조치.** `index.html`의 인라인 `<style>` 안 `.id-bar`·`.doc-header` 규칙에 동일하게 `max-width:640px; margin:0 auto;` 추가.

**검증.** 로컬 서버에서 데스크톱 폭(1400px)으로 `index.html`을 열어 상단 헤더와 설비 목록의 좌우 끝이 정확히 일치하는 것을 스크린샷으로 확인.

**교훈(기록).** 이 저장소는 CSS가 한 곳(`styles.css`)이 아니라 **세 군데**(`styles.css` — 설비 10개 공용, `index.html` 자체 인라인, `pdf-viewer.html` 자체 인라인)로 나뉘어 있다. "설비 페이지에서 발견된 버그"를 고칠 때, 그 버그를 일으킨 CSS 패턴이 다른 두 곳에도 중복돼 있지는 않은지 항상 같이 확인해야 한다 — 하나만 고치고 넘어가면 이번처럼 사용자가 "여기도 똑같다"고 다시 지적하게 된다. §4·§17에 반영.

---

## 2026-09-22 설비 페이지 간 이동 편의성 개선 — 인덱스로 가는 홈 아이콘 버튼

**배경.** 사용자 요청 2건을 함께 처리하려 했다: (1) "각 공정별에서 인덱스로 이동할 수 있게 해줘" — 설비 페이지에서 전체 설비 인덱스(`index.html`)로 돌아갈 방법이 브라우저 뒤로가기뿐이었음. (2) "설비개요, 첨부문서 등 각 탭별로 색으로 구분 좀 해줘" — 아코디언 섹션 9개가 전부 같은 네이비 번호 원이라 구분이 안 됨.

**1차 시도 → 사용자 피드백으로 롤백.** 처음엔 (1)을 상단 문서 라벨 텍스트("공정 설비 정보관리 시스템")를 `index.html`로 가는 링크로, (2)를 각 `<details class="section">`에 내용 기준 `data-sec` 값(overview/docs/tech/procedure/spec/checklist/manual/trouble/notice)을 부여해 번호 원 배경색 9종으로 구분하는 방식으로 구현·검증까지 마쳤다. 그런데 사용자가 재검토 후 "① 텍스트 링크는 눈에 안 띄어서 사용자가 모를 수 있다, 육안으로 뚜렷하게 해달라"·"② 색 구분은 너무 화려해서 별로다, 취소해달라"고 피드백. 둘 다 **기능은 정상 동작했지만 실제 사용성/취향 측면에서 반려**된 경우.

**최종 조치.**
1. **인덱스로 가는 링크 → 아이콘 버튼으로 교체.** 텍스트 링크(`.home-link`) 방식을 걷어내고, `pdf-viewer.html`에 이미 있던 "뒤로가기 원형 아이콘 버튼" 패턴을 그대로 가져와 `.id-bar` 맨 왼쪽에 집 모양 SVG 아이콘 버튼(`.home-btn`, `rgba(255,255,255,.12)` 배경의 둥근 사각형)을 추가 — 텍스트보다 "버튼처럼 보이는 것"이 클릭 가능성을 훨씬 명확히 전달한다는 판단. `.id-bar`가 원래 `justify-content:space-between`으로 좌우 2요소만 배치하던 구조라, 아이콘을 추가하며 `align-items:center;gap` + 오른쪽 요소에 `margin-left:auto`로 레이아웃을 바꿈.
2. **섹션별 색상 구분은 완전히 되돌림.** `styles.css`의 `.section[data-sec="..."]` 배경색 규칙 9개와 `.home-link` 관련 CSS를 삭제, 설비 10개 파일에서 `data-sec` 속성을 전부 제거(원래의 균일한 네이비 번호 원으로 복귀).

**검증.** 로컬 서버에서 데스크톱·모바일(375px) 양쪽으로 홈 아이콘 버튼이 뚜렷하게 보이는 것, 클릭 시 `index.html`로 정상 이동하는 것, 아코디언 번호 원이 전부 원래의 네이비 단색으로 돌아간 것을 스크린샷·`getComputedStyle`로 확인. `git diff`로 10개 파일의 최종 변경분이 "홈 버튼 3줄 추가"만 남고 `data-sec`·`.home-link` 관련 변경은 순감(net-zero)인 것도 확인.

**사고(작업 중 실수, 즉시 복구).** 되돌리는 스크립트를 작성하며 `re.subn()`의 반환값 순서(`(새 문자열, 치환 횟수)`)를 반대로 언패킹하는 실수를 해서(`n_sec, content = ...subn(...)` → 실제로는 `content, n_sec = ...subn(...)`이어야 함) `content` 변수에 정수가 들어갔고, 그 상태로 `open(path, "w")`을 이미 연 뒤(파일이 그 순간 빈 파일로 잘림) `f.write(content)`에서 `TypeError`가 나면서 **`laser-marking.html`이 빈 파일로 잘렸다.** 다행히 이 파일은 아직 `git commit` 전이었으므로 `git checkout -- laser-marking.html`로 즉시 원상복구하고, 스크립트에 `len(content) < 3000`이면 아예 쓰지 않는 안전장치를 추가한 뒤 재실행해 해결했다. 다른 9개 파일은 순서상 이 파일 다음 차례라 영향 없었다.

**교훈(기록).** ① 여러 파일에 같은 텍스트 치환을 거는 스크립트를 짤 때는 `open(path, "w")`로 먼저 파일을 열어(그 순간 내용이 사라짐) 그 다음에 새 내용을 계산하는 순서가 아니라, **새 내용을 전부 계산·검증(최소 길이 등)한 뒤에만 쓰기 모드로 열어야** 중간에 예외가 나도 원본이 보존된다. 이번엔 `git`으로 추적되던 파일이라 즉시 복구할 수 있었지만, 커밋 전 상태였다면 더 위험했다. ② UI 개선 요청은 "기능이 동작하는가"와 "사용자가 실제로 좋아하는가"가 다른 축이다 — 텍스트 링크도 색 구분도 기술적으로는 문제없이 동작했지만 실사용 관점에서 반려됐다. 검증을 "의도대로 렌더링되는가"에서 멈추지 말고, 저장·보고 전에 결과물을 스스로도 한 번 더 훑어보는 습관이 재작업을 줄인다.

**후속(같은 날, 2차 피드백).** 사용자가 "모바일로는 버튼이 안 보인다"고 재차 보고. 확인해보니 **이 시점까지 위 변경사항 전부가 아직 `git commit`/`push` 전(로컬에만 존재)** 이었다 — §"기술문서 PDF..." 항목에서 이미 한 번 겪은 것과 똑같은 배포 누락 패턴. 다만 이번엔 사용자가 디자인 자체도 더 눈에 띄게 해달라고 요청했으므로, 배포 여부와 별개로 디자인도 함께 개선: 아이콘을 얇은 선(16px, 투명에 가까운 흰 배경)에서 **꽉 채운 집 모양(18px) + 32×32px 시안(`#37c6e0`) 배경**으로 키움 — 이 시안은 QR 라벨(§18)의 브랜드 시안과 동일해 다른 곳과 색상 일관성도 챙김. 이번엔 커밋·푸시 후 GitHub Pages 빌드 완료를 확인하고, 실제 배포 주소에 모바일 폭으로 접속해 스크린샷까지 찍어 검증한 뒤 사용자에게 보고 — 배포 확인 없이 "고쳤다"고 말하지 않는 습관이 자리잡음.

---

## 2026-09-22 설비 페이지 상단 id-bar 텍스트 줄바꿈 지점 지정

**배경.** 홈 아이콘 버튼을 추가한 뒤, 좁아진 여백 때문에 상단 id-bar의 "DnK MOBILITY · 후공정 생산기술팀" / "e-AWD 70kW MOTOR HOUSING ASS'Y" 텍스트가 브라우저 자동 줄바꿈에 맡겨져 "DnK MOBILITY · 후공정" / "생산기술팀"처럼 어중간한 지점에서 끊기고 있었다. 사용자가 "DnK MOBILITY 다음줄에 후공정생산기술팀", "e-AWD 70kW 다음줄에 MOTOR HOUSING ASS'Y"로 지정해서 요청.

**조치.** 설비 페이지 10개의 해당 두 `<span>`에 `<br>`을 넣어 줄바꿈 지점을 고정 — `DnK MOBILITY<br>후공정 생산기술팀`, `e-AWD 70kW<br>MOTOR HOUSING ASS'Y`. 요청이 "공정별 화면"으로 한정됐으므로 `index.html`(홈)은 건드리지 않음. 10개 파일에 동일 문구라 이전과 같은 방식(파이썬 스크립트, 쓰기 전 결과 길이 검증)으로 일괄 적용.

**검증.** 로컬 서버에서 데스크톱(900px)·모바일(375px) 양쪽 스크린샷으로 지정한 지점에서 정확히 줄바꿈되는 것, 오른쪽 텍스트("e-AWD 70kW"/"MOTOR HOUSING ASS'Y")가 오른쪽 정렬돼 자연스럽게 보이는 것을 확인.

**교훈(기록).** 자동 줄바꿈(`white-space:normal`)은 컨테이너 폭이 바뀔 때마다(이번처럼 버튼이 추가돼 여백이 줄어들 때 등) 끊기는 지점이 달라질 수 있다 — 상단 브랜드 표기처럼 "항상 이 지점에서 끊겨야 한다"는 요구가 있는 텍스트는 자동 줄바꿈에 맡기지 말고 `<br>`로 명시하는 게 안전하다.

---

## 2026-09-22 챗봇 위젯 버튼에 마우스오버 글로우 펄스 효과 추가

**배경.** 사용자 요청: "모든 화면에서 챗봇 호버효과 진해졌다가 흐려지는 기능 넣어줘."

**조치.** `assistant.js`(모든 설비 페이지 + `index.html`에 공통 로드되는 단일 파일이라 한 곳만 고치면 "모든 화면"에 반영됨)의 `.dnk-asst-btn`에 `::after` 후광(radial-gradient, QR 라벨·홈 버튼과 동일한 브랜드 시안 `#37c6e0`)을 추가하고, `@keyframes dnk-asst-glow`(opacity 0.3↔1, 1.6초 주기)를 마우스 hover 시에만 재생되도록 함. 기존 §4 인터랙션 원칙을 그대로 따라 `@media (hover:hover) and (pointer:fine)` 안에만 둬서, 터치 기기에서 탭한 뒤 손을 떼도 글로우가 들러붙는(sticky hover) 일이 없게 했고, `prefers-reduced-motion:reduce`에서는 애니메이션을 끄도록 예외 처리.

**검증.** 로컬 서버에서 CSSOM(`document.styleSheets`)으로 `::after` 규칙이 정상 파싱된 것, 기본 상태 `opacity:0`(안 보임)인 것을 확인. 자동화 브라우저의 `hover` 액션으로 실제 마우스 진입을 재현해 `:hover` 매칭과 `animation-name:dnk-asst-glow`가 정상 적용되는 것까지 확인했다 — 다만 이 자동화 브라우저는 탭이 백그라운드/비포커스 상태일 때 `requestAnimationFrame` 기반 애니메이션 진행이 멈추는 경향이 있어(`document.getAnimations()[].currentTime`이 0에서 안 움직임), 프레임 단위로 실제 밝기가 오르내리는 것까지는 이 환경에서 확인하지 못했다 — CSS 자체는 표준적인 무한 반복 키프레임이라 실제 포커스된 브라우저에서는 정상 동작할 것으로 판단.

**검증 중 발견한 별개 이슈(작업에 지장 없음, 참고용).** 수정 직후 로컬 서버로 열었던 탭에서 `getComputedStyle`로 확인했더니 방금 추가한 CSS 규칙이 아예 안 보이는 현상이 있었다 — 알고 보니 그 탭이 이전 테스트에서 이미 `assistant.js`를 한 번 캐시해둔 상태라, 파일을 고친 뒤에도 브라우저가 기존 캐시본을 계속 쓰고 있었던 것(`fetch(url,{cache:'no-store'})`로 서버 쪽엔 새 내용이 있는 걸 확인해서 서버 문제가 아님을 확인). `fetch(url,{cache:'reload'})`로 캐시를 강제 갱신한 뒤에야 새 CSS가 반영됨. 로컬 정적 서버(`python -m http.server`)는 별다른 `Cache-Control` 헤더를 안 보내서 브라우저가 휴리스틱 캐싱을 적극적으로 하는 것으로 보인다 — 로컬 테스트 중 "분명히 고쳤는데 반영이 안 된다"싶으면 실제 코드보다 브라우저 캐시부터 의심할 것 (§17 반영).

---

## 2026-09-22 챗봇 글로우 효과 "전혀 반응이 없다" — PC hover 전용 설계가 원인

**배경.** 배포 확인까지 마친 위 글로우 효과에 대해 사용자가 "전혀 반응이 없다"고 보고. "PC인지 스마트폰인지" 물었더니 **"둘 다"**라는 답변.

**원인분석.** 로컬에서 직접 재현해보니 코드 자체는 정상이었다 — 자동화 브라우저로 실제 마우스 진입을 재현해 `getComputedStyle`로 `opacity`가 시간에 따라 0.2~1 사이에서 실제로 움직이는 것, `animation-name`·`background-image`·좌표가 전부 의도대로인 것까지 확인. 문제는 **설계 자체**에 있었다: 이 효과를 `@media (hover:hover) and (pointer:fine)` 안에만 넣어 **PC의 마우스 hover에서만** 재생되게 만들었었다(§4의 "터치 기기에서 sticky hover 방지" 기존 원칙을 그대로 따른 것). 그런데 스마트폰(터치 기기)에는 `hover` 자체가 존재하지 않으므로 — 아무리 손가락으로 눌러도 이 조건은 절대 참이 될 수 없다. 즉 스마트폰에서 "반응이 없다"는 건 버그가 아니라 **설계상 당연한 결과**였다. PC에서도 반응이 없었다는 보고는, 최초 요청이 "모든 화면에서"였던 걸 감안하면 사용자가 실제로 테스트한 게 PC가 아니었거나(우선순위상 스마트폰 위주로 테스트하는 경향이 이 대화 내내 있었음), 혹은 효과 자체가 미미해서 못 알아챘을 가능성이 있다 — 근본적으로 "터치에서 아예 안 뜬다"는 문제가 있었으므로 더 캐지 않고 바로 재설계했다.

**조치.** hover 조건을 완전히 제거하고, **글로우가 hover 여부와 무관하게 항상 재생되는 앰비언트 애니메이션**으로 전환(`.dnk-asst-btn::after{...animation:dnk-asst-glow 1.8s ease-in-out infinite;}`를 미디어쿼리 밖으로 뺌). 동시에 눈에 더 잘 띄도록 후광 범위(`inset:-8px`→`-14px`)와 최대 밝기(중심 알파 `.65`→`.9`, 최고 opacity 그대로 1이지만 최저를 `.3`→`.2`로 낮춰 명암 대비를 키움)를 키웠다. `prefers-reduced-motion:reduce`에서는 애니메이션 대신 고정된 은은한 밝기(`opacity:.45`)로 대체(완전히 없애지 않고 정적 힌트는 남김).

**검증.** 로컬 서버에서 마우스를 올리지 않은 기본 상태에서도 `animationPlayState:"running"`인 것을 확인하고, 시차를 두고 두 장의 스크린샷을 찍어 데스크톱·모바일(375px) 양쪽에서 버튼 주변 시안 후광이 실제로 옅다가 진해지는 것을 육안으로 확인(이전엔 오피니언대로 "코드는 맞는데 화면으로 확인은 못함" 수준이었다면, 이번엔 후광 범위를 키운 덕에 스크린샷에서도 뚜렷하게 보임).

**교훈(기록).** ① "hover 효과"라는 요청을 받으면 "터치 기기에는 hover라는 개념 자체가 없다"는 전제를 먼저 사용자와 맞춰야 한다 — 이 프로젝트는 QR 스캔으로 스마트폰이 주 사용 환경인데, 정작 "모든 화면에서"라는 요청을 문자 그대로 지키려면 hover에 의존하는 상호작용을 애초에 선택하면 안 됐다. 사용자가 "hover"라고 말해도 실제 의도는 "주목을 끄는 효과를 넣어달라"였을 가능성이 크고, 그렇다면 hover 대신 상시 재생 애니메이션이 더 맞는 해법이다. ② 코드가 논리적으로 맞다는 걸 확인했다고 해서 "사용자 눈에도 보인다"는 게 보장되지 않는다 — 이번엔 실제로 설계(조건 범위)에 문제가 있었다. §17에 반영.
